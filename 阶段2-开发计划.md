# 阶段2开发计划(修订版 v12):Sidecar 编排层 + 中央配置

> 本计划面向执行 agent。**只做阶段2,不要继续阶段3**。
> 前置:阶段1(Tauri 壳 + 单 Agent 进程管理 + 流式输出)已完成并通过验收。
> 代码注释用中文,关键逻辑写清"为什么这么做"。
> 交付后停下,告诉用户如何测试,创建了哪些文件,等用户反馈。
>
> **v2 修订依据**:《AgentHub Desktop 项目开发任务书.md》《AgentHub Desktop 设计规范.md》《阶段1-开发计划.md》与《阶段1-验收报告.md》(2026-09-13,结论"部分通过,零功能失败项")。修订内容是**对阶段1 实际交付代码的核对结果**与几处技术缺陷的更正,不改变阶段2 的目标与范围。
>
> **v3 修订依据**:第一轮独立审查 13 条,逐条核对后全部成立,已全部收敛(其中 2 条把 v2 的方向又改了回来,见 v3 的 11、12)。
>
> **v4 修订依据**:第二轮独立审查 8 条,逐条核对后全部成立,已全部收敛(并发保护只做了一半、同一规则前后不一致、状态模型缺件)。
>
> **v5 修订依据**:第三轮独立审查 5 条,逐条核对后全部成立,已全部收敛。共性是**"状态在某个时刻无法收敛"**。
>
> **v6 修订依据**:第四轮独立审查 5 条,逐条核对后全部成立,已全部收敛。共性是**"一条规则被用在了两种性质不同的东西上"**。
>
> **v7 修订依据**:第五轮独立审查 3 条,逐条核对后全部成立,已全部收敛。共性是**"修了一半"**。
>
> **v8 修订依据**:第六轮独立审查 4 条,逐条核对后全部成立,已全部收敛。3 条是 v7 新引入的——新机制没检查与既有约定的交叉点。
>
> **v9 修订依据**:第七轮独立审查 3 条,逐条核对后全部成立,已全部收敛。三条都是"广播 / 快照 / 延时回调三者的先后顺序"。
>
> **v10 修订依据**:第八轮独立审查 2 条,逐条核对后全部成立,已全部收敛。补上了"刷新后、快照返回前"那个零本地状态的窗口。
>
> **v11 修订依据**:第九轮独立审查 3 条,逐条核对后全部成立,已全部收敛。这一轮把前面几轮**零散补出来的同一个道理,提炼成了一条贯穿全篇的原则**:
>
> > **字段按"归属"分两类,各自有各自的判据**:
> > - **运行身份**(`runId`、`resolvedModel`、`injectedEnv`、`cwd`)——判据是 "**是不是同一轮 `runId`**",整轮运行内不变
> > - **当前相位**(`phase`、`result`、`lastError`)——判据是 "**`rev` 有没有变**",随时会翻
>
> 回头看,v8 的 `StartAck` 按 `runId` 归属、v10 的事件建立记录、v9 的 `rev` 判快照过期——**其实都是这一条原则的不同侧面,但前面几轮都是按场景逐个补的**。v11 把它写进正文,并据此修掉两处:
> - 快照的 `resolvedModel` 被"整组丢弃"连坐(它属于运行身份,只该看 `runId`)
> - `agent/state` 不带模型名,导致事件建出的记录残缺
>
> **给后续阶段的经验(接 v7–v10 四条)**:同一个道理**第三次**以新场景出现时,就该停下来把它抽成一条原则写进文档,而不是再补一个特例。**特例补得越多,不一致的地方就越多**——这正是这几轮反复出现"前面改了、后面没改"的根源。
>
> **v12 修订依据**:第十轮独立审查 2 条,逐条核对后全部成立,已全部收敛。两条都是 v11 那条"运行身份按 `runId`、相位按 `rev`"原则**自己留下的缺口**:
> - **身份侧的例外分支**("本地 `runId` 不同、`rev` 未变时仍采纳快照")没有问"**本地这条记录是谁写的**"——本地新写入的 `runId` 不递增任何计数器,于是它在途快照面前毫无防御,会被写回旧轮次。
> - **"事件自足"只落实到了一半**——`agent/state` 补了 `resolvedModel`,两个**终态事件漏了**。而刷新后最先到达的完全可能是一场退出,进程随即从池里删除,快照再也补不回模型名。
>
> **给后续阶段的经验(接 v7–v11 五条)**:**一条原则定下来之后,要立刻拿它去逐条复核所有已存在的分支,尤其是"例外分支"。** v11 提炼出的原则是对的,却给身份侧留了一个例外——**而例外恰恰是原则最容易被绕过的地方**。写完一条原则,紧接着的动作应该是"照它把所有分支重列一遍",而不是"原则定了就收工"。
>
> **v2 主要变更(共 13 条;**其中 5、6、11 三条已被 v3 推翻,保留仅作修订历史,以 v3 为准**)**:
> 1. 删除对 `src/App.css` 的修改(该文件阶段1 已删除,样式应进 `src/index.css`)
> 2. 删除"沿用阶段1 已搭好的布局骨架"的错误前提——阶段1 **没有**建布局骨架,本阶段必须从零搭建
> 3. 阶段1 的 `agent-output` / `agent-exit` **改名为** `diagnostic-output` / `diagnostic-exit`,消除与阶段2 事件的串扰(见 §协议契约 C)
> 4. GBK 解码位置更正:`readline` 默认按 UTF-8 解码,中文 Windows CLI 的 GBK 字节在 Node 侧就已损坏,**Rust 侧回退无效**;改为 Sidecar 内按 Buffer + 两级解码
> 5. 配置去注释必须写成**带字符串状态机的扫描器**,否则会吃掉 `http://127.0.0.1:8787` —— **【v3 已推翻,改用 `json5`,见 v3 第 11 条】**
> 6. `startAgent` 只回传**注入层**环境变量(不回传继承的完整 `process.env`),且对密钥做掩码 —— **【前半句保留;后半句"掩码 + 显示完整值开关"已被 v3 推翻,而 v3 的替代说法又被 v4 修正,以 v4 第 4 条为准】**
> 7. `listAgents` 必须回传**运行时状态**,否则 F5 刷新后前端不知道哪些 Agent 在跑(阶段1 用 `get_agent_status` 解决过同一问题)
> 8. `scripts/env-dump.js` 必须能**常驻长跑**(默认打印后每秒心跳),否则 B 组一半验收项无法执行
> 9. 协议类型改放 `shared/protocol.ts`,前后端共用一份;说明两边的 tsconfig/相对路径怎么写
> 10. sidecar 构建接进 `tauri.conf.json` 的 `beforeDevCommand` / `beforeBuildCommand`,不再只依赖 `.bat`
> 11. 验收 B.9 的"F12 控制台"**当前不可执行**(验收报告已实测 DevTools 被本构建禁用),改为 dev-only `additionalBrowserArgs` + 可机测的替代判据 —— **【v3 已推翻,改为直接删掉该验收点、不引入调试端口,见 v3 第 12 条】**
> 12. 验收 A.1 的 `echo '...'` 是 bash 语法,Windows cmd 下不成立,换成 cmd/bash 通用的 `node -e` 写法
> 13. Sidecar 启动时机从"第一次 `start_agent` 才拉起"改为"随界面首次取 Agent 清单时启动"——原方案与 Agent 下拉需要 roster、刷新恢复需要 roster 两件事直接冲突(死结),详见 §关键逻辑 2.1
>
> **v3 主要变更(共 13 条,编号与独立审查一致)**:
> 1. **操作令牌与 phase 按 agentId 拆分**——全局 `opRef` 在并发下会让另一个 Agent 卡在"正在启动/正在终止"(§前端 3.1)
> 2. **JSON-RPC 错误响应必须带 `error.data.kind`**——否则 Rust 只能靠匹配文案猜,前端的近十种分支无法实现(§协议契约 A)
> 3. **补全 Agent 池的退出清理**——明确"先删记录、后发事件",并写清秒退竞态的处理(§关键逻辑 1.4)
> 4. **引入 Windows Job Object**——Sidecar 被强杀时其 Agent 会变孤儿,原方案完全没管;本阶段实现,不再推到阶段6(§关键逻辑 2.1)
> 5. **配置校验拆成两级**——原方案下 `configured` 字段永远为真,是死代码;拆分后单个 Agent 写错不牵连其它(§关键逻辑 1.1)
> 6. **密钥原文不出 Sidecar**——原方案的"掩码 + 显示完整值开关"协议上无法实现;改为只回 `{ set, length }`(§关键逻辑 1.2)。**【v4 修正:这只堵住了 `startAgent` 响应这一条通道,`env-dump.js` 的输出区仍在明文打印,见 v4 第 4 条】**
> 7. **`stop_agent` 的 `runId` 改为必填**——它是防误杀的唯一凭据,不留可选口子(§协议契约 B)
> 8. **删掉 `## 与阶段1 的衔接` 里残留的旧表格**——v2 改写时留下的 5 行旧方案(含"不需要改事件名")与正文的改名决定直接冲突(§与阶段1 的衔接)
> 9. **合并为唯一启动入口**——底部输入框发送即启动,主内容区不再放启动按钮(§前端 3.2)
> 10. **主内容区瘦身**——完整环境变量表改为折叠详情,删除未实现的附件按钮(§前端 3.2)
> 11. **改用 `json5`,不自己写注释扫描器**——降低核心链路风险,且异常自带行号列号(§关键逻辑 1.1);**这条推翻了 v2 第 5 条**
> 12. **删掉"dev-only 调试端口"这个验收点**——v2 没说清怎么隔离,有把端口带进正式包的风险;真正判据(输出不重复)已可机测(§验收 D);**这条推翻了 v2 第 11 条**
> 13. **`typecheck:sidecar` 串进 `build:sidecar`**——esbuild 只转译不检查类型,原方案下协议字段写错也能"构建成功"(§实施步骤 4)
>
> **v4 主要变更(共 8 条,编号与第二轮审查一致)**:
> 1. **同一个 Agent 可能被并发启动两次**——"检查"与"占坑"之间隔着一个 `await`,两个请求都能通过检查。改为**同步检查 + 同步插入 `starting` 占位记录**,再去 spawn(§关键逻辑 1.4)
> 2. **`cwd` 与 `args` 的路径基准自相矛盾**——`cwd: ".."` 却配 `args: ["../scripts/env-dump.js"]`,进程会去找项目根的上一级。改为 `args: ["scripts/env-dump.js"]`,并写死"只有 `cwd` 被解析,`command`/`args` 原样透传"的规则(§关键逻辑 1.1)
> 3. **验收 9 用了一个不存在的功能**——`args` 从来没有占位符展开。改用 `default_model` 指向不存在的别名(§验收 A)
> 4. **"密钥不出 Sidecar"是错的**——`env-dump.js` 的 stdout 本身就是一条经 `agent/output` 回流到界面的通道。密钥类变量在**注入详情与输出区两处**都只打印 `已设置(共 N 位)`(§关键逻辑 1.2、1.3.1)
> 5. **前端状态模型缺件**——`runtimesRef` 单靠 ref 不会触发重渲染;`AgentRuntime` 少了 `injectedEnv`/`cwd`;启动时必须先写 runId 再 invoke。补齐为 state + ref 双份 + `patchRuntime()`(§前端 3.1)
> 6. **模型别名清单没有数据来源**——`AgentInfo` 新增 `modelAliases`,由配置算出,前端不写死(§协议契约 A、§关键逻辑 1.1)
> 7. **Sidecar 自动重启可能起出两个**——`ensure_started()` 加独立 `start_lock` 做双重检查锁定(§关键逻辑 2.1)
> 8. **Job Object 失败降级与验收 27/32 打架**——失败不阻断应用,但 `jobObject` 状态必须暴露到界面,验收项改为**以 `jobObject === true` 为前置的条件项**(§关键逻辑 2.1、§验收 C)
>
> **v5 主要变更(共 5 条,编号与第三轮审查一致)**:
> 1. **刷新落在 `starting` 时恢复不出来**——`AgentInfo` 把 `running: boolean` 换成 `phase`;前端据此恢复到 `starting` 并保持终止禁用,与 `stopAgent` 对 `starting` 记录返回 `stopped:false` 对齐。同时补上"占位记录被移除时必发终态通知"这条不变式(`agent/error`),让**刷新已经销毁了原调用者**的情况下状态也能收敛(§协议契约 A、§关键逻辑 1.4、§前端 3.1)
> 2. **配置热重载漏了 `listAgents`**——被置灰的 Agent 根本启动不了,也就永远触发不了重载,"改回配置无需重启"直接不成立。`ensureConfigFresh()` 覆盖 `startAgent` / `listAgents` / `configReload` 三个入口,并给前端补上重新取 roster 的触发点(§关键逻辑 1.1、§前端 3.1)
> 3. **`jobObject` 刷新后丢失**——它只在事件里,而 Sidecar 已活着时不会产生新事件。改为同时放进 `list_agents` 返回值(`sidecar.jobObject`),并修好死亡事件缺该字段的不一致(§协议契约 A/B、§关键逻辑 2.1)
> 4. **"密钥不进入 `agent/output`"超出了实现边界**——Sidecar 对 Agent 输出是**原样转发、不脱敏**。改写为"阶段2 的 `env-dump.js` 不输出密钥原文",并留阶段3 的重新评估注记(§关键逻辑 1.2、§验收 36)
> 5. **`SidecarClient` 结构示例没跟着正文更新**——补上 `alive` / `start_lock` / `job` / `job_object_ok`,并说明与协议字段的对应关系(§关键逻辑 2.1)
>
> **v6 主要变更(共 5 条,编号与第四轮审查一致)**:
> 1. **快照接受规则把两种数据混用了**——"只在 `subscribing`/`unknown` 接受"对运行时状态是对的,但会连**配置元数据**一起丢掉,于是 `ready` 状态下点开下拉重新加载配置时,新别名与修好的 `configured` 全部不生效。拆成**元数据(永远接受最新的,按请求序号防乱序)**与**运行时状态(只在通道不活时接受 + opToken 校验)**两组;`sidecar.version` 定为**仅显示用**(§前端 3.1)。**【v9 修正:运行时那一组的判据改为 `rev`;v11 又进一步拆成"运行身份 / 当前相位"两组,以 v11 为准】**
> 2. **缺 `terminating` 相位、终止不幂等**——`killed === true` 时直接返回 `{ stopped: true }` 不再碰 `child`;`AgentInfo.phase` 增加 `terminating`;3 秒超时**回滚 `killed = false`**(否则记录永远卡在"终止中",用户再也无法终止它)(§协议契约 A、§关键逻辑 1.4)
> 3. **`SidecarClient` 的锁设计过不了编译**——`child`/`stdin` 裸着,`ensure_started(&self)` 无法替换。改为把本代进程状态整体关进 `SidecarProc`,由一把 `proc` 锁持有;明确三把锁的职责与**"任何时刻最多持有一把"**的铁律,并给出 `spawn_locked` 的写法(§关键逻辑 2.1)。**【v8 修正:这条规则与启动代码矛盾,已改为分层锁序,见 v8 第 3 条】**
> 4. **配置里删掉的运行中实例会"人间蒸发"**——`listAgents` 只遍历配置,进程还在跑却从界面消失、再也无法终止。改为**配置 ∪ 池记录**合并展示,孤立实例标 `configured: false` + 原因,可终止,终止后自然消失(§关键逻辑 1.4)
> 5. **验收 16 的时序没法稳定复现**——`starting` 只持续几毫秒,人工按不出 F5。新增 `start_delay_ms` **测试钩子**(默认 0,注释写明仅测试用),并指出②也可以直接在协议层断言 `agent/error`,不必抢时间(§关键逻辑 1.4、§验收 16)
>
> **v7 主要变更(共 3 条,编号与第五轮审查一致)**:
> 1. **致命配置错误会让运行中的实例失去可操作性**——`list_agents` 不再整体失败,改为返回**部分成功**:`agents`(池记录 ∪ 能解析的配置)+ `config: { ok, error? }`,配置问题作为**数据**而不是**错误**返回。前端收到 `config.ok === false` 时**不压通道**,只显示红字,已有实例照常可见可终止。`configInvalid` 现在**只由 `start_agent` 产生**(§关键逻辑 1.1、§协议契约 B、§前端 3.1)
> 2. **终止超时回滚没有广播**——新增非终态通知 **`agent/state`**;在 `starting→running`、`running→terminating`、`terminating→running`(超时回滚)三处发出。同时明确 **`stopAgent` 返回的 `stopped: true` 只是"请求已受理",不是终态承诺**——前端收到后只推进到 `terminating` 并等事件(§协议契约 A、§关键逻辑 1.4、§前端 3.1)
> 3. **读线程无法比较代际**——v6 把 `generation` 收进 `SidecarProc` 锁内,却又要求读线程不锁 `proc`,自相矛盾。改为**锁外的 `Arc<AtomicU64> current_gen`** 作为唯一真相来源,读线程 / 守护线程在 spawn 时捕获本代编号、动作前原子比对(§关键逻辑 2.1)
>
> **v8 主要变更(共 4 条,编号与第六轮审查一致)**:
> 1. **`agent/state` 误解锁了它自己那次启动**——`spawn` 成功后 Sidecar 先发 `running` 广播、再返回 `StartAck`;广播若递增 `opToken` 就会把紧接着的 `StartAck` 判为作废,**注入详情间歇性空白**。改为:**`agent/state` 不递增 `opToken`**(只有终态事件才递增),且 `StartAck` 按 **`runId` 归属**应用、相位仅在仍是 `starting` 时推进(§协议契约 A、§前端 3.1、§验收 40)
> 2. **`listAgents` 部分成功的协议没补全**——RPC 返回值漏了 `config`;孤立实例 / 配置解析失败时必填的 `defaultModel`、`modelAliases` 没有取值规则。补齐 RPC 字段、逐情形定死取值、明确"谁产生哪个字段"、并写出前端对这些记录的界面契约(§协议契约 A/B)
> 3. **"任何时刻最多持有一把锁"与启动代码自相矛盾**——`ensure_started` 必然同时持有 `start_lock` 与 `proc`。改为**分层锁序**:`start_lock → proc` 是唯一允许的嵌套,禁止反向;`pending` 与两者互斥(§关键逻辑 2.1)
> 4. **验收 38 在 Windows 上无法制造超时**——Node 的 `child.kill()` 走 `TerminateProcess`,脚本忽略不掉。新增 `stop_delay_ms` 测试钩子(与 `start_delay_ms` 对称,默认 0),并明确"3 秒超时从收到请求起算"这一语义(§关键逻辑 1.4.1、§验收 38)
>
> **v9 主要变更(共 3 条,编号与第七轮审查一致)**:
> 1. **旧快照能盖掉 `agent/state` 的结果**——v8 让 `agent/state` 不递增 `opToken`(为了不误伤 `StartAck`),于是快照的校验形同虚设,能把 `running` 改回 `starting`。新增**每 Agent 的状态修订号 `rev`**(任何写运行时记录的事件都递增;`opToken` 仍只管终态),快照携带请求时的 `rev`,不一致就丢弃运行时字段(§前端 3.1、§验收 39)
> 2. **终止失败只在"3 秒超时"这一支做了回滚 + 广播**——`kill()` 立即抛错但进程仍存活时没有规定,后果完全相同(记录卡 `terminating`、进程还在跑)。改为**统一收尾规则:只要没能确认进程已结束,一律回滚 + 广播 `agent/state{running}` + 返回 `stopFailed`**,不按失败原因分叉(§关键逻辑 1.4)
> 3. **`stop_delay_ms` 的延时回调会踩烂已经变过的状态**——它必然在超时回滚之后才醒来,不加校验就会把已经回到"工作中"的进程突然杀掉。新增 `termSeq`,并要求回调醒来后校验 `runId` / `killed` / `termSeq` 三项;验收 38 增加"回滚后再等过 `stop_delay_ms`,进程必须仍然活着"(§关键逻辑 1.4、1.4.1、§验收 38③)
>
> **v10 主要变更(共 2 条)**:
> 1. **刷新后、首次快照返回前到达的事件被丢弃**——此时新页面没有任何 `runId` 记录,"runId 必须一致"把事件挡掉;随后旧快照又因为"本地没有 `rev`"畅通无阻,`rev` 在这条路径上完全失效。改为**事件自己把记录建起来**(三行判定表:本地没有记录 / `runId` 为 `null` 时,用 payload 的 `runId` 建记录并 `rev += 1`),旧快照随即被判过期。这是阶段1 那条"runId 尚不可知时仍接受退出事件"的按 Agent 版本(§前端 3.1、§验收 41)
> 2. **`StdioExt` 是错的 trait 名**——提供 `creation_flags` 的是 **`std::os::windows::process::CommandExt`**,照抄会直接编译失败(§关键逻辑 2.1)
>
> **v11 主要变更(共 3 条)**:
> 1. **`agent/state` 不带模型名,事件建出的记录残缺**——刷新期间只收到这条广播时,界面会显示"工作中"却不报当前模型。给 `agent/state` 补上 `resolvedModel`,让**事件能独立支撑起一条完整记录**(§协议契约 A、§关键逻辑 1.4、§前端 3.1)
> 2. **快照的 `resolvedModel` 被"整组丢弃"连坐**——本地记录由事件建立、`runId` 与快照完全一致、缺的正是模型名,却因为 `rev` 不等被一起丢掉。把快照的运行时字段按**运行身份(`runId`/`resolvedModel`,判 `runId`)**与**当前相位(`phase`,判 `rev`)**拆成两组分别判定(§前端 3.1)
> 3. **`JobHandle` 的 `Send` 问题没交代**——`windows-sys` 的 `HANDLE` 是裸指针,不是 `Send`,`SidecarClient` 进不了 Tauri 状态。改用 **`std::os::windows::io::OwnedHandle`**(自带 `Send + Sync`,drop 即 `CloseHandle`)(§关键逻辑 2.1)
>
> —— 另:§前端 3.1 那张"快照接受规则"表此前仍写着"通过 `opToken` 校验",与正文的 `rev` 方案不一致,已统一(那是执行时最容易照着抄错的地方)。
>
> **v12 主要变更(共 2 条)**:
> 1. **在途快照能把本地新一轮的 `runId` 写回旧轮次**——v11 给运行身份留的例外("本地 `runId` 不同、`rev` 未变时仍采纳")是实质性漏洞:本地 `start()` 写入新 `runId` 时**不递增任何计数器**,所以旧快照能趁虚而入,把第 1 轮的 `runId` 与模型名写回,随后第 2 轮的 `StartAck` / 事件因 `runId` 不匹配被丢弃,整轮悬空。**删除该例外:本地 `runId` 非空且与快照不同 → 运行身份无条件丢弃**;同时把 `rev` 改成**逢写递增**(`patchRuntime()` 默认 `rev + 1`),本地 `start()` / `stop()` 也自动作废在途快照(§前端 3.1、§验收 42)
> 2. **两个终态事件不能独立构成完整记录**——`agent/exit` / `agent/error` 补上 `resolvedModel`。刷新后最先到达的可能就是一场退出,进程随即从池中删除,快照再也补不回模型名;Sidecar 侧要在**删池记录之前**把该值抄进局部变量(§协议契约 A/B、§关键逻辑 1.4、§验收 41③)

---

## Context

AgentHub Desktop 目前只有"能启动一个子进程并把输出显示出来"(阶段1)。阶段2 要引入**编排层**:一个常驻的 Node 进程(Sidecar)作为大脑,它维护中央配置、认识每个 Agent、按配置拉起 Agent 进程、注入模型路由环境变量,并把输出送回界面。

**这一阶段的真正目标不是"能启动 Agent",而是"配置可被用户掌握"**——用户改一下模型别名,Agent 启动时的环境变量就跟着变。这是任务书验收标准的原话,也是本项目"用户不懂编程但能自己调模型"这个核心诉求的第一次落地。

### 当前项目状态(已按阶段1 实际交付核对)

阶段1 由另一个 agent 实现并**已通过独立验收**(《阶段1-验收报告.md》,2026-09-13,零功能失败项)。以下是**实测核对后的事实**,执行前仍建议扫一眼确认:

| 事实 | 出处 |
|---|---|
| `src-tauri/src/lib.rs` 已有:进程槽位、stdout/stderr 读线程、`agent-output` / `agent-exit` 事件、结构化 `CmdError`、`RunEvent::Exit` 清理 | 验收报告 §2.1 |
| `scripts/test-agent.js` 已存在;`tauri.conf.json` 的 `bundle.resources` 已是**映射写法** `{"../scripts/test-agent.js": "scripts/test-agent.js"}` | 验收报告 §2.2 |
| `src/index.css` 已有设计规范全量变量(含完整深色变量组)+ 深色模式;**`src/App.css` 已被删除** | 验收报告 §2.2 |
| 前端已有一套经过评审的健壮性机制:`mountGenRef` 挂载代际、`opRef` 操作令牌、`phase` 单状态、`subscribing`/`unknown` 分离 | `src/App.tsx:106-229` |
| 阶段1 的命令只有 `spawn_test_agent` / `kill_test_agent` / `get_agent_status`;事件只有 `agent-output` / `agent-exit` | 验收报告 §2.1 |
| **`src-tauri/Cargo.toml` 包名仍是 `agenthub-tmp`**,产物为 `agenthub-tmp.exe` | 验收报告 §7.1 |
| **`index.html` 的 `<title>` 仍是模板值** `Tauri + React + Typescript`;favicon 仍指向 `/vite.svg` | 验收报告 §7.2 |
| `tauri.conf.json` 的 `beforeDevCommand` 是 `npm run dev`,`beforeBuildCommand` 是 `npm run build` | 本文件 §修改清单 |
| 项目**当前不是 git 仓库**(`git rev-parse` 报 `not a git repository`),`.gitignore` 存在但未生效 | 阶段2 核对 |

**阶段2 是加法的**:新增 Sidecar 相关文件,复用阶段1 已建立的进程管理经验与前端健壮性机制,**不推倒重写**。

> ⚠️ **一个必须更正的前提**:阶段1 计划明确把当时的界面定位为**临时诊断控制台**——"正式布局骨架在阶段2/3 搭建,不要在此页面扩展视觉体系"(阶段1 计划 §已确认的决策 2),且"阶段1 暂不引入 Tailwind/shadcn,手写 CSS"。所以**本阶段没有现成的布局骨架可以"沿用"**,侧边栏 / 顶部栏 / 主内容区 / 底部输入框要从零搭(见 §前端 3.2)。这不是重写阶段1,而是把阶段1 明确推迟的东西补上。

### 宿主机约束

- Windows 11;项目路径含空格(`D:\Project\Agent Hub Destop`)——所有路径必须走参数数组传递,禁止拼字符串命令行
- 中文 Windows:Agent 输出可能是 GBK,需解码兜底
- 用户不懂编程:每个可配置项都要能在配置文件里直观看到并修改

---

## 已确认的决策

1. **Rust 仍是"哑管道"**。任务书要求 Rust 只做进程管理和事件转发。Rust **不解析** `agents.json`、不拼命令、不认 Agent 名,这些全在 Sidecar。Rust 只做三件事:拉起/守护 Sidecar、按 id 转发 JSON-RPC、把通知转成 Tauri 事件。
2. **Sidecar 由 Rust 用 `std::process` 拉起,不引入 tauri-plugin-shell**。plugin-shell 封装掉的正是要验证的 stdio 长连接链路。
3. **Sidecar 编译为单文件 `.cjs`(esbuild),用系统 node 运行**。开发期不引入 node_modules 运行时依赖;阶段6 再决定是否做成独立 exe。
4. **`config/agents.json` 支持热更新**:`startAgent` / `listAgents` / `configReload` 三个入口都先做 mtime 检查,变了就重载。用户改完配置**不用重启应用**即可验证——这是验收标准"修改模型别名,启动 Agent 时环境变量随之改变"的前提。
5. **阶段2 只实现深色模式跟随系统**,手动切换开关留到阶段3 的设置页。
6. **同一个 Agent 同时只跑一个实例**。不同 Agent 可并行。理由:阶段2 的验证点是配置与注入,不是并发调度;并发同 Agent 会引入"哪个实例算当前状态"的歧义。阶段3 的 Hermes 调度再放开。
7. **任务书示例里的 `task` 字段改名为 `prompt`**。`task` 在阶段3 的调度 JSON 里是"子任务"含义,同一套代码里两个 `task` 会造成真实混淆。`agents.json` 的注释里说明这一点。
8. **阶段1 的两个事件改名为 `diagnostic-output` / `diagnostic-exit`**(本阶段唯一改到阶段1 代码语义的地方,共 4 处字符串)。理由见 §协议契约 C——同名事件会让诊断控制台被阶段2 的 Agent 退出事件误推进。
9. **GBK 解码放在 Sidecar 内部做,不指望 Rust 兜底**。`readline` 默认用 UTF-8 解码子进程输出,中文 Windows CLI 吐出的 GBK 字节到 Node 手里已经变成 `U+FFFD`,Rust 侧再怎么 `from_utf8 失败 → GBK` 都救不回来(那时它已经是合法 UTF-8 了)。详见 §关键逻辑 1.3。
10. **协议类型放在根目录 `shared/protocol.ts`,前后端共用一份**。Sidecar 与前端是两个 tsconfig,一份类型放两边都会漂移;放共享目录、双方用相对路径引用即可,不需要路径别名。详见 §协议契约 开头。
11. **`scripts/env-dump.js` 默认常驻长跑**(打印环境变量后每秒一行心跳),另支持 `--once` 立即退出。原因见 §关键逻辑 1.3 与 §验收 B 组:阶段1 正是因为"打印一行就退出"而不得不额外造一个"Node 快速退出"预设,本阶段不要重蹈覆辙。
12. **`startAgent` 只回传"注入层"的环境变量**,不回传继承来的完整 `process.env`。既避免把用户机器上所有环境变量(含其它密钥)灌进 WebView,也避免注入预览被几百行 PATH 淹没。**密钥类只回传 `{ kind: "secret", set, length }`**;并且 `env-dump.js` 的输出区对密钥同样只报"已设置/长度"——**两条通道都要堵**(见 §关键逻辑 1.2)。**注意这条保证的边界**:Sidecar 对 Agent 输出是原样转发、不脱敏的,阶段2 之所以成立是因为所有 Agent 都指向 `env-dump.js`;阶段3 接入真实 CLI 后须重新评估。
13. **阶段2 不做设置页,也不引入 Tailwind/shadcn**,继续手写 CSS + `src/index.css` 的 CSS 变量。设置页(规范 4.4)留给阶段3;组件库是否引入留到阶段3 一并决定。理由:设计规范 §10.10 允许"基于 shadcn/ui 自定义,**或手写组件**",当前阶段用 CSS 变量已经能满足规范,引入构建链改动会稀释阶段2 的验证重点。

---

## 文件清单

### 新建

| 文件 | 作用 |
|---|---|
| `config/agents.json` | **中央配置文件**。Agent 清单、模型别名映射、环境变量注入模板、CC Switch 地址。用户手改的入口 |
| `scripts/env-dump.js` | **阶段2 的验证核心**。读取真实 env,打印注入的模型路由变量 + 全部 `AGENTHUB_*` 变量。**默认打印后常驻**(每秒一行心跳 + 收到终止时退出),`--once` 打印完立即退出。用户改配置后靠它肉眼比对,B 组的运行中/终止/并发验收也都靠它 |
| `sidecar/package.json` | Sidecar 自己的包:`"private": true`、`"type": "module"`。**devDependencies 留空**——`esbuild` / `typescript` / `@types/node` 统一装到**根** `package.json`(全项目只有一份 `node_modules`,分两处装会得到两份不同版本的 esbuild) |
| `sidecar/tsconfig.json` | 独立 tsconfig(不继承根 tsconfig,避免被 Vite 的 bundler 模式影响) |
| `sidecar/src/index.ts` | 入口:建 RPC server、装退出兜底、注册方法表 |
| `sidecar/src/rpc.ts` | JSON-RPC 帧读写:**stdin 侧**用 readline 按行读(Rust 写来的一定是 UTF-8 JSON,直接用没问题);**stdout 侧**按行写,写入串行化。响应收集器 |
| `shared/protocol.ts` | **前后端唯一一份协议类型**:RPC method 名、params/result 类型、通知与事件 payload、`CmdError` 的 `kind` 联合类型。Sidecar 与前端各自用相对路径引用它 |
| `sidecar/src/types.ts` | 仅 **Sidecar 内部**类型:配置文件结构(`AgentsConfig`)、模板占位符类型等。协议类型一律从 `../../shared/protocol` 导入,不在此重定义 |
| `sidecar/src/config.ts` | 配置加载:路径解析(相对路径按配置文件所在目录转绝对)、mtime 缓存、校验与友好报错 |
| `sidecar/src/model.ts` | 模型别名解析 + 环境变量注入(按 Agent 类型套模板) |
| `sidecar/src/agent-process.ts` | 单个 Agent 进程:spawn、stdout/stderr **按 Buffer 读 + 两级解码**(见 §关键逻辑 1.3)、退出事件、清理 |
| `sidecar/src/pool.ts` | Agent 池:`startAgent` / `stopAgent` / `shutdownAll`,runId 管理、幂等终止 |
| `sidecar/dist/sidecar.cjs` | 构建产物(不提交 git) |
| `src-tauri/src/sidecar/mod.rs` | Rust 侧 Sidecar 客户端:启动时机(见 §2.1)、请求/响应配对、健康检查、重启、退出清理 |
| `src-tauri/src/sidecar/protocol.rs` | RPC 报文结构(serde)、`error.data.kind` → `CmdError.kind` 的映射、`CmdError` 定义 |
| `src-tauri/src/sidecar/job.rs` | Windows Job Object 封装:创建 / 指派 Sidecar / `TerminateJobObject` / **句柄存 `std::os::windows::io::OwnedHandle`(自带 `Send + Sync`、drop 即关闭)** / **暴露是否指派成功**。非 Windows 平台为空实现 |
| `src/lib/errors.ts` | 前端解析 Rust 结构化 `CmdError`(`errKind` / `errMessage`),`kind` 联合类型从 `shared/protocol` 导入 |
| `src/components/AppShell.tsx` | **主框架**(规范 3.1):侧边栏 + 顶部栏 + 主内容区 + 底部输入框。阶段1 没有这个文件,本阶段新建 |
| `src/hooks/useSidecar.ts` | **前端核心**:事件订阅、启动/终止、状态同步,承接阶段1 的健壮性机制 |
| `src/components/AgentStatusList.tsx` | 侧边栏底部 Agent 状态指示器(规范 4.3),工作中显示当前模型 |
| `src/components/OutputView.tsx` | 流式输出区:等宽字体、自动滚动、用户上滚暂停 +「回到最新」(规范 5.4) |
| `src/components/RunView.tsx` | 运行视图(规范 §3.4):运行状态行 + 输出 + 可折叠的注入摘要。**不含启动按钮**(唯一入口是 `PromptInput`) |
| `src/components/PromptInput.tsx` | 底部输入框(规范 §3.5):上方 Agent 选择 + 模型别名选择,下方任务描述。**发送 = 阶段2 唯一的启动入口**;`Enter` 发送 / `Shift+Enter` 换行、空内容或通道未就绪时禁用 |

### 修改

| 文件 | 改动 |
|---|---|
| `src-tauri/Cargo.toml` | `[package] name` → `agenthub-desktop`;`[lib] name` → `agenthub_desktop_lib`;加 `uuid`(v4,请求 id)与 **`windows-sys`**(Job Object 的 FFI,仅 `[target.'cfg(windows)'.dependencies]`) |
| `src-tauri/src/main.rs` | crate 名改 `agenthub_desktop_lib::run()` |
| `src-tauri/tauri.conf.json` | ① `bundle.resources` **追加** `"../sidecar/dist/sidecar.cjs"` 与 `"../config/agents.json"` 的映射(**映射写法不能改**,否则进 `$RESOURCE/_up_/`;已有的 `test-agent.js` 映射保持不动);② `beforeDevCommand` → `"npm run build:sidecar && npm run dev"`,`beforeBuildCommand` → `"npm run build:sidecar && npm run build"`(**类型检查含在 `build:sidecar` 里**,见下)。**不加任何 WebView 调试参数**——理由见 §验收 D |
| `package.json` | 加 `"build:sidecar"`(**内含类型检查**,见下)与 `"typecheck:sidecar"`;devDeps 加 `esbuild`、`json5`、`@types/node`。**不需要加 `tauri:dev`**——已有的 `"tauri": "tauri"` 脚本已支持 `npm run tauri dev` |
| `tsconfig.json` | `include` 从 `["src"]` 扩为 `["src", "shared"]`(前端要引用 `shared/protocol.ts`);`sidecar/tsconfig.json` 的 `include` 覆盖 `["src", "../shared"]` |
| `src/App.tsx` | ① 主内容区接入 `RunView` + `OutputView`,侧边栏底部接入 `AgentStatusList`,底部接入 `PromptInput`;② **阶段1 的诊断控制台改名为「诊断」导航项**(条件渲染,切走即卸载,不删);③ 监听的事件名改为 `diagnostic-output` / `diagnostic-exit` |
| `src/index.css` | 追加本阶段布局(侧边栏/顶部栏/主内容区/底部输入框)与三个组件的样式。全部引用已定义的 CSS 变量。**注意 `src/App.css` 在阶段1 已删除,不要新建它** |
| `index.html` | `<title>` 由模板值改为 `AgentHub Desktop`;favicon 指向 `public/tauri.svg` 或换成项目图标(顺手清理阶段1 验收报告 §7.2 记录的遗留) |
| `src-tauri/src/lib.rs` | ① 挂载 `sidecar` 模块与状态;② 注册 `start_agent` / `stop_agent` / `list_agents`;③ `RunEvent::Exit` 追加 Sidecar 清理;④ **阶段1 的两处 `emit` 事件名改为 `diagnostic-*`**。阶段1 的进程管理逻辑本身不动 |
| `.gitignore` | 加 `sidecar/dist/`。**注意:项目当前还不是 git 仓库**(`git rev-parse` 报 `not a git repository`),该文件暂时不生效;阶段5 用 Worktree 前需要先 `git init`,这条先写好备用 |
| `启动开发预览.bat` | 保持不变即可——sidecar 构建已接进 `tauri.conf.json` 的 `beforeDevCommand`,`.bat` 只需继续 `npm run tauri dev` |
| `README.md` | 补:如何改 `config/agents.json`、CC Switch 需用户自行启动并配置路由 |

---

## 协议契约(前后端必须一致)

> **类型的唯一出处是 `shared/protocol.ts`**。Sidecar 用 `../../shared/protocol` 导入,前端用 `../../shared/protocol` 导入(两边都用相对路径,不引入 tsconfig paths 别名)。Rust 侧只能手写对应的 `serde` 结构——注意 Rust 的结构体与 TS 类型**没有编译期联动**,任何一方改了字段必须同时改另一方,这是本阶段最容易悄悄出错的地方:凡是动到 `shared/protocol.ts` 的提交,都要在 PR/提交说明里点名"Rust 侧需同步"。

### A. Rust ↔ Sidecar(JSON-RPC 2.0 over stdio,**按行分隔的 JSON**)

> **关键约束:stdout 是唯一的数据通道**。Sidecar 里任何调试打印必须走 stderr,严禁 `console.log`。

**Rust → Sidecar 请求**

| method | params | result |
|---|---|---|
| `ping` | `{}` | `{ ok: true, version: string }` |
| `configReload` | `{}` | `{ ok: true, agents: string[], path: string }` |
| `startAgent` | `{ agentId, prompt, model?, runId }` | `{ agentId, runId, resolvedModel, injectedEnv, cwd }` |
| `stopAgent` | `{ agentId, runId }` | `{ stopped: boolean }` |
| `listAgents` | `{}` | `{ agents: AgentInfo[], config: { ok, error? }, version: string }` |

> **注意 `list_agents` 命令的返回值与这个 RPC 结果不同**:`jobObject` / `available` 是 **Rust 侧**的概念(Job Object 由 Rust 创建,`available` 是 Rust 对 Sidecar 存活性的判断),Sidecar 根本不知道。所以 Rust 在把 RPC 结果透传给前端时会**补上这两个字段**:
> ```
> list_agents 命令 → { agents, sidecar: { available, jobObject, version } }
> ```
> 具体见 §协议契约 B 与 §关键逻辑 2.1 的"Job 状态怎么同步到前端"。**这也是 finding「`jobObject` 刷新后丢失」的根治办法**——把它挂在一个前端本来就会在挂载时调用的命令上,而不是只靠一个可能已经错过的事件。
| `shutdown` | `{}` | `{ ok: true }` |

```ts
// AgentInfo —— 必须带运行时状态,理由见下方说明
type AgentInfo = {
  id: string
  label: string
  defaultModel: string
  /** 这个 Agent 可选的模型别名(如 ["fast","balanced","powerful"]),界面按它渲染下拉 */
  modelAliases: string[]
  /** 配置里 command / 别名 / cwd 是否都可用(静态检查,不 spawn) */
  configured: boolean
  /** configured 为 false 时的原因,界面直接显示;为 true 时省略 */
  reason?: string
  /** 运行时相位。没有实例时是 "idle" */
  phase: "idle" | "starting" | "running" | "terminating"
  /** phase 不为 "idle" 时的本次 runId,否则 null */
  runId: number | null
  /** phase 不为 "idle" 时实际解析出的模型名,否则 null */
  resolvedModel: string | null
}
```

**"孤立实例"和"配置整体解析失败"时的取值 —— 必须逐字段定死,否则协议不可实现**

`label` / `defaultModel` / `modelAliases` 是**必填字段**(不加 `?`),但当 Agent 已从配置里删掉、或者整个配置解析失败时,这些值**根本没有来源**。取值规则如下,三种情形逐字段给出:

| 情形 | `label` | `defaultModel` | `modelAliases` | `configured` / `reason` |
|---|---|---|---|---|
| 正常(配置里有) | 配置里的 `label` | 配置里的 `default_model` | 由 `model_aliases` 算出 | `true`;若单 Agent 有问题则 `false` + 原因 |
| **孤立实例**(配置里已删、池里还在跑) | **池记录里缓存的 `label`**(启动时抄下来的,见 §关键逻辑 1.4) | `""` | `[]` | `false` + "该 Agent 已从 config/agents.json 中移除,但实例仍在运行" |
| **配置整体解析失败** | 有池记录就用缓存的 `label`,否则用 `agentId` | `""` | `[]` | `false` + "配置当前无法解析" |

> **统一约定:`defaultModel === ""` 且 `modelAliases.length === 0` 就表示"这个 Agent 不可启动"**,与 `configured: false` 是同一件事的两种表达。
> **前端的界面契约**:
> - **不给它渲染模型下拉**(空列表渲染出来是个坏掉的控件),直接把这一行标灰;
> - **启动入口对它禁用**,鼠标悬停/展开显示 `reason`;
> - **但 `phase` / `runId` 仍然照常渲染**——它可能正在跑,**终止入口必须可用**。这正是"配置写坏/删掉也能管好已有进程"那条要求的落点(见 §关键逻辑 1.1、验收 37)。
>
> **不要让这两类记录"没有 `AgentInfo`"**:`agents` 数组里宁可给空串和空数组,**也不要省略或塞 `null`**——`tsc` 会挡住前者,而后者会在运行期变成一堆 `undefined` 判断。协议层的做法是**永远返回完整形状的对象**,由 UI 决定怎么展示。

> **为什么必须是 `phase` 而不是 `running: boolean`**:`starting`(已在池里占位、子进程还没起来)与 `running` **对用户和前端是两个不同的状态**——前者的终止按钮必须禁用(还没有进程可杀),后者可用。只给一个布尔值,前端在 F5 之后只能一律恢复成 `running`,于是终止按钮亮着而 `stopAgent` 必然返回 `stopped: false`,用户点了没反应也看不懂为什么。详见 §前端 3.1 的"刷新恢复"。
>
> **`terminating` 同样不能省**:用户在"正在终止"期间按 F5,池记录还在(Sidecar 正在等 `exit`,最多 3 秒),如果枚举里没有这一档,列表只能把它恢复成 `running` → 终止按钮又亮起来 → 用户再点一次。**这正是 v5 漏掉的那一档**,与 §关键逻辑 1.4 的幂等终止配套。

**Sidecar → Rust 通知(`notifications`,无 id)**

| method | params |
|---|---|
| `agent/output` | `{ runId, agentId, stream: "stdout" \| "stderr", line }` |
| `agent/state` | `{ runId, agentId, phase: "starting" \| "running" \| "terminating", resolvedModel: string }` |
| `agent/exit` | `{ runId, agentId, code: number \| null, killed: boolean, resolvedModel: string }` |
| `agent/error` | `{ runId, agentId, kind, message, resolvedModel: string }` |
| `sidecar/log` | `{ level: "info" \| "warn", message }` |

**`agent/state` —— 非终态的相位变化广播(不能省)**

| 何时发 | `phase` |
|---|---|
| `spawn` 成功、占位记录升级为 `running` | `"running"` |
| 收到终止请求、`killed` 置为 `true` | `"terminating"` |
| **终止超时,`killed` 回滚为 `false`** | `"running"` |

**三个字段各有各的用处,一个都不能少**:`runId`(哪一轮运行)、`phase`(什么阶段)、**`resolvedModel`(这一轮用的哪个模型)**。
> ⚠️ **`resolvedModel` 不是"顺带带上"的**。刷新期间前端可能**只**收到这条广播而拿不到快照(快照会被 `rev` 判为过期丢弃,见 §前端 3.1),如果广播里没有模型名,记录就是残缺的——**界面会显示"工作中"却报不出当前模型**。事件要能**独立支撑起一条完整记录**,不能指望"反正后面还有快照会补上"。
> 这也和"运行身份 vs 当前相位"那组划分一致:`resolvedModel` 属于**运行身份**(这一轮用哪个模型,整个 run 内不变),`phase` 属于**当前相位**。事件里两者都给,接收方就不必再向快照要。

> **为什么需要一条"往前推进的非终态通知"**:`agent/exit` / `agent/error` 只覆盖**终态**,但池记录的相位在**非终态之间**也会变,而每一次变化都可能有页面刚刷新过——**那个发起调用的 promise 已经不在了**。最典型的就是 v6 漏掉的这条:**3 秒终止超时回滚**。用户点了终止 → 按 F5 → Sidecar 超时把 `killed` 改回 `false` → 但这个 `stopFailed` 只回到那个**已经被销毁的** `invoke()`,新页面永远停在"正在终止"、终止按钮永远禁用,**而进程其实还在跑**。这与"启动失败后刷新"是**完全同一类问题**,不能只修一半。
> **为什么不复用 `agent/error` 再发一个错误**:`agent/error` 的语义是终态(`finished` / `failed`,见 §前端 3.1),而这里实例**还活着**。把非终态硬塞进终态通道,会让前端的状态机自相矛盾。**一条通用的 `agent/state` 比逐个场景补专用通知更省事,也更不容易再漏。**

> **`stopAgent` 返回 `stopped: true` 是"请求已受理",不是"进程已停止"**。第二次调用返回 `true` 时,第一次调用**仍可能随后超时回滚**——所以这个 `true` 不构成任何终态承诺。
> **推论(前端必须遵守)**:`stop()` 收到成功响应后**只把该 Agent 推进到 `terminating` 并等待事件**,绝不直接落 `finished`。状态最终由 `agent/exit`(成功终止)或 `agent/state { phase: "running" }`(回滚了,还在跑)决定。**这与阶段1 的 `kill()` 完全一致**("成功不做别的:等 `agent-exit` 推进到 finished"——`src/App.tsx:281-283`),阶段2 保持同一语义。

**Sidecar → Rust 错误响应(有 id 的失败请求)—— `kind` 必须走结构化字段,不能只给 message**

```jsonc
{
  "jsonrpc": "2.0",
  "id": 7,
  "error": {
    "code": -32000,
    "message": "第 12 行:期望 '}'",
    "data": { "kind": "configInvalid" }   // ← 关键:Rust 靠它分支,不靠解析 message
  }
}
```

> ⚠️ **这是原计划的一处硬伤**。原文只写了"handler 抛异常 → `-32000` 并带上 `message`",但前端却要按 `configInvalid` / `alreadyRunning` / `agentStartFailed` 等近十种 `kind` 分支。仅凭一条人可读的 `message`,Rust 只能靠字符串匹配去猜——文案一改就全断,而且 `alreadyRunning` 这种分支走错会直接导致前端状态机失联(见 §前端 3.1)。**错误种类必须和数据一起传,不能和文案绑在一起。**
>
> 约定:Sidecar 抛出的 `RpcError` 一律是 `{ code, message, data: { kind } }`;Rust 的 `call()` 读到 `error.data.kind` 就原样映射成 `CmdError.kind`,`message` 原样透传;`data` 缺失或 `kind` 不在白名单内 → 降级为 `internal` 并把原始报文附在 message 里,方便排错。

**`kind` 的来源划分**(前端不需要知道谁产生的,但 Rust 侧要分清):

| 来源 | kind |
|---|---|
| Rust 自己产生(边链路问题) | `nodeMissing`、`sidecarCrashed`、`sidecarTimeout`、`internal` |
| Sidecar 返回(业务问题) | `configInvalid`、`agentNotFound`、`alreadyRunning`、`agentStartFailed`、`stopFailed` |

**两条关键设计说明**

1. **`startAgent` 回传的是 `injectedEnv`,不是完整 env**。只含第 2 层(模板展开后的模型路由变量)与第 3 层(`AGENTHUB_*`),**不含继承来的 `process.env`**。理由有二:把用户机器上所有环境变量(其中可能有别的密钥)灌进 WebView 没有任何好处;注入预览若是几百行 `PATH`/`APPDATA` 就失去了"一眼比对"的意义。
   每个值都带类型标记(见 §关键逻辑 1.2 的 `InjectedValue`),**密钥类只回传 `{ set, length }`**。用户改完配置一眼就能看到 `ANTHROPIC_MODEL` 从 `haiku` 变成 `glm-4-flash`——这是阶段2 唯一能让用户亲手验收的入口,所以要可见;密钥没必要可见,`已设置(共 42 位)` 就够证明它被注入了。
   > 注意这只解决了**这一条**通道。`env-dump.js` 的 stdout 经 `agent/output` 事件同样会流到界面,那一侧也必须做同样的处理,否则等于白做——见 §关键逻辑 1.3.1。

2. **`listAgents` 必须回传运行时状态(`running` / `runId` / `resolvedModel`)与可选别名(`modelAliases`),不能只回配置里的静态信息**。
   - **运行时状态**:阶段1 用 `get_agent_status` 解决了"F5 刷新后前端不知道后端还在跑什么"的问题(验收报告 §6 的六.1/六.3 专门测了这条);阶段2 若不带上运行时状态,刷新后 `AgentStatusList` 会全部显示"空闲"、终止入口会全部消失,而进程其实还在跑——这是**对阶段1 已有能力的回归**。前端 `useSidecar` 的刷新恢复就靠它。
   - **`modelAliases`**:底部输入框上方的模型选择器需要一份"这个 Agent 能选哪几档"的清单。**这份清单只能由配置算出来**:`modelAliases = model_aliases 里所有含有该 agentId 条目的别名的键`,按 `fast` → `balanced` → `powerful` 的固定顺序排列(配置里没出现过的新别名按字典序排在后面)。
     前端**绝不能写死 `["fast","balanced","powerful"]`**——用户改了 `model_aliases`(增删别名、或删掉某个 Agent 的某一档)之后,写死的列表会让界面显示一个后端根本不认的选项,点下去才报错。反过来,前端也不需要"猜"别名,它照 `modelAliases` 渲染即可,配置一改 `list_agents` 一刷新就同步。
     选中项默认为 `defaultModel`;若 `start_agent` 的 `model` 传了 `null`/省略,Sidecar 回落到 `default_model`(见 §关键逻辑 1.4)。

### B. Rust ↔ 前端

**命令**(错误一律返回结构化 `CmdError`,不使用裸字符串;前端按 `kind` 分支,文案可随时改)

```rust
#[derive(Serialize)] #[serde(rename_all = "camelCase")]
struct CmdError { kind: String, message: String }
```

| 命令 | 签名 |
|---|---|
| `start_agent` | `(agent_id: String, prompt: String, model: Option<String>, run_id: u64) -> Result<StartAck, CmdError>` |
| `stop_agent` | `(agent_id: String, run_id: u64) -> Result<(), CmdError>` |
| `list_agents` | `() -> Result<ListAgentsResult, CmdError>` |

`StartAck` = `startAgent` 的 result 原样透传(仅字段名转 camelCase):`{ agentId, runId, resolvedModel, injectedEnv, cwd }`。

`ListAgentsResult` = Sidecar 的 `listAgents` 结果 **+ Rust 补的 sidecar 状态**:

```ts
type ListAgentsResult = {
  agents: AgentInfo[]                 // 池记录 ∪ 能解析出来的配置项(配置坏了也一定有池记录)
  config: {                           // ← 配置问题作为「数据」返回,不是「错误」
    ok: boolean
    error?: { kind: string; message: string }   // ok=false 时带行号与字段路径
  }
  sidecar: {
    available: boolean     // Rust 判断的 Sidecar 存活性
    jobObject: boolean     // Job Object 是否成功接管(见 §关键逻辑 2.1)
    version: string
  }
}
```

> **字段的来源必须分清,否则执行时会不知道谁填谁**:
> | 字段 | 谁产生 |
> |---|---|
> | `agents` | **Sidecar**(它才认识配置和池) |
> | `config` | **Sidecar**(它才知道解析/校验结果);Rust **原样透传** |
> | `sidecar.{available, jobObject}` | **Rust**(Job Object 是 Rust 建的,Sidecar 根本不知道) |
> | `sidecar.version` | Sidecar 的 `ping.version`,Rust 顺带带上 |
>
> **Rust 不"发明" `config` 字段**,只是把 Sidecar 的 RPC 结果搬过来、再补上 `sidecar`。这里最容易出的错是:Sidecar 的 RPC 返回值里漏了 `config`,而 Rust 的结构体又要求它——那 Rust 只能填个 `ok: true` 糊过去,**配置错误就再也传不到界面了**。**改协议时两边一起改。**

> **`list_agents` 永远返回 `Ok`**(除非 Sidecar 本身不可用)。配置有致命错误时,它照样把**池里的运行记录**返回,只是 `config.ok = false`。
> **为什么**:否则用户在 Agent 运行期间改坏 JSON 再按 F5,就会**既看不到那个进程、也无法终止它**——与"重载不清掉运行中的记录"直接矛盾。详见 §关键逻辑 1.1 的两级校验。
> 也就是说:`configInvalid` 这个 `kind` **只由 `start_agent` 产生**,不在 `list_agents` 的错误通道里出现。前端处理这两条命令的方式因此不同,不要写成同一个分支。

> **为什么把 sidecar 状态挂在 `list_agents` 上,而不是只发事件**:前端在挂载时**一定会**调 `list_agents`(刷新恢复需要它),但不一定会收到 `sidecar-status` 事件——如果 Sidecar 已经活着,`list_agents` 不会重新 spawn 它,也就不会产生新事件。F5 之后前端状态被清空,单靠事件就会永远丢掉"清理降级模式"这个提示,验收 27/32 的前置条件也无从确认。**挂在一个必然会调的查询上,才是可靠的。**

`kind` 取值与前端行为:

| kind | 触发场景 | 前端行为 |
|---|---|---|
| `nodeMissing` | spawn node 失败(未安装/不在 PATH) | 红字"未检测到 Node.js,请先安装后重启应用",禁用启动 |
| `sidecarCrashed` | Sidecar 进程退出、管道断开 | 标记不可用,显示「重新连接」;下次启动自动重启 |
| `sidecarTimeout` | 请求超时(Sidecar 卡死) | 同上,附"已超时" |
| `agentNotFound` | `agentId` 不在配置里 | 红字"请检查 config/agents.json" |
| `alreadyRunning` | 该 Agent 已有实例在跑 | 转入状态未知并重新同步 |
| `configInvalid` | 配置语法错/缺字段。**只由 `start_agent` 产生**——`list_agents` 永远成功返回,配置问题在 `config.error` 里(见 §关键逻辑 1.1) | 显示 Sidecar 返回的**原文错误**(含行号) |
| `agentStartFailed` | 子进程 spawn 失败(命令不存在等) | 红字显示原始错误 |
| `stopFailed` | 终止失败 | 红字,保持运行态 |
| `internal` | 兜底 | 红字 |

**事件**(Rust 转发 Sidecar 通知,payload 统一 `#[serde(rename_all = "camelCase")]`)

- `agent-output`:`{ runId, agentId, stream, line }`
- `agent-state`:`{ runId, agentId, phase, resolvedModel }`   // 非终态相位变化,见 §协议契约 A
- `agent-exit`:`{ runId, agentId, code, killed, resolvedModel }`
- `agent-error`:`{ runId, agentId, kind, message, resolvedModel }`
- `sidecar-status`:`{ available: boolean, message?: string, jobObject: boolean }`
  - `jobObject`:子进程清理是否处于 Job Object 保护之下(见 §关键逻辑 2.1 的"分配失败怎么办")。**这是给验收用的**:`false` 时验收 27/32 须记为"未验证—环境限制",而不是判通过。

**runId 规则**:由前端用 `Date.now()` 生成,先写 ref 再 invoke,Rust 不分配。

> **为什么 runId 必须由前端生成**:阶段1 已踩过"启动期丢首行输出"的坑——Rust 分配 id 的话,第一条输出事件可能早于 invoke 的返回值到达前端,没有匹配基准。前端预生成 + 先写 ref 才能兜住。

### C. 阶段1 事件的改名(必做,否则诊断控制台会串扰)

阶段1 的事件名是 `agent-output` / `agent-exit`,payload 为 `{ runId, stream, line }` / `{ runId, code, killed }`(见 `src-tauri/src/lib.rs:202`、`lib.rs:270` 与 `src/App.tsx:191-206`)。阶段2 想在同名事件上挂 `agentId` —— **这个方案不能用**,因为阶段1 的诊断控制台有这样一段逻辑(`src/App.tsx:195-206`):

```tsx
listen<ExitEvent>("agent-exit", (event) => {
  if (event.payload.runId !== runIdRef.current) {
    // 刷新恢复或前后端脱节时 runId 尚不可知，仍接受退出事件避免永久卡住。
    if (phaseRef.current === "unknown" || phaseRef.current === "subscribing") {
      runIdRef.current = event.payload.runId;
      applyExit(event.payload);      // ← 接受任意 runId 的退出事件
    }
    return;
  }
  ...
```

也就是说:**只要诊断控制台处于 `subscribing` 或 `unknown` 相位,任何一个 runId 的退出事件都会被它当真**,并把界面推进到 `finished`。阶段2 一引入多 Agent 并行,这个"接受任意 runId"的兜底就变成了串扰源——Diagnostic 页会被别的 Agent 的退出事件莫名冻结。

**改法(共 4 处字符串,不动任何逻辑)**:

| 位置 | 改动 |
|---|---|
| `src-tauri/src/lib.rs:202` | `app.emit("agent-output", ...)` → `app.emit("diagnostic-output", ...)` |
| `src-tauri/src/lib.rs:270` | `app.emit("agent-exit", ...)` → `app.emit("diagnostic-exit", ...)` |
| `src/App.tsx:191` | `listen<OutputLine>("agent-output", ...)` → `"diagnostic-output"` |
| `src/App.tsx:195` | `listen<ExitEvent>("agent-exit", ...)` → `"diagnostic-exit"` |

改完之后:阶段1 的 `agent-output` / `agent-exit`(带 `agentId`)与诊断用的 `diagnostic-output` / `diagnostic-exit`(不带 `agentId`)是两套完全独立的事件,互不干扰。比"前端按 `agentId` 是否存在来分流"可靠得多——后者只要有一处漏判就回到同样的串扰。

> 这是本阶段**唯一**改到阶段1 代码语义的地方(外加 `mod sidecar;`、状态注册、三个新命令、`RunEvent::Exit` 追加清理)。阶段1 的进程管理逻辑本身一行不动。

---

## 关键逻辑

### 1. Sidecar

#### 1.1 配置加载 `config.ts`

```jsonc
{
  // 路径规则(只有 cwd 会被解析成绝对路径,command / args 原样传给 spawn)
  "agents": {
    "hermes": { "label": "Hermes(大管家)", "command": "node",
                "args": ["scripts/env-dump.js"],
                "default_model": "balanced", "cwd": "..",
                "start_delay_ms": 0, "stop_delay_ms": 0 },
    // start_delay_ms / stop_delay_ms:仅用于测试/演示,把"正在启动"/"正在终止"状态延长 N 毫秒,
    // 便于观察与复现刷新竞态。日常使用请保持 0 —— 它们会让启动与终止都变慢。详见 §关键逻辑 1.4.1。
    "pi":     { "label": "Pi(数据分析)",  "command": "node", "args": ["scripts/env-dump.js"], "default_model": "fast",     "cwd": "..", "start_delay_ms": 0, "stop_delay_ms": 0 },
    "claude": { "label": "Claude Code",   "command": "node", "args": ["scripts/env-dump.js"], "default_model": "powerful", "cwd": "..", "start_delay_ms": 0, "stop_delay_ms": 0 },
    "codex":  { "label": "Codex",         "command": "node", "args": ["scripts/env-dump.js"], "default_model": "balanced", "cwd": "..", "start_delay_ms": 0, "stop_delay_ms": 0 }
  },
  "model_aliases": {
    "fast":     { "claude": "glm-4-flash", "codex": "deepseek-v4-flash", "pi": "haiku",  "hermes": "haiku" },
    "balanced": { "claude": "glm-5.3",     "codex": "qwen3-coder",       "pi": "sonnet", "hermes": "sonnet" },
    "powerful": { "claude": "glm-5.3",     "codex": "doubao-seed-code",  "pi": "opus",   "hermes": "opus" }
  },
  "env_injection": {
    "hermes": { "ANTHROPIC_BASE_URL": "{base_url}", "ANTHROPIC_AUTH_TOKEN": "{api_key}", "ANTHROPIC_MODEL": "{model}" },
    "pi":     { "ANTHROPIC_BASE_URL": "{base_url}", "ANTHROPIC_AUTH_TOKEN": "{api_key}", "ANTHROPIC_MODEL": "{model}" },
    "claude": { "ANTHROPIC_BASE_URL": "{base_url}", "ANTHROPIC_AUTH_TOKEN": "{api_key}", "ANTHROPIC_MODEL": "{model}" },
    "codex":  { "OPENAI_BASE_URL": "{base_url}",    "OPENAI_API_KEY": "{api_key}",       "OPENAI_MODEL": "{model}" }
  },
  "cc_switch": { "base_url": "http://127.0.0.1:8787", "api_key_env": "CC_SWITCH_API_KEY" }
}
```

- **路径规则(只有一条,必须写死)**:

  | 字段 | 解析方式 |
  |---|---|
  | `cwd` | 相对路径以**配置文件所在目录**(`config/`)为基准,由 Sidecar 解析成**绝对路径**后传给 `spawn` 的 `cwd` 选项 |
  | `command` | **原样传给 `spawn`**,Sidecar 不做任何解析。`"node"` 靠 PATH 找到;若写相对路径,由操作系统按 `cwd` 解析 |
  | `args` | **原样传给 `spawn`**,Sidecar **不做任何替换、不改成绝对路径**。其中的相对路径由**子进程**按 `cwd` 解析 |

  > ⚠️ **原方案的矛盾(必改)**:配置里写着 `cwd: ".."`(解析后 = 项目根)却同时写着 `args: ["../scripts/env-dump.js"]`。Node 是以**子进程的 `cwd`** 为起点解释参数里的相对路径的,所以实际会去找 `项目根的上一级/scripts/env-dump.js` —— 也就是 `D:\Project\scripts\env-dump.js`,**根本不存在**,启动直接失败。
  >
  > 修法就是让两个字段说同一种话:`cwd: ".."`(项目根)+ `args: ["scripts/env-dump.js"]`(相对项目根)。**不要试图用"Sidecar 把 args 里的路径偷偷改成绝对路径"来调和**——那样 Sidecar 就得去猜哪个参数是路径、哪个是普通参数(比如 `--once`),猜错就是静默的行为改变。**规则保持"原样透传"最可预测**,用户看配置文件就能算出进程跑在哪、命令是什么。
  >
  > 这条也是 §验收 1 那个 `node -e` 自测能不能跑通的前提。

- **阶段2 全部 `command` 指向 `node scripts/env-dump.js`**。用户不懂编程,先跑得通、看得见。阶段3 把 `claude`/`codex` 换成真实 CLI 命令即可,配置结构不变。
- **支持 `//` 注释 —— 用 `json5` 解析,不自己写扫描器**。
  用户在配置文件里写注释是刚需(他不懂编程,注释就是他理解每个字段的唯一途径),但手写去注释逻辑要自己保证字符串边界、转义、换行、行号映射全都正确,**而这段代码的失败模式极隐蔽**:本配置里就有 `"base_url": "http://127.0.0.1:8787"`,朴素的 `//` 剥离会把 URL 变成 `http:`,报错却指向"别名解析失败",没人能倒查到根因。
  `json5` 是成熟的、只做这一件事的库,`JSON5.parse` 的异常对象带 `lineNumber` / `columnNumber`,`configInvalid` 要的"行号 + 字段路径"直接就有。
  **与"不引依赖"的原则不冲突**:`json5` 是 devDependency,由 esbuild 在构建时**打进 `sidecar.cjs` 单文件**,运行时不需要任何 `node_modules`——这正是决定 3 "单文件 .cjs" 的用意。
- **`config/agents.json` 的扩展名保持 `.json`**(不放宽成 `.jsonc`):用户的编辑器靠它给语法高亮和括号匹配,而内容由 json5 解析,两边都满意。
- 校验必须**分成两级**,不能一律 `configInvalid`:

  | 级别 | 触发条件 | 结果 |
  |---|---|---|
  | **致命**(配置整体不可用) | JSON5 语法错;顶层不是对象;`agents` 缺失或为空;`model_aliases` 缺失(或不是对象);`cc_switch.base_url` 缺失;`env_injection` 缺失 | **`startAgent` → `configInvalid`(带行号与字段路径)**;**`listAgents` 不失败,而是返回"部分成功"**——见下方 |
  | **单个 Agent 的问题**(不影响其它 Agent) | 该 Agent 缺 `command`;`default_model` 不在**该 Agent 自己的** `modelAliases` 里(注意不是"在 `model_aliases` 里存在"就算数——别名存在但没给这个 Agent 配模型,等于没配);该 Agent 的 `modelAliases` 为空(一个别名都没配);缺 `env_injection[id]`;`env_injection[id]` 引用了无法解析的占位符;`cwd` 不存在 | **只有这个 Agent 不可用**:`AgentInfo.configured = false` 并附 `reason`,其余 Agent 照常可用 |

  > ⚠️ **致命错误不能让 `listAgents` 整体失败(必改)**。原方案写的是"整个 `listAgents` / `startAgent` 返回 `configInvalid`"。对 `startAgent` 这是对的(配置坏了确实没法启动);**对 `listAgents` 会造成一个很糟的后果**:
  > 用户正在跑一个 Agent → 手贱把 JSON 改坏了 → 按 F5 → `list_agents` 失败 → 前端把通道压到 `unknown`、**禁用所有按钮** → 用户**既看不到那个还在跑的进程,也没有任何办法终止它**,只能先修好配置、或者去任务管理器。
  > 这和本计划另一条铁律——"重载**不清掉**正在运行的记录"(见下方 mtime 缓存)——**直接矛盾**:进程还在跑,界面却假装它不存在。
  >
  > **`listAgents` 永远返回 `Ok`,配置问题作为数据放在返回值里**:
  >
  > ```ts
  > type ListAgentsResult = {
  >   agents: AgentInfo[]                       // 池记录(一定能拿到)∪ 能解析出来的配置项
  >   config: { ok: boolean; error?: { kind: string; message: string } }   // ok=false 时带行号
  >   sidecar: { available: boolean; jobObject: boolean; version: string }
  > }
  > ```
  >
  > 配置整体解析失败时,`agents` **仍然包含池里那些记录**(`label` 用启动时缓存的,`configured` 一律 `false`,`reason` 说明"配置当前无法解析"),所以用户看得见、也能终止。`config.error` 单独驱动一条红字提示。
  >
  > **`startAgent` 保持不变**:配置不可用时它就该失败(`configInvalid`),这是对的——但**它失败的后果只是"不能启动新实例",不影响已有实例的可操作性**。这两件事必须分开。

  > **别名校验的粒度**:`model_aliases` 是"别名 → 各 Agent 的模型名"的表,所以某个别名对某个 Agent 缺失是**正常且允许**的(例如 `fast` 只给 Pi 和 Codex 配了)。这不算错误,只是这个别名不会出现在那个 Agent 的 `modelAliases` 里。**只有"某 Agent 一个别名都没有"或"`default_model` 落到它自己没有的别名上"才是这个 Agent 的错误。**

  > ⚠️ **原计划把两类混为一谈了,后果是 `AgentInfo.configured` 这个字段永远不可能为 `false`**:校验规定"每个 agent 都必须有 `command`,否则 `configInvalid`"→ 配置一旦能加载成功,所有 Agent 必然 `configured: true`;而配置失败时前端连 roster 都拿不到,压根没有"某个 Agent 置灰"的界面状态。**那个字段和它对应的 UI 是死代码。**
  >
  > 拆成两级之后 `configured` 才真正有用,**而且对不懂编程的用户更友好**:四个 Agent 里有一个写错了,不该让另外三个一起罢工。界面上的表现是——坏的那个在下拉里置灰并显示原因(悬停或点击展开),好的照常启动。
  >
  > 对应地:`startAgent` 遇到 `configured === false` 的 Agent 返回 `agentNotFound` 或新增一个 `agentUnavailable`(取 `agentNotFound` 即可,message 里带上 `reason`,不必新增 kind)。
- **mtime 缓存:`ensureConfigFresh()` 必须在下列每个入口都先调用**——`startAgent`、**`listAgents`**、`configReload`。实现就是每次 `statSync(configPath).mtimeMs` 比对,变了就重载 + 重新校验。
  > ⚠️ **原方案只写了"每次 `startAgent` 前检查",这会形成一个死锁式的死角**:某个 Agent 因为配置写错被置灰(`configured: false`)→ 用户**根本没法启动它** → 也就永远触发不了重载 → 即使他把配置改回正确,**那一项依然是灰的**,只能重启应用。而验收 9、10 明确要求"改回后**无需重启**恢复"。
  > **`listAgents` 是"配置是否有变"的主要观测点**,它本来就该反映最新配置。把它漏掉,"配置可被用户掌握"这个阶段2 的核心目标就断了一半。
  > 重载后**不要顺手清掉正在跑的记录**——配置变了不代表运行中的进程要停;已有实例继续跑完,新配置只影响下一次启动。**这一点要在代码注释里写明**,否则很容易被写成"重载 = 重置"。`ensureConfigFresh()` 的返回也应当带上"是否发生了重载",便于上层决定要不要补发一次 roster。
- **打包后配置的位置是阶段6 的遗留问题**:`bundle.resources` 装的 `config/agents.json` 落在只读的安装目录里,用户改不了,这与"配置可被用户掌握"的初衷冲突。阶段2 只在开发模式验证(读项目根的 `config/agents.json`),阶段6 需改为"首次启动把默认配置复制到用户配置目录(`appConfigDir`),之后固定读那一份"。**本阶段不实现,但要在 README 和代码注释里写清这条路径的归属**,否则阶段6 会踩。**

#### 1.2 环境变量注入 `model.ts`

```
三层环境,后者覆盖前者:
1) 继承宿主 process.env
   —— Agent 需要 PATH / HOME / APPDATA 等基础变量
2) 展开 env_injection[agentId] 模板:
     {model}    -> model_aliases[alias][agentId]   (别名解析后的真实模型名)
     {base_url} -> cc_switch.base_url
     {api_key}  -> process.env[cc_switch.api_key_env] ?? ""
3) AGENTHUB_* 上下文:
     AGENTHUB_AGENT_ID / AGENTHUB_MODEL_ALIAS / AGENTHUB_MODEL /
     AGENTHUB_PROMPT / AGENTHUB_RUN_ID
```

传入 `spawn` 的是**完整三层合并结果**;回传给前端的 `injectedEnv` **只有第 2 层和第 3 层**(见 §协议契约 A 的设计说明 1)。

- **为什么分三层而不是直接覆盖**:未来的 claude / codex CLI 依赖继承来的 `PATH`、`HOME`、`APPDATA`,清空环境会导致命令找不到。
- **为什么回传时砍掉第 1 层**:第 1 层是用户整台机器的环境变量,既含无关噪声,也可能含与本项目无关的密钥。WebView 里没有任何理由拿到它们。
- **API Key 取不到时不阻断启动**,但回传的 `injectedEnv` 里该变量为空字符串,同时发一条 `sidecar/log {level:"warn"}`,前端在注入预览里用 `--status-warning` 标出"未设置 CC_SWITCH_API_KEY"。**理由**:阶段2 的验证目标是"配置改变 → 环境变量改变",不是"CC Switch 通不通"。让用户卡在环境变量缺失上会掩盖真正的验证点。
- **密钥原文不进入任何回传通道**。原计划写的是"回传掩码值,前端点『显示完整值』再请求明文"——这个设计**自相矛盾且无法实现**:`startAgent` 返回的已经是掩码,协议里又没有单独的明文获取方法,开关就是个死按钮。
  改成:**`injectedEnv` 的每个值是一个带类型标记的对象**,密钥类的值只报"有没有、多长":

  ```ts
  type InjectedValue =
    | { kind: "plain"; value: string }                       // 非密钥,照常显示
    | { kind: "secret"; set: boolean; length: number }       // 密钥:只报"有没有、多长"
  ```

  预览里密钥显示为 `ANTHROPIC_AUTH_TOKEN   已设置(共 42 位)` 或 `未设置`。
  **这已经足够支撑用户的验证目标**——他要确认的是"我改了配置,这个变量确实被注入了",而不是"我要读到密钥内容"。**不新增任何"取明文"协议方法,界面上也不放"显示完整值"开关。**

- **⚠️ 光做到上面这条还不够 —— 子进程的 stdout 也是一条回传通道**。
  上一版计划在这里写了"密钥原文永不出 Sidecar",**这句话是错的**:`env-dump.js` 是由 Sidecar 启动并读取输出的**子进程**,它 `console.log` 出来的任何东西都会经 `agent/output` 事件流回 Rust、再流进前端输出区。如果它照原样打印 `ANTHROPIC_AUTH_TOKEN`,密钥就**原封不动地出现在界面上、出现在任何截图里、出现在验收记录里**——注入详情做得再干净也没用。
  **因此 `env-dump.js` 对密钥类变量也只打印 `已设置(共 N 位)`**(判定规则与 Sidecar 一致),不提供"显示明文"的参数。用户若要核对密钥本身,那是他自己机器上的环境变量,在系统里查即可。
  **准确的说法(注意边界,别写成普遍保证)**:
  - **Sidecar 自己产生的内容**——即 `startAgent` 的响应里那个 `injectedEnv`——**不含密钥原文**。这是 Sidecar 主动构造的,它能保证。
  - **`agent/output` 通道是"原样转发",Sidecar 不扫描、不脱敏**。它转发的是 Agent 子进程自己 `console.log` 出来的东西,Sidecar 无从判断哪一段是密钥。
  - 所以阶段2 成立的结论是:**"阶段2 的 `env-dump.js` 不输出密钥原文"**,而不是"`agent/output` 这条通道里一定没有密钥"。前者是本阶段能兑现的承诺,后者不是。

  > **为什么不去做输出脱敏**:在转发层按已知的密钥值做字符串替换,看着能兜底,实际上既挡不住(Base64、分段打印、截断都会绕过)又一定会误伤(用户正常的输出里出现相同子串就被改掉)。**用一层不可靠的过滤换来"以为安全了"的错觉,比不做更糟。**
  >
  > **阶段3 的遗留项**:阶段3 接入真实的 Claude Code / Codex CLI 之后,这些 CLI 自己会不会把密钥打进日志或错误信息里,是**由它们决定的**,需要在那时重新评估。**这一条要在本阶段的代码注释与 README 里写明是"阶段2 的边界,阶段3 需重新评估"**,否则会被当成已经解决的问题一直带下去。
- **注入模板写在配置里而不是硬编码**:不同 CLI 认的变量名不同(Claude Code 认 `ANTHROPIC_*`,Codex 认 `OPENAI_*`),写进配置用户能自己改;阶段4 的适配器再在此基础上做格式转换。

#### 1.3 Agent 进程 `agent-process.ts`

- `spawn(command, args, { cwd, env, stdio: ["ignore","pipe","pipe"], windowsHide: true })`
- `cwd` 解析:配置里的 `cwd`(相对配置文件目录)转绝对;不存在则报 `agentStartFailed` 并给出具体路径。**阶段5 的 Worktree 就落在这个字段上**,现在先把它做成可配置的。
- **stdout/stderr 不能用 `readline` 直接读**(这是原计划里的一处技术错误,必须改):
  `readline.createInterface({ input })` 会按流的编码解码,默认 UTF-8。中文 Windows 下 `ping`、部分国产 CLI、以及任何 GBK 输出的进程,吐出的字节在**这一步就已经被替换成 `U+FFFD`**,之后再怎么处理都是坏数据。更要命的是:Rust 侧"先 `from_utf8`,失败再 `encoding_rs::GBK`"的兜底**在这个链路上完全失效**——Node 交给 Rust 的已经是合法 UTF-8(只是内容是 `????`),`from_utf8` 根本不会失败。原风险表里"中文 Agent 输出乱码 → Rust 侧 GBK 回退"这条对策**对 Sidecar 转发的输出不成立**。
  **正确做法**:让子进程 stdout/stderr 保持 `Buffer`(不调用 `setEncoding`),自己维护行缓冲(按 `0x0A` 切分,保留跨 chunk 的半行),每凑齐一行调用同一个解码函数:

  ```ts
  // 与阶段1 Rust 侧同源的策略:先按 UTF-8 试解,出现替换字符才回退 GBK
  const gbk = new TextDecoder("gbk");
  function decodeLine(buf: Buffer): string {
    const utf8 = buf.toString("utf8");
    if (!utf8.includes("�")) return utf8;
    return gbk.decode(buf);
  }
  ```
  Node 24 官方构建带 full-icu,`new TextDecoder("gbk")` 可用(已在本机 `v24.19.0` 实测:`[0xd6,0xd0,0xce,0xc4]` → `中文`)。若某天换成 small-icu 构建会抛异常,**把 `new TextDecoder("gbk")` 包在 try 里**,失败则退回 `utf8` 的 lossy 结果,不要让整个 Sidecar 崩掉。
  行尾统一 `\r\n?` 去掉(Windows 的 `ping` 输出带 `\r`)。
  Rust 侧的解码逻辑**保持不动**,它对阶段1 自己的 `ping` 预设仍然有效(那条链路是 Rust 直接读管道,不经过 Node)。
- **prompt 不走命令行参数,走 `AGENTHUB_PROMPT` 环境变量**。理由:命令行参数在 Windows 下要处理引号转义,任务描述可能很长;环境变量最稳,也让 `env-dump.js` 天然能打印出任务内容。
- `child.on("exit", (code, signal) => ...)` → 发 `agent/exit`。
- **stdin 是 `ignore`**,所以 Agent 拿不到 EOF 信号,只能被 kill。这决定了 `env-dump.js` 必须自己控制生命周期(见下一条)。

#### 1.3.1 验证脚本 `scripts/env-dump.js` —— 必须能常驻

原计划只写了"打印注入的模型路由变量 + 全部 `AGENTHUB_*` 变量",如果它打印完就退出,那么 §验收 B 组的 7–11、15 项(禁用态、双击、终止、刷新恢复、并发、关窗无残留)**全部无法执行**——它们都要求 Agent 处于"运行中"。

阶段1 已经踩过同一个坑(计划原文:"此前该场景无法手工复现,故增设该预设"),因此这里的预设要有两个模式:

| 参数 | 行为 | 用途 |
|---|---|---|
| 默认 | 打印环境变量清单 → 每秒输出一行心跳 `[env-dump] 仍在运行 t=Ns`,直到被终止 | 验收 B 组全部"运行中"场景、终止、并发、关窗清理 |
| `--once` | 打印环境变量清单后立即正常退出(exit 0) | 复现秒退竞态、验证 `finished` 不被覆盖 |

- 心跳让用户能直观看到"进程真的活着",也让终止操作有可观察的反馈。
- 打印内容分两组(与前端注入详情的分组**完全一致**,方便肉眼对照):
  1. **模型路由变量**:`ANTHROPIC_BASE_URL` / `ANTHROPIC_AUTH_TOKEN` / `ANTHROPIC_MODEL` / `OPENAI_*` —— 只打印实际存在的那几个,键序固定
  2. **`AGENTHUB_*`**:全量打印,包括 `AGENTHUB_PROMPT`(验证 prompt 确实通过环境变量传进来了)
- **密钥类变量只打印 `已设置(共 N 位)`,不打印明文,也不提供任何"显示明文"的参数**。判定规则与 Sidecar 侧同一套(键名匹配 `/_TOKEN$|_API_KEY$|_KEY$/i`),两边必须一致,否则验收 2 的"两处逐项对照"就无从谈起。
  > **为什么这条是硬要求**:`env-dump.js` 的 stdout 会经由 Sidecar 的 `agent/output` 事件一路流到前端输出区——**它本身就是一条回传通道**。在这里打印明文,等于绕过了注入详情的所有保护。(v3 修订里"让它打印明文以便用户自己看"的说法是错的,已作废。)
- **`env-dump.js` 里可以用 `console.log`**,它不经过 Sidecar 自己的 stdout(那是 JSON-RPC 专用通道,污染它会直接破坏协议)。这一点和上面的密钥要求是两件事。

#### 1.4 Agent 池 `pool.ts`

- `Map<agentId, PoolEntry>`,**一个 agentId 最多一个实例**:

  ```ts
  type PoolEntry =
    | { status: "starting"; runId: number; resolvedModel: string; cwd: string;
        label: string }                                    // 缓存 label:配置里删掉它之后还要靠这个显示
    | { status: "running";  runId: number; resolvedModel: string; cwd: string;
        label: string; child: ChildProcess;
        killed: boolean;        // true = 正在终止(相位 terminating)
        termSeq: number;        // 每次收到终止请求 +1 —— 延时回调靠它判断"自己那一次"还算不算数
        startedAt: number }
  ```

  > `label` 在启动时从当时的配置里抄一份存下来。**理由**:Agent 可能在中途被用户从配置里删掉,而 `listAgents` 仍要把它作为"孤立实例"展示(见下方 `listAgents()`),那时已经没地方去取 label 了。
  > `termSeq` 是为 `stop_delay_ms` 准备的(见 §1.4.1):延时回调醒来时,世界可能已经变了——终止被回滚了、或者用户又重新发起了一次终止。**没有这个序号,晚到的 kill 会把一个已经回到"工作中"的进程突然杀掉**,界面和真实状态再次分叉。

- `startAgent` 顺序(**第 3 步和第 4 步之间绝不能有 await**):
  1. `ensureConfigFresh()`(mtime 检查)
  2. `agentId` 不存在或 `configured === false` → `agentNotFound`(message 带上 `reason`)
  3. **同步检查并同步占位**:池里已有该 agentId 的记录 → `alreadyRunning`;否则**立刻**同步插入一条 `{ status: "starting", runId, resolvedModel, cwd }`,**然后**才去做任何异步的事
  4. 解析别名 → 合成 env → `spawn`
  5. **spawn 是异步的**,`error` 事件可能在 RPC 响应之后才到。因此:先等 `spawn` 事件(拿到 pid)再回 RPC 响应;若先收到 `error`(命令不存在)→ **删掉占位记录** → **补发一条 `agent/error { runId, agentId, kind: "agentStartFailed", message, resolvedModel }`**(占位记录里有 `resolvedModel`——发布前先把它抄进局部变量,删记录之后仍要拿得到) → 回 `agentStartFailed`
  6. spawn 成功 → 把占用中的记录**原地升级**为 `{ status: "running", child, killed: false, ... }` → **发 `agent/state { runId, agentId, phase: "running", resolvedModel }`** → 返回 `{ agentId, runId, resolvedModel, injectedEnv, cwd }`
     > 这条广播看起来多余(发起方能从 RPC 响应里知道),但**刷新过的页面不知道**——用户在 `starting` 期间按了 F5,新页面从 `list_agents` 恢复成"正在启动",之后没有任何事件告诉它启动成功了。**凡是"页面可能在等待"的相位变化,都要广播。**

  > ⚠️ **原方案的并发漏洞(必改)**:原计划写的是"已在跑 → alreadyRunning"然后"spawn 成功时插入池记录"。这两步之间隔着一个 `await spawn`,而 Node 的 `await` 会**让出事件循环**——两个 `startAgent` 请求若几乎同时到达,第二个会在第一个还没插入记录时就看到"池是空的",于是**同一个 Agent 被启动两个进程**。
  >
  > **前端的按钮禁用救不了这个**:React 的状态更新是异步的,用户连点两下时第二次点击读到的还是旧的 `phase`;阶段1 为此专门在 `start()` 函数内部加过一次门禁(阶段1 计划 §关键逻辑 1),但那只能挡住**同一个渲染进程**。Sidecar 是独立进程,必须自己保证。
  >
  > **规则一句话**:**"检查"和"占坑"必须是同一个同步块里的相邻两行,中间不能有任何 `await`。** 占位记录带着 `status: "starting"`,后续请求看到它就直接 `alreadyRunning`,与进程是否已经真的起来无关。

- `stopAgent(agentId, runId)`:
  - **`runId` 是必填的,不是可选**。它是"防止陈旧界面误杀新进程"的唯一凭据,留一个不传的口子等于把这个保护作废——前端在不知道该传什么的时候本就不该发终止请求。
  - 池里没有该 agentId → `{ stopped: false }`(幂等,不报错)
  - 记录是 `status: "starting"` → `{ stopped: false }`。**理由**:此刻还没有 `child` 可杀。前端的每 Agent phase 表在 `starting` 期间禁用终止按钮,挡住了**同一份页面**;但 **F5 之后前端会从 `list_agents` 恢复状态**,所以这不是"正常不可达"。两件事必须一起做:① `AgentInfo` 回传 `phase`,让刷新的前端能恢复成 `starting` 并禁用终止;② 万一真收到针对 `starting` 记录的终止请求,就退回"没在跑",由前端重新同步,而不是报错或悬着。
  - `runId` 不匹配 → 说明是过期请求,`{ stopped: false }`,**不动当前实例**(防止陈旧界面误杀新进程)
  - **`killed === true`(已经在终止中)→ 直接 `{ stopped: true }`,不再碰 `child`**。这一条是**幂等终止的核心**:重复调用 `child.kill()` 时,进程可能已经死了,Windows 上会抛 `ESRCH` 之类的错误,于是第二次终止返回 `stopFailed`、界面翻红——**而用户的操作其实完全正确**。阶段1 的 `kill_test_agent` 就有这个分支("`killed` 已为 true → `Ok(())`,不重复 store"),阶段2 必须保留同等语义。幂等的判据是**用户的意图已经达成**,不是"这次调用有没有真的发出信号"。
  - 在跑且匹配且尚未终止:**`termSeq += 1`** → `killed = true` → **发 `agent/state { runId, agentId, phase: "terminating", resolvedModel }`** → (若配了 `stop_delay_ms` 则先等,见 §1.4.1) → `child.kill()` → 等 `exit` 事件,**超时 3 秒从收到请求起算**

  **终止失败的收尾规则 —— 只有一条,不要按失败原因分叉**

  只要**没能确认进程已结束**,无论原因是哪一种,都走**同一套收尾**:

  ```
  killed = false
  发 agent/state { runId, agentId, phase: "running", resolvedModel }
  返回 stopFailed
  不移除池记录
  ```

  触发这套收尾的情形(必须全部覆盖,不能只写超时):

  | 情形 | 判断方式 |
  |---|---|
  | **3 秒超时** | `exit` 事件在超时前没来 |
  | **`child.kill()` 立即抛错,而进程仍活着** | 先 `try_wait()`:`Ok(None)` / `Err` → 仍存活 |
  | 其它任何"无法确认已结束"的路径 | 一律归入上面这套收尾 |

  > ⚠️ **v8 只给"3 秒超时"规定了回滚和广播,漏了 `kill()` 立即失败这一支**。但两者的**后果完全一样**:记录停在 `killed: true` → `listAgents` 永远回 `terminating` → 终止入口永久禁用 → **而进程还活着**;如果用户此时刷新了页面,新页面就再也等不到任何事件,永久停在"正在终止"。
  > **规则要写成"只要没能确认结束就回滚 + 广播",而不是枚举失败原因**——按原因分叉正是上一轮漏掉一支的原因(**权限不足、系统拒绝终止**这些都会走 `kill()` 抛错那一支,而且恰恰是最需要让用户看到"没关掉"的场景)。

  - **唯一的例外**:`try_wait()` 显示进程**已自行结束** → 按正常结束处理(`killed = false`,由 `exit` 处理器发 `killed: false` 的正常退出事件,界面显示绿"已完成"而不是红"失败")——这条照抄阶段1,它的收尸窗口已被实测命中过。**这里不发 `agent/state`**,因为马上会有 `agent/exit` 覆盖它,多发一条只会让相位闪一下。

  > **回滚这一步不能省**:若不回滚,记录会永远停在 `killed: true`,于是 `listAgents` 永远回 `terminating`、终止按钮永远禁用、而进程还活着——**用户再没有任何办法从界面上终止它**。回滚后相位退回 `running`,用户可以再试一次。
  > **那条 `phase: "running"` 的广播同样不能省**:终止失败的典型情形恰恰是"进程卡住不响应",此时用户很可能已经不耐烦刷新了页面——**发起调用的那个 promise 早没了**,只有广播能把新页面从"正在终止"里拽出来。只改 `killed` 而不广播,等于只修了"原页面"那一半。

  > **`stopAgent` 返回的 `{ stopped: true }` 不含终态承诺**(见 §协议契约 A):它只表示"这次调用既没有报错,也没有拒绝你"。前端收到后**只推进到 `terminating` 并等事件**,由 `agent/exit` 或 `agent/state { phase: "running" }` 决定最终相位。**别在 RPC 成功的分支里直接落"已终止"。**

  > **Rust 侧 `stop_agent` 的 RPC 超时必须大于这里的 3 秒**(本计划定为 5 秒)。反过来的话,Sidecar 还在老老实实等 `exit`,Rust 已经判超时并把 Sidecar 标记成不可用——一次正常的慢终止会被误报成"Sidecar 挂了"。
- **自然退出时必须删池记录 —— 原计划漏了这一步,会直接导致两个可见故障**。
  `child.on("exit")` 的处理顺序**照抄阶段1 的"先清槽、后发事件"**:
  1. 校验池中记录的 `runId` 与 `child` 是同一个实例(防御性,防止旧实例的 exit 吃掉新实例的记录)
  2. **先从池里删除该条记录**
  3. 再发 `agent/exit { runId, agentId, code, killed, resolvedModel }`   // resolvedModel 在删记录前从池记录抄进局部变量

  > **为什么顺序不能反**:前端收到 `agent/exit` 后会立刻允许用户重新启动同一个 Agent。若此时池里还留着旧记录,`startAgent` 第 3 步会撞上一条**早就死掉的记录**并误报 `alreadyRunning` —— 前端随即转入"状态未知 + 重新同步",而实际根本没有进程在跑。阶段1 验收报告里的"四.4 进程自然结束后 78ms 内重启成功"测的就是这条,阶段2 必须保持同样的性质。
  >
  > 漏删的后果同样直接:`listAgents` 会**永远**报告该 Agent `running: true`,界面一直显示"工作中"、终止按钮一直可点,而进程早没了。

- **秒退竞态(子进程在 `startAgent` 返回前就退出)**:这是阶段1 专门造了「Node 快速退出」预设来复现的场景,阶段2 同样必须成立。
  - 池记录**在 `spawn` 之前就已经插入**(见上面的"占位"步骤),所以它一定**早于**任何 `exit` 事件——Node 保证同一进程的事件不会乱序。不存在"先删后插"把死进程又写回池里的情况。
  - `exit` 处理器要能处理两种记录:若还是 `status: "starting"`(进程起来又立刻死了,`spawn` 回调还没来得及升级它),直接删掉即可;若是 `running`,按上面的顺序删。
  - RPC 响应仍照常返回 `{ agentId, runId, … }`,**不需要为这个竞态做特殊处理**——前端阶段1 的令牌机制本来就覆盖"exit 早于 invoke 返回"(验收报告 §三 实测通过)。Sidecar 侧的职责只有一条:**别把死进程留在池里**。
  - `alreadyRunning` 判定前顺手加一道保险:若记录是 `running` 但 `child.exitCode !== null || child.signalCode !== null`,说明它已经死了而 `exit` 事件还在路上 → 直接删掉该记录并当作"未在跑"处理。

- `listAgents()`:
  1. **先 `ensureConfigFresh()`**(见 §关键逻辑 1.1 —— 这一步不能省,否则"改回配置无需重启"不成立)
  2. 合成清单 = **配置里的全部 Agent ∪ 池里有记录但配置里已经没有的 Agent**(见下方"配置里被删掉的运行中实例")
  3. `phase` **直接映射池记录的状态**,按顺序判:`killed === true` → `"terminating"`;`status: "running"` → `"running"`;`status: "starting"` → `"starting"`;没有记录 → `"idle"`。
     > **`killed` 必须先判**:它是"已经在终止中"的标记,漏掉这一档,F5 之后列表会把一个正在终止的实例恢复成普通 `running`、把终止按钮重新放出来(见 §协议契约 A 的 `terminating` 说明)。
  4. `runId` / `resolvedModel` 在非 `idle` 时照常回传——**这是 F5 刷新后前端唯一的状态来源**,正因如此,"自然退出删池记录"不是可选项,而是 `listAgents` 正确性的前提

- **配置里被删掉的运行中实例:合并展示,不静默丢弃。**
  热重载不清掉正在跑的记录(见 §关键逻辑 1.1),但 `listAgents` 如果只遍历配置里的 Agent,就会出现:**用户从 `agents` 里删掉一个正在跑的 Agent → 池记录还在、进程还在跑、可它从列表里消失了**——界面再也看不到它,也**再没有任何办法终止它**,只能去任务管理器。这是 v5 的一处遗漏。

  **处理方式**:以**池记录**为准把这些"孤儿实例"也返回,并标上原因:

  | 字段 | 值 |
  |---|---|
  | `id` | 池记录里的 agentId |
  | `label` | 池记录里缓存的 label(启动时存下的);取不到就回落到 agentId |
  | `configured` | `false` |
  | `reason` | `"该 Agent 已从 config/agents.json 中移除,但这个实例仍在运行;终止它之后再保存一次配置即可让它从列表里消失"` |
  | `phase` / `runId` / `resolvedModel` | 照常从池记录取,和普通实例一样 |

  > **为什么不"配置一改就杀掉移除的实例"**:用户改一个 JSON 文件,不该导致正在跑的任务被强制中断——他可能只是改错了、或者想临时把它挪走。**把决定权留给用户**:他能看到它、能终止它,终止后恰好又触发一次 roster 刷新,它就自然消失了(因为池记录已经被 `exit` 处理器删掉)。
  > 反过来,"静默丢弃"是最糟的选择:进程还在占资源,用户却以为它没了。
  > 与之对称的是**重新把 Agent 加回配置**:池记录还在,它就成了一个正常的 `configured: true` 的 Agent,显示为"工作中"——这不需要任何特殊处理,是合并规则的天然结果。

- **不变式:池记录被移除时,必须伴随一条终态通知。**
  记录只有两种归宿,每一种都配一条通知:

  | 记录怎么没的 | 通知 |
  |---|---|
  | 子进程起来后自行退出 | `agent/exit { runId, agentId, code, killed, resolvedModel }` |
  | 占位后 spawn 失败(根本没起来) | `agent/error { runId, agentId, kind: "agentStartFailed", message, resolvedModel }` |

  > **为什么这条不变式非有不可**:发起 `start_agent` 的那个前端调用**可能已经不在了**。最典型的就是用户在 `starting` 期间按了 F5——页面重载,那个 `invoke()` 的 promise 连同它的 `.catch` 一起被销毁,**RPC 错误再也没人接收**。而刷新后的新页面从 `list_agents` 拿到的是"这个 Agent 在 starting",它没有任何途径知道启动后来失败了,界面就**永久停在"正在启动"**(或者更糟,恢复成了"运行中"),而池里其实早已空空如也。
  > 通知是**广播**给所有监听者的,不依赖某一个调用者还活着。**顺序照旧:先删记录、再发通知**——否则新页面在收到通知后立刻重新同步,会撞上一条已经不存在的占位记录。
- `shutdownAll()`:遍历 kill,不等退出(应用要关了,不阻塞)。
  > 因此 Rust `RunEvent::Exit` 里 `shutdown` 的 1 秒超时是安全的:Sidecar 收到 `shutdown` 会立刻回 `{ok:true}` 然后才去 kill 子进程,不会让 Rust 等到超时。
  > **注意 `shutdownAll()` 只在 Sidecar 自己能跑起来时才有效**——Sidecar 被任务管理器强杀时它根本没机会执行,这种情况由 Rust 侧的 Job Object 兜底(见 §关键逻辑 2.1)。

#### 1.4.1 两个仅测试用的延时钩子 `start_delay_ms` / `stop_delay_ms`

这两个字段**默认都是 `0`,只服务于"把某个瞬时状态拉长到可操作"**。阶段2 有好几条验收都要在某个瞬间状态下动作,而那两个瞬间通常只有几毫秒——阶段1 遇到过同一类问题,当时的解法是造一个「Node 快速退出」预设去复现秒退竞态,**这里是同一套路的反向应用**。

| 字段 | 作用点 | 延长了什么 |
|---|---|---|
| `start_delay_ms` | 占了坑之后、真正 `spawn` 之前 | `starting` 相位(验收 16 用) |
| `stop_delay_ms` | 收到终止请求、相位已推进到 `terminating`、发完 `agent/state` 之后,**真正调 `child.kill()` 之前** | `terminating` 相位(验收 38 用) |

- **`start_delay_ms` 必须由 Sidecar 侧等待,不能在子进程里 sleep**:子进程 sleep 时 `spawn` 事件早就回来了,池记录已经是 `running`,起不到任何作用。**要延长的是 `starting` 这一段**。
- **`stop_delay_ms` 的关键点是"超时从什么时候开始算"**:终止的 **3 秒超时从"收到终止请求"开始计时,而不是从"调用 `child.kill()`"开始**。于是设 `stop_delay_ms: 5000` 配默认的 3 秒超时,就会**稳定地**命中"超时回滚"分支,而且全程**进程根本没被碰过、一直活着**——这正是验收 38 需要的那种可复现场景。
  **为什么不能靠"让脚本忽略终止信号"来制造超时**:Node 在 Windows 上的 `child.kill()` 走的是 **`TerminateProcess`,不可被目标进程拦截**,普通脚本根本"忽略"不掉它。所以那个做法在 Windows 上**不可靠**,必须用延时钩子从 Sidecar 侧制造。
  `stop_delay_ms: 0`(默认)时 `kill()` 几乎立即发生,生产语义与"3 秒内没退出就算超时"完全一致,**没有行为变化**。
- ⚠️ **延时回调醒来后必须重新校验,才允许真的 kill**。这不是锦上添花:延时期间世界会变——**超时可能已经回滚了、用户可能又点了一次终止、Agent 可能已经自然退出**。一个不加校验的定时器会把这些状态全部踩烂。
  ```ts
  // 捕获:发起这次终止时的「身份三件套」
  const myRunId = entry.runId
  const myTermSeq = entry.termSeq
  await sleep(stopDelayMs)

  const now = pool.get(agentId)
  // 三项全部通过才动手;任何一项不成立 → 直接放弃,什么都不做
  if (now?.status !== "running") return        // 记录没了(已退出)或被降级
  if (now.runId !== myRunId) return            // 已经不是那一轮运行了
  if (now.killed !== true) return              // ← 关键:已经被超时回滚了,不能再杀
  if (now.termSeq !== myTermSeq) return        // 用户又发起了一次新的终止,那次会自己处理
  now.child.kill()
  ```
  > **第三条 `killed !== true` 就是本轮要堵的洞**:`stop_delay_ms: 5000` + 3 秒超时,意味着**回调必然在回滚之后才醒**。没有这一条,它醒来就把一个**界面已经显示"工作中"**的进程突然杀掉——用户什么都没做,进程却没了,状态与真实再次分叉。
  > `termSeq` 兜住的是更刁钻的一种:终止 → 回滚 → 用户**又点一次**终止。此时 `killed` 又是 `true`、`runId` 也没变,只有 `termSeq` 能区分"这是不是同一次请求"。
- **不新增 `stop_timeout_ms`**:3 秒这个值本身不需要在验收里改。验收要的是"**制造一次超时**",把 kill 推迟到超时之后,比把超时调到 1 毫秒更稳、更好观察(后者会让进程真的在死,状态在几毫秒内连翻三下,肉眼和 UIA 都抓不住)。
- 两个字段都必须**在配置注释里写明"仅用于测试/演示,会让启动/终止变慢"**,默认值必须是 `0`——**不能让它悄悄留在正式配置里**。

#### 1.5 传输层 `rpc.ts` + 入口 `index.ts`

- `readline.createInterface({ input: process.stdin, terminal: false })` 逐行 `JSON.parse`。**一行解析失败只丢弃该行并记 stderr**,不能让整个 Sidecar 崩掉。
- 响应:收集器攒好后 `JSON.stringify` + `"\n"` 写 stdout。**写入串行化**:用一条 `Promise` 链保证两行不会交错(多 Agent 同时输出是真并发)。
- 未知 method → JSON-RPC `-32601`;handler 抛异常 → `-32000`。**响应的 `error` 必须带 `data: { kind }`**(见 §协议契约 A),不要只发 `message`。`kind` 的取值由 `shared/protocol.ts` 里的联合类型约束,`tsc` 会在编译期挡住拼错的 kind——这也是 finding「Sidecar 错误类型没传给 Rust」的根治办法。
- **退出兜底(三条,缺一不可)**:
  - `process.stdin.on("end")` —— Rust 关闭管道(应用退出或 Rust 崩溃)。**最重要的一条**:它保证 Rust 异常死亡时 Sidecar 不会变孤儿。
  - `SIGINT` / `SIGTERM` / `SIGHUP`
  - 任一触发 → `shutdownAll()` → `process.exit(0)`
- 入口顶部把 `console.log` 重定向到 stderr,防止后续有人误用污染协议流。注释写明原因。

### 2. Rust

#### 2.1 `SidecarClient` —— 本阶段最容易出错的地方

```rust
/// 与「当前这一代 Sidecar 进程」相关的全部可变状态,统一关在一把锁后面。
/// 为什么必须打包:重启要同时替换 child / stdin / job / alive,
/// 分散成几个独立的锁就没法保证它们被一起换掉(读者可能拿到新 child 配旧 stdin)。
struct SidecarProc {
    child: Child,
    stdin: ChildStdin,
    alive: bool,
    job: Option<JobHandle>,            // 当前这一代的 Job Object(内部存 OwnedHandle,见下方"句柄必须可跨线程")
    job_object_ok: bool,               // 本次分配是否成功 —— 要透传给前端
}

struct SidecarClient {
    /// 【状态锁】唯一持有 SidecarProc 的地方。
    /// 作用域严格限定为"读一个字段"或"替换整个 SidecarProc",**绝不在持锁期间做 IO 等待**。
    proc: Mutex<Option<SidecarProc>>,   // None = 从未启动 / 已回收

    /// 【当前代际】唯一真相来源,无锁读取。
    /// spawn 时 `fetch_add(1)` 拿到本代编号,把**这个数字**捕获进读线程与守护线程的闭包;
    /// 它们 emit / 收尸之前先 `current_gen.load()` 比一下,不一致就说明自己已经过期,直接放弃。
    /// 必须放在 proc 锁**外面** —— 读线程绝不能去锁 proc(见下方锁序铁律)。
    current_gen: Arc<AtomicU64>,

    /// 【请求/响应配对】读线程与 call() 共享。
    /// Arc 是因为读线程要持有它;它必须是**独立的锁**,不和 proc 合并
    /// —— 否则读线程投递响应时要抢状态锁,而状态锁可能正被启动流程持有。
    pending: Arc<Mutex<HashMap<u64, SyncSender<RpcResponse>>>>,
    next_id: AtomicU64,

    /// 【单飞】保证同一时刻只有一个线程在 spawn Sidecar。
    /// 独立于上面两把锁 —— 复用其中任何一把,都会让启动期间的正常读写在闸门外干等。
    start_lock: Mutex<()>,
}
```

> ⚠️ **`current_gen` 为什么必须在 `proc` 锁外面(这是 v6 的一处自相矛盾)**:v6 把 `generation` 放进了 `SidecarProc`,同时正文又规定"读线程永远不锁 `proc`"。那读线程就只有自己的局部代际、**没有"当前代际"可以比**,`generation` 的防串扰作用等于零。
> 代际是**单调递增的标量**,不需要和 `child` / `stdin` 同生共死,单独用一个 `Arc<AtomicU64>` 表达最省事,也完全避开了锁。
> **注意别把 `current_gen` 也塞回 `SidecarProc` 里"图个整齐"**——整齐的代价是读线程要么锁 `proc`(破坏铁律),要么拿到一个无法比较的孤立数字。

**锁的三种身份,不能合并**(这是 v5 结构体真正的问题:它让 `child` / `stdin` 裸着,`ensure_started(&self)` 根本没法替换它们):

| 锁 | 保护什么 | 持有时长 | 绝不做什么 |
|---|---|---|---|
| `proc` | 本代进程的全部状态 | 极短:读一个字段,或整体替换 | 不做 spawn、不做 `recv_timeout`、不 join 子线程 |
| `pending` | 请求 id → 响应通道 | 插入 / 取出一个条目 | 不碰 `proc` |
| `start_lock` | "谁负责启动" | 覆盖整个 spawn 过程(可能几百毫秒) | 不碰 `pending` 的写 |

`current_gen` 是 `Arc<AtomicU64>`,**不是锁**,不计入上表——它天生无锁可读,这正是把它单独放出来的原因。

**锁序铁律:规定一个全局顺序,只允许沿一个方向获取**

v6/v7 写的是"**任何时刻最多持有一把锁**"——**这句话是错的**:`ensure_started()` 在持有 `start_lock` 时必然要短锁一次 `proc` 做双检,`spawn_locked()` 最后替换 `SidecarProc` 时也天然同时持有 `start_lock` 和 `proc`。按原话写,启动路径**根本无法实现**。

正确的规则是**分层锁序**:

```
start_lock  →  proc        ← 唯一允许的嵌套
pending                     ← 独立,不与上面任何一个同时持有
```

| 规则 | 说明 |
|---|---|
| **只允许 `start_lock → proc`** | 持有 `start_lock` 时可以再去锁 `proc`;`spawn_locked` 就是靠这一点才能在单飞保护下替换状态 |
| **禁止反向 `proc → start_lock`** | 任何在持有 `proc` 期间去锁 `start_lock` 的代码都是死锁源,**评审时专门看这一条** |
| **`pending` 与另外两把互斥** | 持 `pending` 时不锁 `proc` / `start_lock`,持它们时也不锁 `pending`。读线程全程只碰 `pending` |
| **持有时间按"赋值一条结构体"计** | 尤其 `proc`:只用于读一个字段或整体替换,**绝不在持锁期间做 spawn、`recv_timeout`、join 子线程这类等待** |

具体落到四个路径上:
- `call()`:锁 `proc` 读一次 `alive` 和取 `&mut stdin` 写一行 → **释放** → 再锁 `pending` 取出自己的 `SyncSender`(或反过来,但两步之间必须释放)→ 最后在**全无锁**的状态下 `recv_timeout`
- `ensure_started()`:锁 `start_lock` → 双检时锁 `proc` 读 `alive` → **释放 `proc`**(但**保持** `start_lock`)→ 建 Job、spawn、起读线程 → 最后锁 `proc` 一次性整体替换 `SidecarProc` → 释放 `proc` → 释放 `start_lock`
  > 注意 `start_lock` 是**全程持有**的,只在双检和替换这两个瞬间才叠加 `proc`。这符合"`start_lock → proc`",不会死锁——因为**没有任何代码走反方向**。
- **读线程**:spawn 时把**本代编号**(`Arc<AtomicU64>` 的那次 `fetch_add` 结果)作为局部变量捕获进闭包;每次要 `emit` 前先 `current_gen.load(Ordering::Acquire)` 与自己的局部值比对,不一致就丢弃这条通知。**读线程只锁 `pending`,永远不碰 `proc` / `start_lock`**——代际是原子读,不需要锁。这条是避免死锁的关键,别图省事改成"每次去锁里读当前代际"
- **守护线程**同理:spawn 时捕获本代编号,`try_wait()` 检测到退出后先比对代际,发现自己已过期就**什么都不做**(新的一代有自己的守护线程在管)


**启动时机:随界面首次取 Agent 清单时启动**(即挂载后的第一次 `list_agents`),不是"第一次 `start_agent` 才启动"。

> **这条是对原计划的修正**。原计划写的是纯惰性启动("用户可能只是打开应用看看,没必要常驻一个 node 进程"),但它和两件事直接冲突:
> 1. **Agent 下拉需要一个清单才能选**——用户必须先看到 Hermes / Pi / Claude / Codex 才能点启动,而清单只有 Sidecar 能给(Rust 是哑管道,不解析配置)。纯惰性启动会形成一个死结:没有 roster 就选不了 Agent,选不了 Agent 就永远不启动 Sidecar。
> 2. **刷新恢复需要 roster**——F5 之后前端必须知道哪些 Agent 在跑(见 §前端 3.1),这也要求挂载时就能问一次。
>
> 原计划担心的"node 未安装时应用一打开就报错"仍然成立且已被覆盖:`nodeMissing` 是**内联红字**(规范 §5.3 "不弹窗"),显示在运行视图区域内,应用照常打开、其它页面照常可用。阶段1 验收标准"应用能打开"不受影响。
>
> 代价是应用一打开就会常驻一个 node 进程(实测空载约几十 MB)。这是可接受的:任务书 §3 的架构图本来就把 Sidecar 画成常驻的编排层,而且应用退出时它有完整的清理链路(§2.1)。

**读线程(1 条)**:
- `BufReader::read_until(b'\n')` 循环。**不用 `lines()`**,理由同阶段1:非法字节会中断整条流。
- 解码:先 `std::str::from_utf8(bytes)`,失败再 `encoding_rs::GBK.decode(bytes)`。
  > 这里要认清职责边界:**这条管道里流的全部是 Sidecar 自己 `JSON.stringify` 出来的 UTF-8 报文**,正常情况下 `from_utf8` 永远不会失败。GBK 回退在这一层是**纯粹的纵深防御**,不是中文乱码的解法。真正的中文解码问题在 Sidecar 读 Agent 子进程输出那一层(见 §关键逻辑 1.3),原计划把这两件事混为一谈了。
- 按 JSON 结构分流:
  - 有 `id` → 从 `pending` 取出发送端 `send(response)`;取不到(已超时移除)就丢弃
  - 无 `id` 且 `method` 是 `agent/output` / `agent/state` / `agent/exit` / `agent/error` → `app.emit(...)` 转成 Tauri 事件
  - `sidecar/log` → 打到 Rust stderr(开发时可见),不往前端发

**请求发送 `call(method, params, timeout)` —— 死锁防范是核心**:
1. 取 `id = next_id.fetch_add(1)`,建 `SyncSender` 锁 `pending` 放进去,释放
2. 锁 `proc`,取 `stdin` 写一行(`alive == false` 则放弃并转去 `ensure_started()`),**立即释放 `proc`**
3. **在完全无锁的状态下** `recv_timeout(timeout)`
4. 超时 → 锁 `pending` 移除自己的条目 → `CmdError { kind: "sidecarTimeout" }`

> **为什么绝不能在持锁期间 recv**:读线程要拿 `pending` 才能投递响应;若 handler 持锁等响应,读线程就永远进不来 → 必然死锁。规则简化为一句话:**锁只覆盖准备和写入,不覆盖等待**。
> **为什么第 2 步不能持 `proc` 去等**:`proc` 还被 `ensure_started()` 的替换操作用着;写一行 stdin 是纯内存操作,写完就该放。
> 第 2 步和第 3 步之间的顺序无所谓,但**这两步之间必须释放前一把锁**——`proc` 与 `pending` **不能同时持有**(见上面的分层锁序)。
> `recv_timeout` 而非 `recv`:Sidecar 卡死时 handler 线程不能被永久占住。默认 30 秒(允许冷启动慢),`ping` 用 5 秒。

**Sidecar 死亡处理**:
- 单独的守护线程每 2 秒 `try_wait()`
- **先比代际**:守护线程拿着 spawn 时捕获的本代编号,若 `current_gen.load()` 已经不是它了 → **直接退出,什么都不做**(新的一代有自己的守护线程在管,别去动它的状态)
- 代际仍然匹配 → **先 `TerminateJobObject` 清掉残留的 Agent 孙进程**(见下条,顺序不能反)→ 锁 `pending` `drain` 掉并**丢弃全部发送端**(令在途 `recv` 立即返回 `Err`,不用等超时)→ 锁 `proc` 把 `alive = false`、`job_object_ok = false` → emit `sidecar-status { available: false, message, jobObject: false }`
  > **`jobObject` 是必填字段,死亡事件也不能省**。协议里它没有 `?`,就要处处给值。Sidecar 已经死了,当前当然没有 Job 在保护它——填 `false` 既符合定义,也顺手让前端把降级提示亮起来,提醒用户"现在这个状态下可能有残留"。
  > **两次加锁要分开**:先 `pending` 后 `proc`,中间释放。不能嵌套。
- 下次 `call()` 发现 `alive == false` → **新建一个 Job Object** 再 spawn 新 Sidecar,**重启后自动重跑 `configReload`**(配置可能是用户刚改的)

**自动重启必须是"单飞(single-flight)"的 —— 原方案漏了这层保护**

`call()` 里"`alive == false` 就重新 spawn"是一句**竞态代码**:Sidecar 崩掉之后,如果有两个请求同时发现它不可用(多 Agent 并行时完全正常——用户正同时盯着 Pi 和 Claude,两边都在发请求),**两边会各自 spawn 一个 Sidecar、各自建一个 Job Object**。结果是两个 Sidecar 抢同一个 stdin/stdout 管道、两份 Agent 池、两套事件流,状态彻底错乱。

**做法:双重检查锁定**

```rust
fn is_alive(&self) -> bool {
    self.proc.lock().unwrap().as_ref().is_some_and(|p| p.alive)   // 短暂持锁,读完即放
}

fn ensure_started(&self) -> Result<(), CmdError> {
    if self.is_alive() { return Ok(()); }          // 快路径:绝大多数调用走这里
    let _guard = self.start_lock.lock().unwrap();  // 单飞闸门:只有一个线程能进
    if self.is_alive() { return Ok(()); }          // 双检:等锁期间别人可能已经起好了
    self.spawn_locked()                            // 全程只有这一个线程会 spawn
}
```

`spawn_locked()` 内部**不持 `proc` 锁**——它先建 Job、spawn、起读线程,全部做完后才**一次性**把整个 `SidecarProc` 换进去:

```rust
fn spawn_locked(&self) -> Result<(), CmdError> {
    let Some(job) = JobHandle::create() else { /* 分配失败 → job_object_ok = false,继续 */ };
    let child = Command::new("node").args([...]).spawn()?;
    let job_object_ok = job.assign(&child).is_ok();   // 失败不致命,但如实记账
    let (stdin, stdout, stderr) = ...;

    // 代际:唯一真相来源在这里推进。拿到本代编号后,把它「抄」给读线程和守护线程。
    let gen = self.current_gen.fetch_add(1, Ordering::AcqRel) + 1;
    let reader = spawn_reader(stdout, self.pending.clone(), self.current_gen.clone(),
                              gen, app.clone());       // ← 捕获的是 Arc + 本代编号,不锁 proc

    *self.proc.lock().unwrap() = Some(SidecarProc { child, stdin, alive: true,
        job, job_object_ok });                        // ← 只在这一瞬间持锁
    Ok(())
}
```

> **顺序注意**:`current_gen` 必须在**读线程启动之前**就推进到新值,更要在替换 `proc` 之前。若反过来,新读线程可能在 `current_gen` 还是旧值时就开始收数据,把**属于自己**的通知当成过期丢掉。**先推进代际,再起线程。**

> **`spawn_locked()` 里绝不能持着 `proc` 锁去 spawn**:spawn 冷启动可能几百毫秒,期间任何 `call()` 都会被卡住。**先备好全部材料,最后一次性换上**——这把锁的持有时间应该以「赋值一条结构体」计,而不是以「启动一个进程」计。

- `start_lock: Mutex<()>` 是一把**独立的**锁,**不要复用 `pending` 的那把**——如果复用,读线程在启动期间拿不到 `pending` 锁,新 Sidecar 的第一批通知会被阻塞甚至丢失。
- 等锁的线程不需要额外的等待原语:`_guard` 释放后它们会**重新走一遍双检**,此时 `is_alive()` 已为真,直接返回。**"等一下再看一眼"就够了**,不必引入 `Condvar`。
- 这条与 §关键逻辑 1.4 的"同步检查 + 同步占坑"是**同一个道理**——**"检查"和"创建"之间不能有让出**(那里是 `await`,这里是解锁)。两处都要遵守,否则都是"看起来防住了、实际没有"。
- **`current_gen` 的作用要写准**:每次 spawn 都会为那个子进程单独起一条读线程,旧线程随旧管道 EOF 自然结束,所以"旧进程的事件流进新会话"本身很少发生。它真正要挡住的是**迟到一步的情况**——旧读线程在退出前已经把两行通知读进缓冲区,正要 `app.emit`,此时新的 spawn 已经完成。读线程在 spawn 时捕获本代编号,emit 前与 `current_gen` 比对,不一致就丢弃。**不要**把它写成"重启期间统一丢弃",那样会连同新进程的正常事件一起丢掉。

**Windows Job Object —— 孙进程清理的唯一可靠办法(本阶段必做,不再推到阶段6)**

> **原计划的漏洞**:风险表里写着"强杀 Rust 时孙进程残留 → 已由 Sidecar 的 stdin EOF 覆盖"。这句话**只覆盖了"Rust 死了"这一半**。如果死的是 **Sidecar 自己**(用户在任务管理器里结束 `node sidecar.cjs`——这正是验收 27 要求的操作),`shutdownAll()` 根本没机会执行,它那几个 Agent 子进程就成了孤儿:新 Sidecar 不知道它们的 PID,界面显示"空闲",而 `node` 进程还在后台跑。v2/v3 的验收只查了"能不能重连并启动新 Agent",**完全没查旧进程的下场**——照原样验证,留下一个孤儿进程也能让那一项"通过"。

**做法**(Rust 侧,约 50 行 `unsafe` FFI + `windows-sys` 依赖):

1. `CreateJobObjectW` 建一个 Job,**立刻**设置 `JOBOBJECT_EXTENDED_LIMIT_INFORMATION` 的 `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`
2. `spawn` 出 Sidecar 后立刻 `AssignProcessToJobObject`(Sidecar 在收到第一个 RPC 之前不会产生任何子进程,所以这个时间窗是安全的);此后它**派生出的所有 Agent 自动进入同一个 Job**
3. Rust 持有唯一的 Job 句柄。**应用退出 / Rust 被杀 → 句柄关闭 → 整棵树(含全部 Agent)被内核杀掉**,这条路径比 stdin EOF 更底层、更可靠,是它的兜底
4. **守护线程发现 Sidecar 死亡时调 `TerminateJobObject`** → 立刻清掉孤儿 Agent → 再新建 Job 重启。这正是第 4 条漏洞的正面解法
5. 用一次性的 Job(每次 spawn Sidecar 建一个新的),避免上一次的残留成员混进新会话

**为什么不用 PID 记录文件**:让新 Sidecar 去 kill 旧 Sidecar 记录的 PID 有 **PID 复用**风险——旧 PID 可能已经被系统分配给一个无关进程,杀错代价远大于收益。Job Object 由内核持有成员集合,没有这个问题。

**两个已知坑,实现时直接绕开**:
- **嵌套 Job**:如果 `agenthub-desktop.exe` 本身已经在某个 Job 里(从 VS Code 终端 / CI 里启动时很常见),`AssignProcessToJobObject` 可能返回 `ERROR_ACCESS_DENIED`。Windows 8+ 支持嵌套 Job,正常情况没问题,但**不能假设它一定成功**。
- **句柄必须可跨线程(`Send + Sync`),否则 `SidecarClient` 进不了 Tauri 状态**:`windows-sys` 的 `HANDLE` 是 `*mut c_void`,**裸指针不是 `Send`**;而 `SidecarClient` 要被 `app.manage(...)` 注册成全局状态,Tauri 要求 `Send + Sync + 'static`。`SidecarProc` 虽然裹在 `Mutex` 里(`Mutex<T>: Sync` 只要求 `T: Send`),但那条要求同样绕不过——**`JobHandle` 非 `Send` 就是编译不过**。
  **做法:用 `std::os::windows::io::OwnedHandle` 存句柄**,`CreateJobObjectW` 拿到的裸指针立刻 `OwnedHandle::from_raw_handle` 转进去:
  - 它是 std 的类型,**`Send + Sync` 都有**,不需要任何 `unsafe impl`
  - 它在 `Drop` 时自动 `CloseHandle`——正好省掉手写的 `Drop`,而且不会漏
  - 传给 Win32 API 时用 `AsRawHandle` 取回裸句柄即可
  > **不建议"存 `isize` + 手动 `unsafe impl Send`"**:能编译,但把"生命周期由谁关"这件事交回给人手,而这恰恰是"漏关一次就永远杀不掉孙进程"的关键句柄。**有 std 的 `OwnedHandle` 就不要手搓。**
  > 非 Windows 平台上 `JobHandle` 是空实现(不持有任何东西),天然满足要求。
- **句柄生命周期**:`KILL_ON_JOB_CLOSE` 的语义是"**最后一个**句柄关闭时杀"。要确保 Rust 侧只有一个句柄,并在 `SidecarProc` 里持有它(靠 `OwnedHandle` 的 `Drop` 关闭)。重启 Sidecar 时**先建新 Job、再 spawn,最后才整体替换 `SidecarProc`**——旧的那个随替换被 drop,顺序反了会把刚要启动的新进程一起卷进去。
  > 注意这里**不能"先 drop 旧 Job 再建新的"**:旧 Job 一旦没有句柄就会被内核销毁,连带杀掉它里面还活着的进程——如果此时新 Sidecar 已经被 spawn 并指派进了同一个(或误入旧)Job,新进程会跟着一起死。**整体替换**这个写法天然规避了这个问题。
- **与协议字段的对应**:`sidecar.available` 读 `SidecarProc::alive`;`sidecar.jobObject` 读 `SidecarProc::job_object_ok`。**重启时 `job_object_ok` 必须按新一代的结果重新写**,不能沿用上一代的值——旧的一代可能成功、新的一代可能失败。

**分配失败怎么办 —— 不许静默降级,必须让状态可见**

上一版计划在这里写的是"失败时记一条警告,继续走原有清理路径"。**这个说法和验收打架**:验收 24(强杀 Sidecar 后旧 Agent 一并消失)和验收 29(强杀应用后无残留)**都依赖 Job Object 生效**——如果分配失败还"静默继续",这两条验收就失去了保证,验证者却不知道,等于用一份"通过"的报告掩盖了机制没生效。

**定下来的做法**:

1. **分配失败不是致命错误,应用照常可用**(不让一个清理增强项把主流程搞挂),但**必须把这个事实一路暴露到界面上**:
   - Rust 把结果放进 `sidecar-status` 事件的 `jobObject: boolean` 字段(见 §协议契约 B)
   - 界面在 `jobObject === false` 时显示一行**常驻的浅色提示**:"进程清理降级模式(未能接管子进程组),关闭应用后可能残留 node 进程,请在任务管理器手动结束"。用 `--text-secondary`,不用红色——它不是故障,是能力受限。
2. **验收 27 / 32 变成显式条件项**:执行验收时**先确认界面上的 `jobObject` 状态**:
   - `jobObject === true` → 按原判据验证,必须通过
   - `jobObject === false` → 记为 **"未验证 — 当前环境不支持 Job Object"**,并附上 `ERROR_ACCESS_DENIED` 的原始错误。**不许判为通过**。
   - 这与阶段1 验收报告处理那 4 项未验证项的方式一致(逐条给出代码位置与原因,**不猜测通过**),是可接受的验收结论。
3. **不做 PID 文件兜底**。让新 Sidecar 去 kill 旧记录里的 PID 有 PID 复用风险(可能杀掉无关进程),作为一条增强的备选路径,**风险大于收益**。宁可让用户看到"降级模式"的提示,也不要引入一个可能误杀的机制。

**非 Windows 平台**:`#[cfg(not(windows))]` 下退回到现有的 `RunEvent::Exit` + stdin EOF 路径(阶段6 跨平台打包时再补 `setpgid` / `prctl` 方案)。本阶段宿主机只有 Windows,不阻塞。

**Job 状态怎么同步到前端(两条路都要,缺一不可)**

`jobObject` 必须同时走**事件**和**查询**两条路:

| 路径 | 覆盖的场景 |
|---|---|
| `sidecar-status` 事件 | Sidecar **活着期间**状态发生变化:死了、重启了、这一代 Job 分配失败 |
| `list_agents` 命令返回值里的 `sidecar.jobObject` | 前端**挂载/刷新时**的初始状态 |

> ⚠️ **只靠事件是不够的(这是原方案的漏洞)**:`sidecar-status` 只在**状态变化**时发出,而前端 F5 会把自己的状态清空。如果此时 Sidecar **已经好好地活着**,`list_agents` 不会重新 spawn 它 → **不会产生任何新的 `sidecar-status` 事件** → 前端永远不知道 `jobObject` 是真是假。后果有两个:界面丢掉"清理降级模式"的提示;验收 27/32 的前置条件无从确认,验证者只能靠猜。
> **规则**:凡是"前端在挂载时必须知道的状态",都要挂在一个**挂载时必然会调**的命令上,而不是只依赖事件。事件负责"变化",查询负责"现状"。

**这条要同步进验收**:验收 27 增加"杀掉 Sidecar 后,**旧 Agent 进程也一并消失**";验收 31 / 32 覆盖"正常关窗"与"强杀应用"两条路径的无残留。**三条都以界面上 `jobObject === true` 为前置**,不满足时记"未验证"而非通过。验收时**除了看提示,还要按一次 F5 确认提示仍在**——那正是这条查询路径的验证点。

**路径解析**(Sidecar 脚本 + 配置文件),照抄阶段1 已验证的写法:
```rust
// 开发模式:resource_dir 里可能没有,回退到 CARGO_MANIFEST_DIR 的上一级(项目根)
let script = resolve_resource(app, "sidecar/dist/sidecar.cjs")
    .or_else(|| Some(PathBuf::from(env!("CARGO_MANIFEST_DIR")).parent()?.join("sidecar/dist/sidecar.cjs")))?;
let config = resolve_resource(app, "config/agents.json")
    .or_else(|| Some(PathBuf::from(env!("CARGO_MANIFEST_DIR")).parent()?.join("config/agents.json")))?;
```
两个路径作为 CLI 参数传给 Sidecar:`node <script> --config <config>`。**相对路径全部由 Sidecar 以配置文件所在目录为基准解析**,Rust 不参与。

**spawn 参数**:`Stdio::piped()` × 3;**必须消费 stderr**——不读的话 stderr 管道写满 64KB 会阻塞 Sidecar。再起一条读线程转发到 Rust 的 `eprintln!`,并缓冲最后若干行用于错误提示。

**Windows 隐藏窗口**:`#[cfg(windows)]` + **`std::os::windows::process::CommandExt`** 的 `creation_flags(0x08000000)`(CREATE_NO_WINDOW)。否则每次调用都会闪一个黑框。
> ⚠️ **trait 名别写错**:提供 `creation_flags` 的是 **`std::os::windows::process::CommandExt`**,不是 `StdioExt`(std 里没有这个名字)。v9 及更早的文本写的是 `StdioExt`,照抄会直接编译失败。正确写法:
> ```rust
> #[cfg(windows)]
> use std::os::windows::process::CommandExt;   // ← creation_flags 来自这里
> #[cfg(windows)]
> cmd.creation_flags(0x08000000);              // CREATE_NO_WINDOW
> ```
> (阶段1 的计划文档里有同一个笔误,但那一阶段的代码已经编译通过并验收,实现时用的显然是对的 trait;**本阶段以这里的写法为准**,不要从阶段1 的文档里抄那一行。)

**应用退出清理(`RunEvent::Exit`)**:
1. 先发 `shutdown` RPC(短超时 1 秒),让 Sidecar 自己收拾子 Agent —— **优雅路径**
2. **关闭 stdin**(drop `ChildStdin`)—— 触发 Sidecar 的 EOF 兜底,即使 `shutdown` 没送到也有效
3. 给 1 秒宽限,`try_wait()` 仍在跑就 `kill()` —— 兜底
4. **顺序很重要:先关 stdin 再 kill**。反过来会在 Sidecar 收完子进程之前就把它砍了,子进程变孤儿。
5. **最后 drop Job 句柄** —— 内核兜底。前三步是"讲道理",这一步是"不讲道理也能保证干净";即使 Rust 在 1~4 之间被强杀,句柄随进程销毁关闭,整棵树照样死透(见上一条 Job Object)。

#### 2.2 命令实现

```rust
#[tauri::command]
async fn start_agent(app: AppHandle, agent_id: String, prompt: String,
                     model: Option<String>, run_id: u64) -> Result<StartAck, CmdError>
```

- **必须 async**:Tauri v2 的同步命令在主线程执行,`recv_timeout(30s)` 会把窗口冻住 30 秒。async 命令跑在异步运行时线程池上,不阻塞 UI。
- 内部:取 `SidecarClient`(不存在或已死 → `ensure_started()`)→ `call("startAgent", {...}, 30s)` → 把 `StartAck` 透传给前端(含 `injectedEnv` / `resolvedModel` / `cwd`)。
- `call()` 把 Sidecar 错误响应的 `error.data.kind` **原样映射**成 `CmdError.kind`;Rust 自己产生的边链路错误用 `nodeMissing` / `sidecarCrashed` / `sidecarTimeout` / `internal`(见 §协议契约 A 的来源划分表)。
- `stop_agent`:`call("stopAgent", { agentId, runId }, 5s)` —— **`runId` 必传**(**超时必须 > Sidecar 内部等 exit 的 3 秒**,理由见 §关键逻辑 1.4)。
- `list_agents`:`call("listAgents", {}, 10s)` —— 结果含运行时状态,前端用它做 F5 后的状态恢复。
- 前端调用参数用 camelCase(`agentId` / `runId`),Tauri 自动映射到 Rust 的 snake_case 形参。

### 3. 前端

#### 3.1 `src/hooks/useSidecar.ts` —— 承接阶段1 的健壮性机制

阶段1 那套经过多轮评审的机制**方向全部保留**,但有两处在阶段2 必须改造——因为阶段1 只有**一个**进程,而阶段2 允许多个 Agent 并行。直接照抄会出问题。

| 机制 | 阶段2 的处理 |
|---|---|
| `mountGenRef` 挂载代际编号 | **原样保留**。它管的是"这次挂载是否还有效",挂载与否天然是全局的,与有几个 Agent 无关 |
| `opRef` 操作令牌 | **必须按 Agent 拆分**(见下),全局单令牌在并发下会互相误伤 |
| `phase` 单状态机 | **必须按 Agent 拆分**(见下),"当前主 Agent"在并发下没有定义 |
| `subscribing` / `unknown` 分离 | **保留,并升级为全局维度**。它与具体哪个 Agent 无关,是"事件通道 / Sidecar 连接"的状态 |
| 事件携带 runId 过滤 | **保留,并加上 agentId 过滤** |

> ⚠️ **原计划的漏洞**:一边说"阶段2 允许多个不同 Agent 并行",一边说"机制原样保留"。但阶段1 的 `opRef` 是**一个全局计数器**,`applyExit()` 会 `opRef.current += 1` 作废所有在途操作。并发下这会直接咬人:
> Claude 正在跑,A 用户按下 Pi 的启动 → `start_agent` 请求在途;此时 **Claude 自然结束**,`applyExit()` 把全局令牌加一 → Pi 的启动回调醒来发现"号作废了"直接 `return` → **Pi 的 phase 永远停在 `starting`**,按钮永远禁用,只能刷新页面。
> 终止方向同理:终止 Pi 时 Claude 恰好退出,PI 的 `terminating` 就再也推不到 `finished`。
> 这不是理论问题——阶段1 验收报告里"四.3 收尸窗口""四.4 自然结束后立刻重启"测的正是这类时序,阶段2 只是把并发数从 1 变成了 N。

**状态模型:改成"每 Agent 一份 + 两个全局维度"**

```ts
// 全局维度(与具体 Agent 无关)
type ChannelPhase = "subscribing" | "ready" | "unknown"   // 事件通道
type SidecarInfo  = { available: boolean; message?: string; jobObject: boolean }

// 每个 Agent 一份
type AgentRuntime = {
  phase: "idle" | "starting" | "running" | "terminating" | "finished"
  runId: number | null
  /** 【终态作废】只由 agent/exit / agent/error 递增。用来作废"那次操作还该不该写状态" */
  opToken: number
  /** 【状态修订号】任何**写了运行时记录**的事件都递增(见下方说明),用来判断快照是否已被事件超越 */
  rev: number
  result: "success" | "failed" | "terminated" | null
  resolvedModel: string | null
  /** 上次启动的注入结果,供"注入摘要 / 详情"渲染;没启动过则为 null */
  injectedEnv: Record<string, InjectedValue> | null
  /** 上次启动的工作目录,摘要里显示 */
  cwd: string | null
  lastError: string | null
}
```

> **`opToken` 与 `rev` 是两个计数器,职责不同,不能合并**(v8 只留了 `opToken`,于是旧快照能把 `agent/state` 的结果盖回去):
>
> | | `opToken` | `rev` |
> |---|---|---|
> | 谁递增 | **只有终态事件**(`agent/exit` / `agent/error`) | **任何对运行时记录的写**:事件(`agent/state` / `agent/exit` / `agent/error`)**和本地操作**(`start()` 写 `runId=新值`+`phase=starting`、`stop()` 写 `phase=terminating`)都递增 |
> | 回答的问题 | "我这次异步操作还算不算数" | "这条记录自从我读它之后,有没有被动过" |
> | 用在哪 | `start` / `stop` / 快照的**操作**校验 | **快照的过期判断** |
>
> **为什么不能合成一个**:`agent/state` 必须递增 `rev`(否则快照会盖它),但**绝不能递增 `opToken`**(否则它会把紧接着返回的 `StartAck` 判为作废,注入详情又变空白——那正是 v8 修掉的 bug)。两个集合**必须不同**,所以必须是两个数。
> **`agent-output` 两个都不递增**:它只往输出区追加行,不改运行时记录的相位与 runId。

**状态要用"state + ref 双份",不能只有一个 Map ref**

```tsx
const [runtimes, setRuntimes] = useState<Record<string, AgentRuntime>>({})  // 驱动渲染
const runtimesRef = useRef(runtimes)                                        // 供异步回调同步读取

// 唯一的写入口:先写 ref(同步可见),再 setState(触发渲染)
function patchRuntime(agentId: string, patch: Partial<AgentRuntime>) {
  const next = { ...runtimesRef.current }
  const prev = next[agentId] ?? EMPTY_RUNTIME
  // rev 逢写递增(除非调用方在本次 patch 里显式给了新值)——"写运行时记录"的唯一入口
  next[agentId] = { ...prev, rev: prev.rev + 1, ...patch }
  runtimesRef.current = next          // ← 必须在 setState 之前
  setRuntimes(next)
}
```

> ⚠️ **原方案只给了一个 `runtimesRef = useRef(new Map())`,这是不够的**:直接改一个 ref 持有的 Map **不会触发任何重渲染**,`AgentStatusList` 和运行状态行都不会更新——界面看起来"卡住",而数据其实已经变了。
> 这正是阶段1 用过的模式(`phaseRef` + `phaseState`,由 `setPhase()` 同时写两份),阶段2 只是把它按 agentId 展开。**异步回调一律读 `runtimesRef.current`**(它们闭包里拿到的 `runtimes` 是旧的),**渲染一律读 `runtimes`**。

**规则**:
0. **启动时必须先把 `runId` 写进该 Agent 的记录,再 invoke 后端**:
   ```tsx
   const runId = Date.now()
   patchRuntime(agentId, { phase: "starting", runId, result: null, lastError: null })
   // ……invoke 之后的处理见下方「启动响应的应用方式」
   ```
   这是阶段1"先写 `runIdRef` 再 invoke"(`src/App.tsx:244→253`)的按 Agent 版本。**顺序反了会丢首行输出**——第一条 `agent-output` 到达时 `runtimes[agentId].runId` 还是 `null`,会被当成过期数据丢掉。阶段1 为这个坑专门改过一轮,阶段2 不能重犯。
1. **`beginOp(agentId)` / `opValid(agentId, token)` 读写的是 `runtimes[agentId].opToken`** —— 操作只作废**同一个 Agent** 的在途操作,绝不碰别的 Agent。
2. **`applyExit(agentId, payload)` 递增该 Agent 的 `opToken` **和** `rev`**。"退出事件是最终权威"这句话要加上限定:"**对该 Agent 而言**是最终权威"。
3. **`rev` 的递增点是"任何对运行时记录的写"**:三个事件(`agent/state` / `agent/exit` / `agent/error`)与**本地操作**(`start()` 写入新 `runId` + `starting`、`stop()` 写入 `terminating`)都要递增;`agent-output` 不递增。**漏掉任何一处,快照就会在该场景下盖掉更新的状态**——v8 漏的是 `agent/state` 那一处,v11 漏的是本地 `start()` 那一处(它写入的 `runId` 不递增任何计数器,导致在途快照能趁"rev 未变"把旧轮次写回来,见 §前端 3.1 快照规则表的 v12 警示)。
   **最简单的实现:`patchRuntime()` 默认递增 `rev`**(它就是"写运行时记录"的唯一入口),只有纯输出追加走独立路径。这样"逢写递增"不需要逐个调用点记得加。
   > `StartAck` 补写 `injectedEnv` / `cwd` / `resolvedModel` 也走 `patchRuntime()`,同样递增 `rev`——**没关系**:它改的就是这条记录本身,递增只会把**更旧的**在途快照作废,方向正确。身份字段的"相同 `runId` 就采纳"规则也不受影响:`resolvedModel` 整轮不变,谁写、什么时机写,结果都一样。
4. **`mountGenRef` 的校验对所有 Agent 一律照做**——它是挂载维度的,卸载后任何 Agent 的回调都不该再写状态。
5. `ChannelPhase` 为 `subscribing` / `unknown` 时,**所有** Agent 的启动/终止按钮都禁用(通道没通,发什么都没意义);`ready` 时各 Agent 按自己的 `phase` 独立决定按钮可用性。
6. `AgentStatusList`、运行状态行、注入摘要**全部从 `runtimes` 这个 state 渲染**,不再需要"主编"这个概念。`injectedEnv` / `cwd` 就存在记录里,启动成功后由 `start_agent` 的返回值填入。**刷新恢复只能恢复 `resolvedModel`**(`list_agents` 不回传注入详情),所以 F5 之后注入详情是空的,需要重新启动一次才有——这与"刷新后输出区为空"是同一类取舍,可以接受,但要在界面上显示"重新启动后可见"而不是留一片空白让人以为坏了。

**每个 Agent 的 phase 表**(与阶段1 的同名,只是作用域缩小到一个 Agent):

| phase | 含义 | 启动 | 终止 | 圆点 |
|---|---|---|---|---|
| `idle` | 该 Agent 空闲 | 可用 | 禁用 | 灰 |
| `starting` | 已 invoke 未确认 | 禁用 | 禁用 | 蓝(呼吸) |
| `running` | 该 Agent 运行中 | 禁用 | 可用 | 蓝(呼吸) |
| `terminating` | 已请求终止 | 禁用 | 禁用 | 蓝→灰 |
| `finished` | 已结束(绿/红/灰由退出事件定) | 可用 | 禁用 | 绿/红/灰 |

**通道 phase 表**(全局):

| ChannelPhase | 含义 | 启动 / 终止 |
|---|---|---|
| `subscribing` | 事件监听尚未注册成功 | **全部禁用**,且不显示「重新同步」(监听不在,重试是假的安全感) |
| `ready` | 通道就绪 | 按各 Agent 自己的 phase 决定 |
| `unknown` | 通道就绪但状态未知(Sidecar 不可用 / 前后端脱节) | **全部禁用** + 显示「重新同步」 |

**`agent-exit` 必须按 agentId 区分**:事件到达时只更新 `runtimes[agentId]`。**理由**:阶段2 允许多个不同 Agent 并行,若不区分,Codex 退出会把正在跑 Claude 的状态错误地置成"已结束"。这也是**阶段1 的事件必须改名**的原因——阶段1 的 `agent-exit` 在 `unknown` / `subscribing` 相位会接受任意 runId 的退出事件,复用同名事件会让诊断控制台被这里任何一个 Agent 的退出误推进(见 §协议契约 C)。

**`agent/state` 是"非终态相位"的权威广播**:收到时把该 Agent 的 `runtimes[agentId].phase` **直接改写**成 payload 里的 `phase`,并**一并写入 `resolvedModel`**,再**递增 `rev`**。
- **不改 `result`、不写 `lastError`**——它描述的是"还活着、处于哪个阶段",不是结束。
- **`resolvedModel` 要一起写**:它属于"运行身份",整个 run 内不变。**必须让这条广播能独立建出一条完整记录**——刷新后前端可能只有它、没有快照(见下方事件判定表第三行),漏写就会变成"界面说工作中,却不报用的是哪个模型"。
- **不递增 `opToken`**(与 `applyExit` / `agent/error` 不同,见下方说明)。
- **递增 `rev`**(与它们相同)——否则一份在途的旧快照会把它刚写下的相位盖回去,见下方"为什么需要 `rev`"。
- **只在该 Agent 的 `runId` 与 payload 一致时应用**(不匹配说明是上一轮的残留广播,丢弃)。
- **幂等**:若当前已是同样的 `phase`,直接丢弃(避免重复渲染)。
- **这是把新页面从"正在终止"里拽出来的唯一途径**:终止超时回滚发生在**发起终止的那个 promise 早就被 F5 销毁之后**,只有广播能到。(见 §协议契约 A)

**事件到达时的 `runId` 归属判定 —— 三个事件共用一张表**

`agent/state` / `agent/exit` / `agent/error` 都携带 `runId`,判定规则**完全一致**(不要三个事件各写一套):

| 本地该 Agent 的记录 | 处理 |
|---|---|
| 存在,且 `runId === payload.runId` | 正常应用 |
| 存在,`runId !== null`,且 `!== payload.runId` | **丢弃**——上一轮运行的残留广播 |
| **不存在,或记录的 `runId === null`** | **接受,并用 `payload.runId` 建立 / 补全这条运行时记录**(同时 `rev += 1`) |

> ⚠️ **第三行是 v9 漏掉的兜底,也是刷新窗口里唯一能救命的路径**。场景:**用户 F5 时 Agent 正在跑 → 新页面还没有任何 `runId` 记录 → `list_agents` 已发出但未返回 → 此时 `agent/state` / `agent/exit` / `agent/error` 先到**。按"runId 必须一致"的严格规则,这条事件会被**直接丢弃**;随后旧快照返回,而本地也没有可供比较的 `rev`(记录压根不存在),快照照样被接受——**界面就永久停在错误状态,`rev` 一点忙都帮不上**。
> **把事件里的 `runId` 认下来,一切就顺了**:记录被建立、`rev` 变成 1;等旧快照返回时,`revAtRequest[agentId]` 是 `undefined` 而本地已是 1 → **相位校验失败被丢弃**(同一轮的 `resolvedModel` 仍按 `runId` 匹配补全,这是无害且正确的)。旧快照与事件之间的先后顺序,从此不再重要。
>
> **事件自足原则(与 `agent/state` 必须带 `resolvedModel` 是同一条要求)**:三类事件都可能**先于任何快照**成为一条新记录的唯一来源,所以它们的 payload 必须能独立支撑起完整的运行时记录——`agent/state` / `agent/exit` / `agent/error` **一律携带 `resolvedModel`**(v11 只给 `agent/state` 带了,漏了两个终态事件:刷新后最先到达的完全可能是一场退出,进程随即从池中删除,快照再也不会带这一轮的模型名——这个信息就永远丢了)。Sidecar 侧的实现要求:**发通知前把 `resolvedModel` 从池记录抄进局部变量**,删记录之后仍要拿得到。
>
> **这正是阶段1 那条兜底的按 Agent 版本**。阶段1 写的是"刷新恢复或前后端脱节时 runId 尚不可知,仍接受退出事件(`src/App.tsx:195-206`),避免永久卡住"——**阶段2 必须保留同等语义**,只是判断依据从"通道相位"换成了更精确的"**本地知不知道是哪个 runId**"。
> 用后者而不是照抄阶段1 的 `subscribing` / `unknown` 判据是有原因的:**这个场景里通道可能已经是 `ready`**(`listen()` 早就完成了,只是 `list_agents` 还没回来),按通道相位判会漏掉它。
>
> **`agent-output` 不建立记录**:它只往输出区追加行,不携带相位。刷新后到快照返回之前的那几行输出会被丢掉——这与"刷新后输出区为空"是同一类取舍,**可以接受**,而且窗口只有一次 RPC 往返那么长。快照一回来 `runId` 就有了,输出自然接上。

**为什么需要 `rev` —— 广播与快照的先后顺序**

v8 里 `agent/state` 不递增 `opToken`(为了不误伤 `StartAck`),但这留下了一个新洞:**旧快照可以反过来盖掉广播的结果**。复现路径很短:

1. 页面挂载 / 点开下拉 → 发出 `list_agents`。**此刻 Sidecar 侧该 Agent 还是 `starting`**
2. 响应还在路上,`agent/state { phase: "running" }` 先到 → 前端相位改成 `running`
3. 旧快照返回 → 按 v8 的规则只校验 `opToken`,而 `agent/state` 没动过 `opToken` → **校验通过** → 相位被改回 `starting`
4. 结果:Agent 明明在跑,界面卡在"正在启动",**终止入口永久禁用**

`rev` 就是堵这个洞的:**快照只在该 Agent 的 `rev` 没被动过时才有权写运行时字段。** 一句话概括两者的分工:

> **`opToken` 判"操作要不要作废",`rev` 判"数据是不是过期"。** 前者只看终态,后者看一切写运行时记录的事件。

> **`rev` 的初值与递增点**:新 Agent 记录创建时 `rev = 0`(`EMPTY_RUNTIME` 里定义);递增**不需要各调用点自觉**——`patchRuntime()` 默认 `rev + 1`(见上面的实现),任何写运行时记录的路径天然都递增。**新增事件种类或新的写入路径时,只要它走 `patchRuntime()` 就自动受保护**——这正是把"逢写递增"做成默认行为而非约定纪律的原因:v8 漏 `agent/state`、v11 漏本地 `start()`,两次都是"靠人记得加"出的事。

> ⚠️ **为什么 `agent/state` 绝不能递增 opToken**(v7 的错误):
> `spawn` 成功后 Sidecar **先发 `agent/state { phase: "running" }`、再返回 `StartAck`**(见 §关键逻辑 1.4 第 6 步)。如果这条广播把 opToken 加一,那么**紧接着返回的那个 `StartAck` 就会因为"令牌已作废"被前端丢弃**——结果是:Agent 明明跑起来了,界面却**没有保存 `injectedEnv` 和 `cwd`**,注入详情间歇性空白(取决于广播和 RPC 响应谁先到,所以是**偶发**的)。
> **判据变得很直接**:`opToken` 的职责是"**终态一到,作废所有在途操作**",而 `agent/state` 描述的是**还没结束**。把非终态塞进终态机制,必然误伤——尤其是**它自己那次操作的响应**。**只有 `agent/exit` / `agent/error` 递增 `opToken`**,这条规则不要扩大。

**启动响应的应用方式:按 `runId` 归属,不受 `opToken` 约束**

```tsx
const ack = await invoke("start_agent", { agentId, prompt, model, runId })
// ① 卸载检查照旧
if (!alive(gen)) return
// ② 归属检查:runId 不再匹配 → 期间已经又启动了新的一轮 → 整包丢弃
if (runtimesRef.current[agentId]?.runId !== runId) return
// ③ 既成事实:无条件写入(这是「那一次运行」的属性,按 runId 归属)
patchRuntime(agentId, { injectedEnv: ack.injectedEnv, cwd: ack.cwd, resolvedModel: ack.resolvedModel })
// ④ 相位:只在还是 starting 时才推进(已被事件推进过就不动,避免把 finished 改回 running)
if (runtimesRef.current[agentId]?.phase === "starting") patchRuntime(agentId, { phase: "running" })
```

> **为什么不复用 `opValid` 就完事**:`injectedEnv` / `cwd` **不是"当前状态",而是"第 N 次运行的既成事实"**——只要还在说第 N 次运行,它们就有效,与期间有多少事件经过无关。用"令牌还有效吗"来管它们,就会像上面那样被自己触发的事件误伤;用"还是不是同一轮 runId"来管,才是它们真正的归属判据。
> 这条同时覆盖了**秒退场景**:进程起来又立刻退出时,`injectedEnv` 照样会被写入(便于用户回看当时注入的是什么),而相位因为已经是 `finished`、不满足 `starting` 条件,**不会被错误地改回 `running`**。

**`agent/error` 是一条终态通知,不是"提示一下就完了"**:收到它时,把对应 Agent 的 `runtimes[agentId]` 推进到 `finished` + `result: "failed"` + `lastError: message`,并**递增该 Agent 的 opToken**(与 `applyExit` 同样对待——它也是最终权威)。**幂等**:已经 `finished` 的直接丢弃,避免"RPC 错误 + 通知"先后到达时翻两次。

> 这条处理不是可选的装饰。**它是 §关键逻辑 1.4 那条不变式的接收端**:刷新会销毁原来那个 `invoke()` 的 promise,`start_agent` 的 RPC 错误就没有任何接收者了;能把状态收回来的**只有这条广播通知**。少了它,验收 16 ②(在 `starting` 期间刷新、随后启动失败)会永久卡在"正在启动"。
> 同时注意:`agent/error` 也用于运行期错误(见 §协议契约 A 的通知表),所以它**可能在该 Agent 正在 `running` 时到达**——上面"推进到 `finished`"的规则对这两种情况都成立。

**`alreadyRunning` 分支照抄阶段1,但作用域是该 Agent**:把该 Agent 转入 `unknown` 并重新同步,**绝不清空它的 runId、绝不落 idle**。理由:后端说该 Agent 已有实例在跑 = 前后端已脱节,清空 runId 会让那个真实进程的输出和退出事件永远收不到。

**刷新恢复(不能漏)**:`useSidecar` 挂载成功、`listen()` 确认上线之后,**必须调一次 `list_agents`**。这是阶段1 `syncStatus(gen)` 的等价物——少了它,F5 之后界面会显示"全部空闲"而进程其实还在跑。

**但一份 `list_agents` 响应里混着性质不同的数据,必须分开适用规则**——这是 v5 的一处真实缺陷,而 v10 又把运行时那一组拆得更细了一层:

| 组 | 字段 | 接受规则 |
|---|---|---|
| **配置元数据** | `AgentInfo` 的 `label` / `defaultModel` / `modelAliases` / `configured` / `reason`,`sidecar.{available, jobObject}` | **最新响应一律接受**(只受 `rosterSeq` 约束) |
| **运行身份** | `AgentInfo` 的 `runId` / `resolvedModel` | **本地无记录或 `runId === null`** → 采纳(建立记录);**本地 `runId` 与快照相同** → 采纳(补全 `resolvedModel`);**本地 `runId` 非空且不同** → **运行身份整组丢弃,无条件,不看任何序号** |
| **当前相位** | `AgentInfo` 的 `phase` | **按 `rev`**:必须 `rev` 未变才接受(`rev` 现在是"**逢写递增**"——事件与本地操作都递增,见机制表) |

> ⚠️ **v11 的"不同 → 仅在 `rev` 未变时采纳"是个实质性漏洞,必须去掉这个例外**(v12)。复现序列:
> ① 快照请求发出(内容:第 1 轮);② 用户启动第 2 轮 → 本地写入 `runId=2`——**这一步不递增 `rev`**;③ 快照返回:`runId` 不同、`rev` 恰好没变 → 按 v11 规则**把 `runId=1` 和旧模型名写回本地**;④ 第 2 轮的 `StartAck` / 事件按 `runId` 归属判定 → 不匹配 → **被丢弃**。整个第 2 轮的状态从此悬空。
>
> **为什么 `rosterSeq` 救不了它**:序号只回答"两次**查询**谁新",回答不了"**查询**和**本地操作**谁新"。这两者不在同一个计数空间里。
> **为什么也不需要 Sidecar 的单调运行序号**:本地 `runId` 非空且与快照不同这一事实,本身就已经说明快照不是最新——本地那条记录只有两个来源:**本页刚发起的新一轮**(`runId` 是此刻的 `Date.now()`,快照的查询请求发在它之前,内容必然更旧),或**刚到达的事件建立的**(那么 `rev` 已递增,连相位都进不来)。无论哪种,采纳快照的"不同 `runId`"都是**往回覆盖**。所以规则收紧成无条件丢弃,不需要再加一层序号。
> **顺带把相位侧的同型漏洞一起堵上**:若快照在"本地刚写入 `phase: starting`"之后返回一个 `idle`/旧相位,`rev` 只有事件会递增的话它照样能进来。因此 **`rev` 改为逢写递增**(见机制表),本地写入同样把在途快照作废。

> **为什么运行时那一组还要再拆**:`resolvedModel` 是**整轮运行内不变的身份信息**(这一轮用的哪个模型),`phase` 是**随时会翻的当前状态**。把它们捆在一起用同一个判据,必然要出错——v10 就是这样:
> 刷新期间前端先收到 `agent/state { phase: "running" }` 建起了记录(此时**还没有模型名**),随后快照返回:它的 `runId` 与本地**完全一致**、`resolvedModel` **正是缺的那个值**,却因为 `rev` 不等被**整组丢掉** → 界面显示"工作中"却不报模型。
> **同一轮运行的身份信息,只要 `runId` 对得上就该补上**;只有"当前处于哪个阶段"才需要用 `rev` 防过期。

> ⚠️ **这条划分是贯穿全篇的一条原则,不是这一处的地方规则**。回头看:
> - v8 的 `StartAck`:`injectedEnv` / `cwd` 按 **`runId` 归属**接受,不受 `opToken` 约束 —— 同一个道理
> - v10 的事件判定表:本地没有记录时用 payload 的 `runId` 建记录 —— 也是让**身份信息**优先于**相位**
> - 这里:快照的 `resolvedModel` 只看 `runId`
>
> **一句话:`runId` 管"是不是同一轮",`rev` 管"是不是最新的一版相位"。** 遇到新字段时先问它属于哪一类,再决定判据。

> ⚠️ **原方案把这组规则混用了(必改)**:v5 写的是"只有 `subscribing` / `unknown` 相位才接受这份快照"。这条对**运行时状态**是对的(通道活着时,事件流比任何查询都快、都准,拿一份可能过时的快照去覆盖是**回退**),但对**配置元数据**完全是错的——用户点开下拉重新加载配置时,通道正处于 `ready`,返回的新 `modelAliases` 和刚刚修好的 `configured` **会被这条规则整包丢掉**,配置热重载在界面上等于没发生。**验收 10、11 会直接挂在这条上。**
>
> 元数据是**配置文件的纯函数**,压根没有"过期"的概念——越新越准,永远接受最新的。

**请求序号(解决"旧响应晚到覆盖新结果")**:

```tsx
const rosterSeqRef = useRef(0)
const lastAppliedSeqRef = useRef(0)

async function refreshRoster() {
  const seq = ++rosterSeqRef.current
  // ② 记下发出请求时每个 Agent 的 rev —— 快照的"保质期"基准
  const revAtRequest: Record<string, number> =
    Object.fromEntries(Object.entries(runtimesRef.current).map(([id, r]) => [id, r.rev]))

  const res = await invoke("list_agents")
  if (seq < lastAppliedSeqRef.current) return   // 已有更新的响应落地 → 整包丢弃
  lastAppliedSeqRef.current = seq
  applyMetadata(res)                        // 元数据:无条件接受(见上)
  applyRuntimeIfFresh(res, revAtRequest)    // 运行状态:通道相位 + rev 双重把关
}
```

> **为什么需要序号**:打开下拉用 `onFocus` / `onMouseDown` 作为代理信号,用户点一下可能连触发两次;两次请求并发时**先发的完全可能后返回**。没有序号,新配置会被旧响应盖回去——而且是间歇性的,极难复现。
> **序号管"两次查询谁更新",`rev` 管"查询和事件谁更新"。** 两者缺一不可:序号只比较查询之间,完全不知道期间来了什么事件。

**运行时字段的具体接受条件**(对**每个 Agent 分别**判定,按"运行身份 / 当前相位"两组分别走):

**① 运行身份(`runId` / `resolvedModel`)—— 判据是 `runId`**
- 本地**没有**该 Agent 的记录,或记录的 `runId === null` → **采纳**(用它建立记录)。这是刷新后的常规路径
- 本地 `runId` 与快照**相同** → **采纳 `resolvedModel`**(防御性的补全路径:正常情况下所有事件与 `StartAck` 都已自带模型名,见 §协议契约 A "事件自足"原则;这里再兜一次底,不为已知的缺口而设)
- 本地 `runId` **非空且与快照不同** → **整组丢弃,无条件**。**不要**在这里加任何"`rev` 未变就采纳"之类的例外——本地那条记录只可能来自本页刚发起的新一轮或刚到达的事件,**两种情况下快照都是旧的**,写回就是倒退(复现序列见上方 v12 警示)

**② 当前相位(`phase`)—— 判据是 `rev`**
- **通道是 `subscribing` / `unknown`** → 接受。此时没有活的通道,快照就是最好的信息
- **通道是 `ready`** → **不接受 `phase`**。事件流已经在喂了,别用快照盖它
- 且必须逐个 Agent 校验 **`runtimes[agentId]?.rev === revAtRequest[agentId]`**:不等就说明**这份快照发出之后、返回之前,该 Agent 的运行时记录被创建或被改写过**,快照里的相位已经过期 → **丢弃 `phase`**
  > **注意两边都可能是 `undefined`**(发出请求时本地还没有这个 Agent 的记录)。规则是"**严格相等才接受**",所以:`undefined === undefined` → 接受(期间确实什么都没发生);`1 !== undefined` → 丢弃(期间被事件建立了记录)。**别把这条写成 `?? 0` 之类的"补个默认值再比"**,那会让"期间新建了记录、`rev` 恰好是 0"被误判成"没变化"。

> **两组的判据不同,所以不能被"整组丢弃"这种写法绑在一起**——那正是 v10 丢掉 `resolvedModel` 的原因。**`phase` 过期不代表 `resolvedModel` 也过期**:后者只跟 `runId` 走。

- **查询失败**(`list_agents` 抛错 → 说明**Sidecar 层**不可用:崩溃、超时、node 缺失):若通道是 `subscribing` / `unknown` → 停在 `unknown`,显示「重新同步」,**绝不落 ready**;若通道已是 `ready`,只显示一条错误提示,**不要**把整个通道压回 `unknown`
- **`config.ok === false` 不是查询失败**:命令是成功返回的,**通道保持原样,绝不能压到 `unknown`**,各 Agent 的按钮**继续按它自己的 `phase` 决定可用性**。界面只多做一件事:显示 `config.error.message`(带行号的红字)+「重新加载配置」按钮。
  > **这一条是"配置坏了也能终止正在跑的进程"的落点**。若把 `config.ok === false` 当成查询失败去压通道,用户就会陷入:进程在跑 → 看不到 → 也终止不了。**判据很简单:能拿到 `agents` 数组,就说明运行时信息是好的,通道就不该被压掉。**
- **Sidecar 重连成功后要像挂载一样做一次完整恢复**:那是**换了一个进程**,之前的运行时状态全部作废(池是空的),必须按"通道刚就绪"的规则整包重建

**按 `AgentInfo.phase` 如实还原**(仅在允许接受运行时状态时):
- `"running"` → 前端 `running`,终止可用
- `"starting"` → 前端 `starting`,**终止禁用**(与 §关键逻辑 1.4 的 `stopAgent` 行为对齐)
- `"terminating"` → 前端 `terminating`,**终止禁用**(见 §关键逻辑 1.4 的幂等终止)
- `"idle"` → `idle`
- 写回 `runId`,这样后续该 Agent 的输出与退出事件才能匹配上

**`sidecar.version` 的用途**:**只用于显示**(诊断页里显示 Sidecar 版本),**不参与任何判断逻辑**。判断"Sidecar 是否重启过"由上面两条天然覆盖——重连会触发完整恢复,请求序号会保证新响应覆盖旧值。不要给它额外加业务含义。

**`list_agents` 的重新调用时机(配置热重载的触发点)**:除了挂载时的这一次,还要在这两处再调:
1. **用户打开 Agent 下拉时**(`onFocus` / `onMouseDown` 作为"即将打开"的代理信号)—— 这是最自然的时机,用户想启动某个 Agent 前一定会先选它
2. **任何一个 Agent 处于 `configured: false`、或界面上正显示 `configInvalid` 红字时**,在错误提示旁边显示一个「重新加载配置」文字按钮

> 这两条合起来才让"改好配置、不用重启"成立:Sidecar 侧 `listAgents` 会先 `ensureConfigFresh()`(见 §关键逻辑 1.1),前端侧必须**有办法再问一次**。缺了前端这一半,用户改完配置发现界面纹丝不动,只能重启应用——验收 9、10 都过不去。

**Sidecar 状态接入**:监听 `sidecar-status`。
- `available: false` → 把 `ChannelPhase` 压到 `unknown` 并显示「重新连接」(内部就是下次 `call` 触发自动重启,单飞机制见 §关键逻辑 2.1)。
- `jobObject: false` → 在顶部栏或运行视图显示那行**常驻的清理降级提示**(见 §关键逻辑 2.1)。它与 `available` 各自独立:Sidecar 可能好好活着,只是没被 Job 接管。

#### 3.2 界面 —— 主框架要从零搭

> ⚠️ **原计划写的"沿用阶段1 已搭好的布局骨架"不成立**。阶段1 交付的 `src/App.tsx` 是一个单页控制台,顶部是自己的 `.app-header`(应用名 + 状态),没有侧边栏、没有导航、没有底部输入框——阶段1 计划里明确定位为"临时诊断界面"、"正式布局骨架在阶段2/3 搭建"。**所以本阶段要先搭壳,再往里放组件**,并把阶段1 的整页降级为「诊断」视图。

**先定一件事:阶段2 只有一个启动入口**

原计划同时给了两个:`AgentRunner` 里的启动/终止按钮,和底部输入框的"发送"。**这是必须消掉的分叉**——阶段2 是单次启动,不是多轮对话,底部那个聊天式输入框会让人以为可以追问;而且两处都能启动时,"提示词 / Agent / 模型"这三个选择由谁管就没有答案。

按设计规范 §3.1/§3.5 的布局定义(底部输入框是 shell 的一部分,§10.1 要求"先实现布局框架"),**保留底部输入框,让它成为唯一的启动入口**:

- **输入框上方一行**放 **Agent 选择 + 模型别名选择**(规范 §3.5 原文就是"输入框上方显示当前选中的 Agent")
- **输入框本身**收任务描述;发送按钮 = 唯一的"启动"
- **主内容区只负责"看"**:实时输出 + 当前运行摘要。**不放启动按钮**
- **终止**放在每个 Agent 的状态行/输出区头部(只有正在跑的 Agent 才出现),而不是一个常驻的主按钮
- 位置:`PromptInput` 是 shell 级的,「诊断」视图里不显示(诊断页有自己的预设下拉和按钮,互不干扰)

**新建 `src/components/AppShell.tsx`**(规范 §3.1):

```
┌─────────────────────────────────────────────────────┐
│  ┌──────────┐  ┌──────────────────────────────┐    │
│  │  侧边栏   │  │  顶部栏(当前任务标题 + 操作) │    │
│  │  220px   │  ├──────────────────────────────┤    │
│  │          │  │      主内容区(最大 860px 居中) │    │
│  │  · 运行   │  │                              │    │
│  │  · 诊断   │  │                              │    │
│  │          │  ├──────────────────────────────┤    │
│  │  (底部)   │  │  底部输入框(向 Hermes 提问)  │    │
│  │ Agent 状态│  └──────────────────────────────┘    │
│  └──────────┘                                       │
└─────────────────────────────────────────────────────┘
```

- 侧边栏 `220px`,背景 `--bg-secondary`,**无边框**(靠背景色差分区,规范 §3.2);导航项「运行」「诊断」,高度 36px、圆角 `--radius-md`,选中态 `--accent-subtle` 背景 + `--accent` 文字;底部放 `AgentStatusList`
- 顶部栏高 `52px`,背景 `--bg-primary`,底部 `1px solid --border-light`
- 主内容区背景 `--bg-primary`,内容最大宽度 `860px` 居中,上下 `--space-6`
- 底部输入框固定,背景 `--bg-primary`,顶部 `1px solid --border-light`,最小高 `64px`
- **窗口宽度 < 1100px 时侧边栏自动折叠为 56px 图标模式**(规范 §7);最小窗口 900×600,验收要在这个尺寸下确认无横向溢出
- **视图切换用条件渲染,不是 `display:none`**——切走时组件要真的卸载,这样阶段1 诊断控制台的 `listen()` 会被清理,不会在后台继续收事件

主内容区两个视图:

- **运行视图**——**只负责"看",不放启动按钮**(唯一入口是底部输入框,见上)。内容按"一屏只做一件事"和"留白优先"(规范 §1)精简为三层:
  1. **运行状态行**:当前 Agent 名 + 模型别名 → 实际模型 + 状态圆点 + 耗时;**正在跑时**这一行右侧出现「终止」
  2. **`OutputView`**:实时输出,占据剩余高度的主体
  3. **注入摘要(折叠)**:默认只显示一行摘要——`模型 glm-4-flash · Base URL http://127.0.0.1:8787 · 密钥 已设置`。点「详情」展开成完整的 `injectedEnv` 键值表
  > **为什么折叠**:原计划把完整环境变量表、密钥开关、启动按钮、终止按钮、两个下拉全堆在主内容区,这直接违反规范 §1 的"内容聚焦"和"留白优先"。而且真正需要逐项比对的场合是**配置调好之后的一次性确认**,不是每次启动都要看。摘要 + 按需展开既满足验收的"肉眼比对入口",又让日常使用保持干净。
  > 需要逐项对照时,「诊断」页的 `env-dump.js` 原始输出也是现成的对照物。
  - `list_agents` 返回前用**骨架屏**(规范 §4.6),不用转圈
  - 配置有问题的 Agent:**在下拉里置灰,并在其下方显示一行 `reason`**(见 §关键逻辑 1.1 的两级校验)
- **诊断视图**:阶段1 的控制台原样搬进来,只改事件名。保留理由:终验证要用「Node 快速退出」预设复现秒退竞态;那套进程管理代码也是阶段5 Worktree 的基础,删掉可惜

公共组件:

- **`PromptInput`**(底部输入框,规范 §3.5):上方一行是 Agent 选择 + 模型别名选择(默认 Hermes),下面是输入框。输入框圆角 `--radius-lg`、背景 `--bg-tertiary`、**无边框**,聚焦时加 `1px solid --accent`;右侧圆形强调色发送按钮;`Enter` 发送 / `Shift+Enter` 换行;输入为空或通道未就绪时发送禁用。**这个发送按钮就是阶段2 唯一的启动入口**,不做多轮对话(那是阶段3)
  > **不放附件按钮**:设计规范 §3.5 提到它,但本阶段没有任何附件功能。放一个点了没反应的按钮,既违反"禁止未实现按钮",也让这一行显得拥挤(规范 §1「内容聚焦」)。**有功能了再加。**
- **`AgentStatusList`**(侧边栏底部,规范 §4.3):状态圆点 6px + 名称 + 状态文字;工作中时名称后显示当前模型(`--text-tiny`、`--text-tertiary`);出错时整行可点击展开错误详情
- **`OutputView`**:等宽字体、`--bg-tertiary`、`--radius-md`;每行前缀 `[agentId]`;自动滚动到底,用户上滚后暂停并显示「回到最新」(规范 §5.4)
- 错误用红字不弹窗(规范 §5.3);空状态用线性图标 + 一句话,不画插画(规范 §4.5)
- 图标统一 `lucide-react`,尺寸 16/20/24(规范 §8)

**样式**:全部引用 `src/index.css` 已定义的变量,不硬编码;深色模式跟随系统;动效遵守 `prefers-reduced-motion`。**`src/App.css` 在阶段1 已删除,不要新建它**。

---

## 实施步骤

1. **共享协议**:`shared/protocol.ts`(先定契约,后面所有代码对着它写);根 `tsconfig.json` 的 `include` 加上 `shared`
2. **Sidecar**:`sidecar/` 骨架 → `types.ts` → `config.ts`(json5 + 两级校验 + 路径规则 + `start_delay_ms`) → `model.ts`(别名解析 + 密钥只回 `{set,length}`) → `agent-process.ts`(Buffer 两级解码) → `pool.ts`(**同步占坑** + 退出清理 + **幂等终止** + **配置∪池合并列表**) → `rpc.ts`(`error.data.kind`) → `index.ts`
3. **配置与脚本**:`config/agents.json`、`scripts/env-dump.js`(常驻心跳 + `--once` + 密钥只报"已设置/长度")
4. **构建接线**:`package.json` 加脚本 + devDeps;`tauri.conf.json` 补 resources 映射与 `beforeDevCommand` / `beforeBuildCommand`;`.gitignore`

   ```jsonc
   // package.json —— build:sidecar 把类型检查串在前面,不给出绕过的路径
   "typecheck:sidecar": "tsc -p sidecar/tsconfig.json --noEmit",
   "build:sidecar": "npm run typecheck:sidecar && esbuild sidecar/src/index.ts --bundle --platform=node --format=cjs --target=node20 --outfile=sidecar/dist/sidecar.cjs"
   ```

   > ⚠️ **原计划把 `typecheck:sidecar` 单独列一条,却没有接进任何构建链**。esbuild **只转译、不做类型检查**,所以 `beforeDevCommand` / `beforeBuildCommand` 都会在类型错误的情况下"构建成功"——协议字段写错、`kind` 拼错(正是 finding 2 要防的东西)、参数个数对不上,全都要等到运行时才炸。
   > 把 typecheck 写进 `build:sidecar` 内部而不是并列一条,**任何构建路径都绕不过它**:`tauri dev`、`tauri build`、开发者手敲,走的都是同一条命令。
5. **Rust**:`Cargo.toml` 改名 + 加 `windows-sys` → `sidecar/protocol.rs`(含 `error.data.kind` → `CmdError.kind` 的映射) → `sidecar/job.rs`(`JobHandle`:创建 / 指派 / `TerminateJobObject` / drop 即关闭 / **暴露是否指派成功**,非 Windows 平台空实现) → `sidecar/mod.rs`(含 `ensure_started` 的**单飞双重检查**) → `lib.rs` 挂载与注册(含把两处 emit 改名 `diagnostic-*`) → `main.rs`
6. **前端**:`lib/errors.ts` → `hooks/useSidecar.ts`(`runtimes` **state + ref 双份** + `patchRuntime()` + 按 agentId 拆分的 `opToken` 与 **`rev`** + **`refreshRoster()` 的元数据/运行时分组接受、`rosterSeq` 与 `rev` 双重把关**) → `AppShell` + `AgentStatusList` + `RunView` + `OutputView` + `PromptInput` → `App.tsx`(诊断控制台搬进「诊断」视图) → `src/index.css` 追加样式
7. **清理阶段1 遗留**:`index.html` 的 `<title>`、favicon
8. **README** 补配置说明
9. **自测**:`npm run build:sidecar` 通过(已内含类型检查)→ `cargo check` 通过(`cargo fmt --all -- --check` 干净)→ `npx tsc --noEmit` 通过 → `npm run build` 通过 → `npm run tauri dev`

> 第 1 步先写协议类型不是形式主义:`shared/protocol.ts` 是 TS 侧唯一的契约源,而 Rust 侧的 `serde` 结构只能靠人肉同步,越早定稿越不容易返工。
>
> 第 5 步的 `job.rs` 建议**先写一个最小可跑版本再往下接**:`CreateJobObjectW` → 设 `KILL_ON_JOB_CLOSE` → `AssignProcessToJobObject`,用一个 `node -e "setTimeout(()=>{},10000)"` 的子进程验证"杀 Rust 进程后它也消失",确认通了再嵌进 `SidecarClient`。Job Object 的坑基本都在句柄生命周期和 `set_information` 的结构体布局上,单独验比混在 RPC 代码里排查快得多。**顺手确认这台机器上 `AssignProcessToJobObject` 到底成不成功**——这直接决定验收 27/31/32 能不能按判据执行。
>
> **并发保护要在同一个阶段内一起做完,不要留到"以后再补"**:Sidecar 池的"同步检查 + 同步占坑"(§1.4)、`ensure_started` 的单飞双重检查(§2.1)、前端 `patchRuntime` 先写 ref 再 setState(§3.1)、`refreshRoster` 的请求序号(§3.1)。它们的共同点是两句话——**"检查"和"生效"之间不能让出执行权**(前者),**"旧结果"不能覆盖"新结果"**(后者)。任何一处漏掉,表现都是"偶发、难复现、看起来像玄学"的那类 bug。

---

## 与阶段1 的衔接

阶段1 由另一个 agent 实现并已通过验收,下表是**核对后的事实与处理方式**(不是"若不符如何处理"的猜测):

| 检查点 | 实际情况 | 处理 |
|---|---|---|
| `CmdError` 是否已存在 | ✅ 已存在(`lib.rs:23-37`,`{kind, message}` + camelCase) | 直接复用,只补 `kind` 取值 |
| `bundle.resources` 是否已有 `test-agent.js` 映射 | ✅ 已是映射写法 `{"../scripts/test-agent.js": "scripts/test-agent.js"}` | 追加 sidecar / config 两项,**映射写法照抄,不改动已有项** |
| 前端是否已有 `mountGenRef` / `opRef` | ✅ 已有(`App.tsx:115-136`,且经验收实测有效) | `mountGenRef` 原样沿用;**`opRef` 必须按 agentId 拆分**,不能整体照抄(见 §前端 3.1) |
| `lib.rs` 是否已模块化 | ❌ 单文件 400 余行 | 新增 `sidecar/` 子模块并 `mod sidecar;`,不拆分原文件 |
| 事件名是否已占用 `agent-output` / `agent-exit` | ⚠️ 已占用,且诊断控制台会在 `unknown`/`subscribing` 相位**接受任意 runId 的退出事件**(`App.tsx:195-206`) | **把阶段1 的两个事件改名为 `diagnostic-output` / `diagnostic-exit`**(4 处字符串),阶段2 自己用 `agent-output` / `agent-exit`。详见 §协议契约 C |
| `src/App.css` | ❌ 阶段1 已删除,当前不存在 | 样式追加到 `src/index.css`,**不要新建 App.css** |
| `Cargo.toml` 包名 | ❌ 仍是 `agenthub-tmp`(验收报告 §7.1 已记录) | 本阶段改为 `agenthub-desktop` / `agenthub_desktop_lib`,顺手清理 |
| `index.html` 的 `<title>` | ❌ 仍是模板值 `Tauri + React + Typescript`(验收报告 §7.2) | 本阶段顺手改掉 |
| 项目是否是 git 仓库 | ❌ 不是(`git rev-parse` 报错),`.gitignore` 未生效 | 阶段2 不受影响;阶段5 用 Worktree 前需先 `git init` |

**需要改到阶段1 代码的地方**(全部是加法或字符串改名,不动逻辑):
1. `src-tauri/src/lib.rs` 加 `mod sidecar;`、状态注册、三个新命令、`RunEvent::Exit` 追加清理
2. `src-tauri/src/lib.rs` 两处 `emit` 事件名改 `diagnostic-*`(`lib.rs:202`、`lib.rs:270`)
3. `src/App.tsx` 两处 `listen` 事件名改 `diagnostic-*`(`App.tsx:191`、`App.tsx:195`),并把整页降级为「诊断」视图

---

## 验收(对照任务书)

### A. 配置生效(阶段2 核心)

1. **Sidecar 独立自测**(不起界面,直接验证协议)。用 `node -e` 一行式,cmd 和 bash 都能跑(原计划的 `echo '{...}' | node ...` 是 bash 语法,Windows cmd 下不成立):

   ```bash
   node -e "const{spawn}=require('child_process');const p=spawn(process.execPath,['sidecar/dist/sidecar.cjs','--config','config/agents.json'],{stdio:['pipe','inherit','pipe']});p.stdin.end(JSON.stringify({jsonrpc:'2.0',id:1,method:'ping',params:{}})+'\n')"
   ```

   应输出一行 JSON 响应 `{"jsonrpc":"2.0","id":1,"result":{"ok":true,"version":"..."}}`,进程随后因 stdin EOF 自行退出(这条同时验证了退出兜底)。
2. 启动 Pi:运行状态行显示 `Pi · 快 → sonnet`;展开注入详情,`ANTHROPIC_BASE_URL = http://127.0.0.1:8787`、`ANTHROPIC_MODEL = sonnet`,而 `ANTHROPIC_AUTH_TOKEN` 显示为 **`已设置(共 N 位)`**;输出区出现 `env-dump.js` 打印的变量清单。**非密钥项两处逐项一致,密钥项两处都只显示"已设置(共 N 位)"**——注入详情与输出区**都不出现密钥原文**(验收 36 是这条的完整版)。
3. **改配置立即生效**:把 `config/agents.json` 里 `model_aliases.fast.pi` 从 `haiku` 改成 `glm-4-flash`,**不重启应用**,再次启动 Pi 并选"快" → `resolvedModel` 与 `ANTHROPIC_MODEL` 都变成 `glm-4-flash`。(任务书硬性验收标准)
4. 改 `model_aliases.powerful.claude`,启动 Claude → 环境变量随之改变。
5. **改注入模板也生效**:把 `env_injection.pi` 的 `ANTHROPIC_MODEL` 改成 `ANTHROPIC_MODEL_OVERRIDE`,重启 Pi → 注入详情里键名跟着变。**这条验证的是"模板本身可配置",原计划只验了别名,漏了模板**。
6. **改坏配置有友好提示**:删掉一个 `}`,再启动 Agent → 红字显示带行号的语法错误,不崩溃、不白屏。改回后自动恢复(无需重启)。
7. **`http://` 不被注释吞掉**:确认 `cc_switch.base_url` 解析结果原样是 `http://127.0.0.1:8787`(不是 `http:`),**同时确认配置文件里写的 `//` 注释确实被正确忽略**(json5 解析成立)。
8. 未设置 `CC_SWITCH_API_KEY` 时:注入详情里该变量显示"未设置"并标黄,**但 Agent 仍能启动**。
9. **单个 Agent 配置坏了不牵连其它 Agent**(两级校验的验证点):把 `agents.codex.default_model` 从 `balanced` 改成 `balanced-typo`(一个不存在的别名)→ 重启应用后,Codex 在下拉里**置灰并显示原因**,而 Pi / Claude / Hermes **照常可用、照常能启动**。(原计划所有校验都是致命的,这条根本无法通过)
   > 原计划这条用的是"把 `args` 改成引用一个不存在的占位符"——**`args` 从来就没有占位符展开这个功能**(占位符只存在于 `env_injection` 的模板里,见 §关键逻辑 1.1),照原样写这条验收根本没法执行。改用 `default_model` 落到一个不存在的别名上,这是**已经明确定义的**单 Agent 级错误。
10. **改回配置无需重启**(配置热重载的关键验证点,也是 §关键逻辑 1.1 那条死角的验证):接上一条,把 `default_model` 改回 `balanced` → **不重启应用**,点开 Agent 下拉(或点错误提示旁的「重新加载配置」)→ Codex **立刻恢复可用**。
    > 这条与第 9 条必须**成对执行**:只有第 9 条会漏掉"改回去也能恢复"这一半,而那正是 `listAgents` 漏调 `ensureConfigFresh()` 时会挂掉的地方——**用户改错了配置、改回来、界面纹丝不动,只能重启应用。**
11. **可选别名清单来自配置,不是前端写死的**:在 `model_aliases` 里新增一档 `"cheap": { "pi": "glm-4-flash" }` → 点开 Agent 下拉 → Pi 的模型下拉里**多出"cheap"这一项**,而 Claude / Codex 的下拉里**不出现**它(它们没配这档)。然后把 `model_aliases.fast.pi` 删掉 → Pi 的下拉里"fast"消失,`default_model` 若正好是 `fast` 则该 Agent 置灰。
    > 同样**不需要重启应用**(走的是同一套 `listAgents` → `ensureConfigFresh()` 路径)。

### B. 进程与状态

12. Agent 列表加载时显示骨架屏;加载完成后发送按钮可用。
13. 运行中发送按钮为禁用态;**快速连续点击发送 3 次**(真实鼠标,间隔约 45ms)只会有一个进程,且不出现虚假的 `alreadyRunning`。参考阶段1 的实测手法(`PROCCOUNT` 按命令行筛选 + `wmic`/UIA 读控件 `IsEnabled`)。
    > 注意这条**必须在 Sidecar 层也成立**——前端的按钮禁用只挡得住同一个渲染进程,而 Sidecar 是独立进程。真正的保护是 §关键逻辑 1.4 的"同步检查 + 同步占坑"。验证手法:用 `node -e` 直接对 stdin 连发两条 `startAgent`(同一 agentId、不同 runId),确认**第二条必须返回 `alreadyRunning`**,且进程只有一个。
14. 点击终止:立即停止,灰色"已终止";连续快速点两次终止,**最终仍是灰色"已终止",不出现红色失败**。
15. **运行中刷新页面**:① 恢复为"运行中",输出区为空(预期),注入详情区显示"重新启动后可见";② `AgentStatusList` 里那个 Agent 显示"工作中"而非"空闲";③ 刷新后什么都不做等它自然结束,状态必须变绿/灰,**不能永久卡"运行中"**。(对应 §前端 3.1 的刷新恢复)
16. **在 `starting` 期间刷新页面**(§关键逻辑 1.4 那条"`stopAgent` 对 `starting` 返回 `stopped:false`"的验证点):
    **前置**:给这个 Agent 的配置加上 `"start_delay_ms": 3000`(见 §关键逻辑 1.4 —— `starting` 通常只持续几毫秒,不拉长的话这条验收**没法稳定复现**;原计划把"按 F5"当作随手可做的事,是不成立的)。
    ① 启动该 Agent,趁界面显示"正在启动"时按 F5 → 刷新后必须**仍然显示"正在启动"**,**终止入口保持禁用**(不能变成"运行中"把终止按钮放出来——那个按钮点下去只会得到 `stopped: false`,用户会以为坏了);
    ② **另起一轮**,把 `command` 改成 `node-not-exist` 并同样设 `start_delay_ms: 3000`,重复 ① 的操作 → 界面必须**自行收敛到失败终态**,不能永久停在"正在启动"或"运行中"。
    > 这是"占位记录被移除时必发终态通知"这条不变式的验证(见 §关键逻辑 1.4)——刷新已经销毁了原来那个 `invoke()`,**RPC 错误没有接收者了,只有广播的通知能把状态收回来。**
    > **人工卡时序只是顺带观察,不是唯一判据**:②在协议层可以直接断言"`agent/error` 通知确实被发出",完全不需要抢那 3 秒——做法与验收 1 的 `node -e` 自测同类,只是把断言换成"观察 stderr 上流出的通知行"。这条即使人工复现不了也能判定。
17. **列表随事件实时更新**(state 双份的验证点):Claude 运行期间,侧边栏 `AgentStatusList` 的状态圆点与运行状态行**必须跟着变**(工作中 → 已完成),不能只在刷新后才对。
18. **输出不重复**:刷新后继续接收的输出,每行恰好出现一次(行数可精确核对,不依赖控制台)。**这是 StrictMode 监听器残留的主判据。**
19. **并发·状态互不干扰**(见 §前端 3.1):同时启动 Pi 和 Claude → 两个都在跑;**趁 Claude 运行期间启动 Pi,并让 Claude 在 Pi 的启动请求在途时自然结束** → Pi 必须正常进入"运行中",**不得卡在"正在启动"**。反向同理:终止 Pi 的同时让 Claude 退出,Pi 必须正常落到"已终止"。
    > 这是 finding「全局 `opRef` 撑不住并发」的针对性验证。用「Node 快速退出」预设(约 0.5 秒退出)当那个"搅局者"最容易命中——它的退出窗口足够短,能稳定插进另一个 Agent 的在途请求里。
20. **首行输出不丢**(先写 runId 再 invoke 的验证点):启动一个"第一行就输出"的 Agent(用「Node 快速退出」预设最直接),输出区**必须能看到那一行**。
21. **自然退出后立即重启**:Agent 自然结束、界面刚变绿时,**立刻**再次启动同一个 Agent → 成功,不出现虚假的"已有实例运行中"。(针对 finding「池记录没删」;阶段1 验收 §四.4 测过同一条)
22. **自然退出后 `listAgents` 必须如实**:Pi 自然结束后,切换视图再切回 / 或按 F5 → `AgentStatusList` 里 Pi 必须是"空闲",**不能一直显示"工作中"**。(同上)
23. 运行中启动同一个 Agent → 提示已有实例在跑并转入"状态未知 + 重新同步",不清空状态、不误报空闲。
24. **在 `terminating` 期间刷新页面 + 终止的幂等性**(§关键逻辑 1.4 的 `killed === true` 分支、§协议契约 A 的 `terminating` 相位):
    ① 启动 Pi → 点终止 → **立刻按 F5** → 刷新后该 Agent 必须显示**"正在终止"**,**终止入口保持禁用**;稍后自然收敛到灰色"已终止";
    ② **对已经处于终止中的实例再发一次终止**(直接在协议层连发两条 `stopAgent`、用同一 `runId`,最能稳定命中)→ **第二次必须返回 `{ stopped: true }`,不得返回 `stopFailed`,界面也不得翻红**。
    > 第二次调用时进程可能已经死了,`child.kill()` 会抛错——**幂等的判据是"用户的意图已经达成",不是"这次信号有没有真的发出去"**。阶段1 的 `kill_test_agent` 就有这个分支,阶段2 不能丢。
25. **`ready` 状态下重新加载配置,元数据必须生效**(§前端 3.1 那组"元数据 vs 运行时"接受规则的验证点):
    在**一个 Agent 正在正常运行**、通道处于 `ready` 的情况下,去改 `model_aliases`(验收 11 的做法)→ 点开 Agent 下拉 → **新的别名清单必须出现**。
    > 只按"`subscribing` / `unknown` 才接受快照"的旧规则,这次响应的**相位段被丢弃是对的**(通道活着,事件更准;同轮 `resolvedModel` 仍按 `runId` 匹配补全),但**元数据段也被一起丢掉是错的**——热重载在界面上就等于没发生。这条专门卡住那个 bug。
26. **配置里删掉一个正在运行的 Agent**(§关键逻辑 1.4 "配置里被删掉的运行中实例"的验证点):
    启动 Pi → 在 `config/agents.json` 里**删掉整个 `pi` 条目** → 点开下拉 → Pi **必须仍然出现在列表里**,显示"工作中",并带一行原因说明("已从配置中移除,但这个实例仍在运行")→ **终止入口可用**,点它能把进程停掉 → 停掉之后它**自行从列表消失**。
    > 只遍历配置的旧写法会让它**凭空消失、进程还在后台跑、界面再也无法终止它**,只能去任务管理器。这条与验收 22(自然退出后如实)是一对。

### C. 异常与清理

27. **杀掉 Sidecar 验证自愈 + 孤儿清理**:先启动 Pi 并确认它在跑(任务管理器能看到其 `node` 子进程)→ 结束 `node sidecar.cjs` → 界面出现"Sidecar 不可用" → **等 2~3 秒,确认那个 Agent 的 `node` 进程也一并消失**(Job Object 的验证点)→ 再点启动 → 自动重启并成功启动 Agent。
    > 原计划只要求"能重连并启动新 Agent",完全没查旧进程的下场——照原样验证,留下一个孤儿进程也能让这一项"通过"。
    > **前置条件**:先确认界面上的 `jobObject` 为 `true`。若为 `false`,按 §关键逻辑 2.1 的规定记为"未验证—环境限制",**不判通过**。
28. **`jobObject` 状态在刷新后不丢**(§协议契约 B 那条"查询负责现状"的验证点):确认界面上出现清理降级提示(或确认它是 `true`)→ **按 F5** → 提示**必须仍然在**,不能因为刷新就消失。
    > 只靠 `sidecar-status` 事件时这条必然失败:Sidecar 已经活着,`list_agents` 不会重新 spawn 它,也就不会有新事件。**这正是它必须挂到 `list_agents` 返回值上的原因。**
29. **重启单飞**:Sidecar 崩掉后**同时**让两个 Agent 各自发一次请求(例如同时点两次不同 Agent 的启动)→ 任务管理器里 `node sidecar.cjs` **只能有一个**,且两个 Agent 都能正常启动。(单飞机制的验证点)
30. 把配置里某 Agent 的 `command` 改成 `node-not-exist` → 红字显示原始 spawn 错误。
31. **关闭窗口后任务管理器无残留**:打开应用即可(sidecar 已随界面挂载启动,不必手动启动 Agent)→ 关闭窗口 → `node`(sidecar)消失;再完整跑一次并启动 Agent,关闭窗口后 sidecar 与 Agent 子进程全部消失。
32. **强杀应用进程**(任务管理器直接结束 `agenthub-desktop.exe`,不走正常关窗):残留的 `node` 必须一并消失(Job Object 内核兜底)。**同样以 `jobObject === true` 为前置**。
33. 关闭窗口时若 Agent 正在运行:同样无残留。
34. 深色模式下输出区/边框/状态色无亮白断层;900×600 窗口无横向溢出、无控件被挤出(照阶段1 验收 §8.1 的边界检查做法)。
35. **导航切走不残留监听**:在「运行」视图启动 Agent → 切到「诊断」→ 再切回,输出**不出现重复行**,且诊断页的状态不被运行页的事件干扰。
36. **密钥不出现在界面上**:启动一个注入了 `ANTHROPIC_AUTH_TOKEN` 的 Agent → 注入详情里显示 `已设置(共 N 位)`;**输出区的 `env-dump.js` 打印里该变量也必须是 `已设置(共 N 位)` 而不是明文**;全屏截图后肉眼搜不到密钥原文。
    > **这条验的是"阶段2 的 `env-dump.js` 不输出密钥",不是"`agent/output` 通道被脱敏"**——Sidecar 对 Agent 输出是原样转发的(见 §关键逻辑 1.2)。阶段2 所有 Agent 都指向 `env-dump.js`,所以这条能通过;阶段3 换成真实 CLI 后需要重新评估。
37. **配置整体写坏时,运行中的实例仍可操作**(§关键逻辑 1.1 "致命错误不让 `listAgents` 失败"的验证点):
    启动 Pi 并确认它在跑 → 把 `config/agents.json` 改成一个**整体语法错误**(删掉一个 `}`)→ **按 F5** → 必须同时满足:
    ① 界面显示带行号的配置错误红字;
    ② **Pi 仍然出现在列表里、显示"工作中"**;
    ③ **终止入口可用**,点它能真的把进程停掉;
    ④ **通道没有被压成"状态未知"**(其它 Agent 的启动入口不该因为配置坏了就全部禁用——它们本来也启动不了,但那是各自 `configured: false` 的结果,不是"通道挂了")。
    > 原方案下这四条全都反过来:**列表空白、按钮全灰、进程还在跑**,用户只能先修配置或去任务管理器。而本计划另一条铁律是"重载不清掉运行中的记录"——两者直接矛盾,这条验收就是那个矛盾的照妖镜。
38. **终止超时回滚后,刷新过的页面能把状态收回来**(§协议契约 A 的 `agent/state`、§关键逻辑 1.4 的 3 秒超时回滚):
    **前置**:给这个 Agent 的配置加上 `"stop_delay_ms": 5000`(**配上默认的 3 秒超时,就能稳定制造一次超时回滚,而且进程全程活着**——见 §关键逻辑 1.4.1)。
    ① 启动该 Agent → 点终止 → 界面显示"正在终止"时 **按 F5**;
    ② **3 秒后** Sidecar 把 `killed` 回滚 → **刷新后的页面必须自己从"正在终止"退回到"工作中",终止入口重新可用**,不得永久卡在"正在终止";
    ③ **回滚之后继续等,等过了 `stop_delay_ms`(本例再等 3 秒以上)** → **进程必须仍然活着**(任务管理器里那个 `node` 还在、界面仍是"工作中")。
    > ③ 是这条验收里**最容易漏掉、也最致命**的一半:延时 5 秒的那个回调醒来时,世界已经变成"已回滚、正在运行"。若不校验就 kill,**用户什么都没做,进程却突然消失**。见 §关键逻辑 1.4.1 的"延时回调必须重新校验"。
    > 顺带把这条也验了:**等过 `stop_delay_ms` 之后重新点一次终止,必须能正常终止**(`termSeq` 生效,新的那次请求自己会处理)。
    > 回滚发生的那一刻,**发起终止的那个 promise 早就随 F5 销毁了**——只有 `agent/state { phase: "running" }` 这条广播能把新页面拽出来。只改 `killed` 而不广播,等于只修了"原页面"那一半。
    > **不要用"让脚本忽略终止信号"来制造超时**:Node 在 Windows 上的 `child.kill()` 走 `TerminateProcess`,**不可被目标进程拦截**,那个做法在 Windows 上不可靠。也不要靠"临时把超时改小"——那要改源码,不是稳定的验收手段。**`stop_delay_ms` 就是为这条验收准备的。**
    ④ **协议层的等价判据(不依赖人为卡时序)**:直接对 Sidecar 连发 `startAgent` / `stopAgent` 并观察 stderr 上流出的通知行,断言 `agent/state phase=terminating` 与随后的 `agent/state phase=running` **都被发出**。
39. **旧快照不能盖掉新的广播**(§前端 3.1 那个 `rev` 的验证点):
    **反复刷新页面(建议 10 次以上)**,每次刷新时都让某个 Agent 处于启动过程中 → 刷新后该 Agent 最终**必须收敛到"工作中"**,不允许出现"进程在跑、界面却停在『正在启动』、终止按钮灰着"。
    > `list_agents` 的快照在发出时该 Agent 还是 `starting`,而 `agent/state { phase: "running" }` 可能先于快照返回。只校验 `opToken` 时(而 `agent/state` 不动 `opToken`)**这条竞态必然发生,快照会把 `running` 盖回 `starting`**。`rev` 就是判据:快照只在该 Agent 的 `rev` 没被动过时才有权写运行时字段。
    > 与验收 40 同样是时序竞态,**单次通过不算过**,必须多跑。
40. **注入详情不因广播而丢失**(§前端 3.1 "`agent/state` 不递增 opToken" 的验证点):
    **连续启动同一个 Agent 若干次(建议 10 次,因为这是时序竞态,单次看不出来)**,每次都展开注入详情 → **每一次都必须有内容**(模型名、Base URL、密钥"已设置(共 N 位)"),不允许出现空白。
    > `spawn` 成功后 Sidecar **先发 `agent/state { phase: "running" }`、再返回 `StartAck`**。如果那条广播递增了 `opToken`,紧接着返回的 `StartAck` 就会被判为"令牌已作废"而丢弃 → `injectedEnv` / `cwd` 没被保存 → **注入详情间歇性空白**。**竞态方向取决于广播与 RPC 响应谁先到,所以必须多跑几次才现形**,一次通过不能算过。
41. **刷新窗口内的事件不被丢弃**(§前端 3.1 "事件 `runId` 归属判定"的验证点,也是 `rev` 的最后一块拼图):
    让某个 Agent 处于**长时间运行**状态(它每秒都有心跳输出;必要时配 `stop_delay_ms` 让终止动作也落在窗口内)→ **反复按 F5** → 每次刷新后必须满足:
    ① 该 Agent **收敛到"工作中"**并带正确的 `runId`;
    ② **侧边栏里它后面必须显示当前模型**(如"Pi · sonnet")——**不允许出现"显示工作中、却报不出模型"**;
    ③ 若在这段窗口里刚好赶上它退出(`agent/exit`)或终止回滚(`agent/state { phase: "running" }`),**状态必须正确反映那个事件**,且**退出那条记录也要带模型名**(v12 起 `agent/exit` / `agent/error` 自带 `resolvedModel`),不能被随后返回的旧快照覆盖回刷新前的样子;
    ④ **绝不能出现"旧快照把状态改回 `starting` / `terminating`"** 这种倒退。
    > ② 是 v11 加的、v12 补全的:刷新期间若只有某一条事件先到、快照随后被 `rev` 判为过期丢弃,记录里就会缺模型名。修法是**事件自足 + 快照兜底**双保险——`agent/state` / `agent/exit` / `agent/error` 都自带 `resolvedModel`(v11 只给 `agent/state` 带了,漏了两个终态事件),同时快照的 `resolvedModel` 按 `runId` 归属接受(补全)。**任一条路生效都该看到模型**,同时失效才会复现。
    > 这是"刷新后、`list_agents` 返回前事件先到"的针对性验收:此时新页面**没有任何 `runId` 记录**,严格按"runId 必须一致"会**把事件丢掉**,而旧快照因为本地也没有 `rev` 可比而照样被接受——`rev` 在这条路径上一点忙都帮不上。修法是**先用事件里的 `runId` 建立记录**(见 §前端 3.1 的三行判定表),让快照随后的相位段因 `rev` 不等而被正确丢弃。
    > **与验收 39/40 同样是时序竞态,必须多跑几次**。想稳定复现的话,把 `list_agents` 的响应人为拖慢一点(例如临时在 Sidecar 的 `listAgents` 里加个几百毫秒的延时)最容易命中——**窗口就是这个 RPC 的往返时间**。
42. **在途快照不能把新一轮写回旧一轮**(§前端 3.1 快照规则表 v12 警示的验证点):
    ① 启动 Pi(第 1 轮)并确认运行中;② **临时拖慢 `list_agents`**(同上),终止 Pi,等它确认结束后**立刻启动第 2 轮**,同时保证有一个拖慢的快照请求在第 2 轮写入本地 `runId` **之前**发出、**之后**返回;③ 结果必须仍是**第 2 轮**:`runId` 是新的、模型名正确、注入详情(若可见)对应第 2 轮——**绝不能被带回第 1 轮**;④ 之后按正常流程终止第 2 轮,必须成功。
    > v11 的规则"本地 `runId` 不同、`rev` 未变时仍采纳快照运行身份"在这条序列里会把旧轮次的 `runId` 与旧模型名写回来,随后第 2 轮的 `StartAck` / 事件因 `runId` 不匹配被丢弃,整个第 2 轮悬空。**修法是把那个例外删掉**(不同就无条件丢),并用 `patchRuntime()` 的默认 `rev + 1` 把本地操作也纳入"逢写递增"。
    > 本条比 41 更考验实现纪律,**若嫌人工难凑,可降级为协议层 + 代码评审项**:协议层验证不了前端规则,那就以"代码评审确认 `runId` 不同 → 无条件丢弃、无例外分支"为主,人工实验为辅——**如实记录用了哪种手段**,不猜通过。

### D. "控制台无 React 警告" —— 直接删掉,不做

阶段1 的验收报告已经**实测证明**这一条在当前构建下无法执行(§8 未验证项 4):F12 与 Ctrl+Shift+I **都打不开 DevTools**;`WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9222` 被 Tauri 自身的 `additionalBrowserArgs` 覆盖,9222 端口未监听。

**处理:删掉这个验收点,不引入任何调试端口参数**(v2 修订里"加 dev-only `additionalBrowserArgs`"的想法作废):

- 要按环境隔离 `additionalBrowserArgs`,得额外做 `--config` 覆盖文件 + 专门的 dev 脚本,而 v2 只写了一句"要隔离"、**没给具体做法**——照做很可能把调试端口带进正式包。为一条锦上添花的检查冒这个险,不划算。
- **真正要验的东西不依赖控制台**:"StrictMode 双挂载有没有造成监听器残留"的判据本来就是**输出行数精确、每行只出现一次**(验收 18),可完全机测,阶段1 也正是用它确证的(31 = 29 + 2)。控制台只是佐证。
- 这也符合"简约大气":不加没必要的构建分支和配置面。

若将来某次排查确实需要 CDP,再用 Tauri 的 dev 专属配置覆盖(`tauri dev --config`)临时打开,用完即撤,不写进本阶段计划。

---

## 风险与对策

| 风险 | 对策 |
|---|---|
| 项目路径含空格导致命令拼接错误 | 全部用参数数组传递,禁止拼字符串命令行 |
| Sidecar 里 `console.log` 污染协议流 | 入口顶部重定向到 stderr;`rpc.ts` 是唯一写 stdout 的地方 |
| 请求/响应配对与读线程争锁死锁 | 锁只覆盖"准备+写入",`recv_timeout` 一律在锁外 |
| **`child` / `stdin` 裸在结构体里,`ensure_started(&self)` 换不掉它们** | 本代进程状态整体关进 `SidecarProc` + 一把 `proc` 锁;`spawn_locked` 备齐材料后**一次性替换**。锁序:`start_lock → proc` 是唯一允许的嵌套,`pending` 与两者互斥 |
| **配置元数据被运行时快照的接受规则连坐丢弃** | 响应拆成三组分别定规则:元数据**永远接受最新的**(按 `rosterSeq` 防乱序);**运行身份**(`runId`/`resolvedModel`)按 `runId` 归属;**当前相位**(`phase`)按 `rev`。验收 25 专门验它 |
| **旧响应晚到覆盖新配置** | `rosterSeqRef` / `lastAppliedSeqRef`,序号更小的响应整包丢弃。下拉用 `onFocus`/`onMouseDown` 可能连触发两次,没有序号就是间歇性 bug |
| **F5 落在 `terminating` 期间,终止按钮又被放出来** | `AgentInfo.phase` 增加 `terminating`;`listAgents` 里 **`killed` 必须先判** |
| **重复终止返回失败、界面翻红** | `killed === true` → 直接 `{ stopped: true }`,不再调 `child.kill()`。幂等判据是"用户意图已达成",不是"信号有没有发出去"。验收 24 ② |
| **终止超时后记录永远卡在"终止中"** | 3 秒超时**回滚 `killed = false`** → 相位退回 `running` → 用户可以再试一次 |
| **回滚只通知到"原页面",刷新过的页面卡在"正在终止"** | 新增 `agent/state` 广播,超时回滚时发 `phase: "running"`。凡"页面可能在等待"的相位变化都要广播。验收 38 专门验它 |
| **`stopped: true` 被误当成"已终止"** | 它只是"请求已受理";前端收到后只推进到 `terminating`,等 `agent/exit` 或 `agent/state` 定终局 |
| **配置写坏 → `listAgents` 整体失败 → 运行中的实例看不见也杀不掉** | `list_agents` **永远返回 `Ok`**,配置问题放 `config.ok`/`config.error`;前端**不压通道**,按钮按各 Agent 自己的 phase 决定。验收 37 专门验它 |
| **读线程拿不到"当前代际",`generation` 防串扰形同虚设** | `current_gen: Arc<AtomicU64>` 放**锁外**;读线程 / 守护线程捕获本代编号后原子比对,全程不锁 `proc` |
| **`agent/state` 把自己那次启动的响应作废掉** | 非终态广播**不递增 `opToken`**;`StartAck` 按 **`runId` 归属**应用,相位仅在仍是 `starting` 时推进。验收 40 专门验它(要连跑 10 次才现形) |
| **旧快照盖掉广播,相位被改回 `starting`** | 每 Agent 一个 **`rev`**(任何写运行时记录的事件都递增),快照带上请求时的 `rev`,不一致就丢弃运行时字段。验收 39 专门验它(连刷 10 次) |
| **刷新窗口里"零本地状态",`runId` / `rev` 双双失效** | 事件在本地无记录(或 `runId` 为 `null`)时,**用 payload 的 `runId` 建立记录并 `rev += 1`**——让快照随后因 `rev` 不等被判过期。这是阶段1 那条兜底的按 Agent 版本。验收 41 专门验它 |
| **`rev` 比较时给 `undefined` 补默认值** | 规则写成"**两边严格相等才接受**",不写 `?? 0`——否则"期间新建了记录、`rev` 恰好是 0"会被误判成"没变化",兜底又失效 |
| **把"运行身份"和"当前相位"用同一个判据** | `runId` / `resolvedModel` / `injectedEnv` / `cwd` 判 **`runId`**;`phase` / `result` / `lastError` 判 **`rev`**。捆在一起必然出错——v11 之前就因此丢过 `resolvedModel` |
| **事件不足以独立建出完整记录** | `agent/state` / `agent/exit` / `agent/error` **一律带 `resolvedModel`**。刷新后前端可能**只有事件、没有快照**,别指望"反正快照会补上"。发通知前把值从池记录抄进局部变量,删记录后仍拿得到。验收 41② 专门验它 |
| **旧快照把本地新一轮的 `runId` 写回旧轮次** | 本地 `runId` 非空且与快照不同 → 运行身份**无条件丢弃**,不留"`rev` 未变就采纳"的例外(序号回答不了"查询 vs 本地操作谁新");`rev` 逢写递增(`patchRuntime()` 默认 +1),本地 `start()` / `stop()` 自动作废在途快照。验收 42 专门验它 |
| **`JobHandle` 存裸 `HANDLE`,不是 `Send`** | 用 `std::os::windows::io::OwnedHandle`(std 类型,自带 `Send + Sync`,drop 自动 `CloseHandle`);不写 `unsafe impl`,也不存 `isize` 手动管生命周期 |
| **`opToken` 和 `rev` 被合并成一个计数器** | 明确两者**必须分开**:`agent/state` 要递增 `rev` 但**绝不能**递增 `opToken`——合并就会重新触发上一条 |
| **`kill()` 立即失败但进程仍活着,没回滚没广播** | 收尾规则写成"**只要没能确认进程已结束**就回滚 + 广播 + `stopFailed`",**不按失败原因分叉**(权限不足、系统拒绝终止都走这条) |
| **延时回调醒来时世界已变,仍把进程杀掉** | 池记录加 `termSeq`;延时回调到期后校验 `status` / `runId` / `killed` / `termSeq` 四项,任一不符就放弃。验收 38③ 专门验它 |
| **非终态广播与终态事件混用同一套作废机制** | 铁律:`opToken` **只由 `agent/exit` / `agent/error` 递增**(终态才是"作废一切在途操作"的时机) |
| **RPC 返回值漏字段,前端拿到 `undefined`** | 协议表与 `shared/protocol.ts` / Rust 结构体三方同步;`listAgents` 的 `config` 由 Sidecar 产生、Rust 透传。改协议时三处一起改 |
| **孤立实例的 `AgentInfo` 缺必填字段** | 逐情形定死取值:`defaultModel: ""`、`modelAliases: []`、`label` 用池里缓存的;约定"空模型 + 空别名 = 不可启动"。UI 不渲染空下拉,但保留终止入口 |
| **锁规则与代码打架,执行时只能自己发挥** | 从"最多持有一把"改为**分层锁序**:`start_lock → proc` 是唯一允许的嵌套,禁止反向;`pending` 与两者互斥 |
| **验收要求"制造一次 kill 超时",但 Windows 上做不到** | `child.kill()` = `TerminateProcess`,不可拦截。用 `stop_delay_ms`(测试钩子)把 kill 推迟到超时之后;并明确"3 秒从收到请求起算" |
| **配置里删掉的运行中实例从界面消失** | `listAgents` 返回**配置 ∪ 池记录**;孤立实例 `configured:false` + 原因,可终止,终止后自然消失。验收 26 专门验它 |
| **验收要求"在某个瞬时状态下操作",但那个瞬间只有几毫秒** | 加 `start_delay_ms` 测试钩子(默认 0,仅测试用);能机测的判据(如 `agent/error` 是否发出)优先走协议层,不依赖人工卡时间 |
| Sidecar 卡死导致界面永久转圈 | 每次调用带超时,超时按 `sidecarTimeout` 报错并标记不可用 |
| Rust 崩溃后 Sidecar 变孤儿 | stdin EOF 兜底 → `shutdownAll()` → `process.exit(0)` |
| Sidecar 崩溃时在途请求挂 30 秒 | 守护线程 drain `pending` 并丢弃发送端,在途 `recv` 立即返回 Err |
| 旧 Sidecar 的残留事件混入新会话 | `Arc<AtomicU64>` 的 `current_gen`(**在 `proc` 锁外**);读线程 / 守护线程在 spawn 时捕获本代编号,动作前比对 |
| 多 Agent 并行时一个退出误置另一个的状态 | `agent-exit` 按 agentId 区分,各 Agent 独立维护自己的 `phase` |
| **同一个 Agent 被并发启动两次** | Sidecar 池的"检查 + 占坑"放在同一个**同步**块里,中间不许有 `await`;`starting` 占位记录先于 spawn 存在。验收 13 含协议层直测 |
| **Sidecar 自动重启起出两个** | `ensure_started()` 用独立 `start_lock` 做双重检查锁定,不与 `pending` 共用锁。验收 29 专门验它 |
| **全局操作令牌在并发下互相误伤** | `opToken` 按 agentId 拆分;`applyExit(agentId)` 只作废该 Agent 的在途操作。验收 19 专门验它 |
| **Agent 自然退出后池记录没删** | `exit` 处理器按"先删记录、后发事件"的顺序;判定 `alreadyRunning` 前先确认 `exitCode === null`。验收 21/22 专门验它 |
| **前端状态改了但界面不更新** | `runtimes` 用 state + ref 双份,唯一写入口 `patchRuntime()`(先写 ref 再 setState)。验收 17 专门验它 |
| **首行输出丢失** | 启动时先 `patchRuntime({ phase:"starting", runId })` 再 invoke。验收 20 专门验它 |
| **模型别名下拉与配置脱节** | `list_agents` 回传 `modelAliases`(由配置算出),前端不写死;别名表改动需重新取 roster。验收 11 专门验它 |
| **`env-dump.js` 把密钥打进输出区** | 它自己就是一条回传通道(`agent/output` → Rust → 前端);密钥类变量在**两边**都只打印 `已设置(共 N 位)`,且不提供显示明文的开关。验收 36 专门验它 |
| **Job Object 分配失败却"静默通过"** | 失败不阻断应用,但 `sidecar-status.jobObject` 必须暴露到界面常驻提示;验收 27/32 以 `jobObject === true` 为前置,否则记"未验证" |
| **Sidecar 的错误种类没传到 Rust** | JSON-RPC `error.data.kind` 结构化传递;kind 由 `shared/protocol.ts` 的联合类型约束,`tsc` 挡住拼写 |
| **Sidecar 被强杀留下孤儿 Agent** | Rust 侧的 Windows Job Object(`KILL_ON_JOB_CLOSE`)+ 守护线程 `TerminateJobObject`。验收 27 专门验它 |
| **孙进程残留**(应用正常退出 / 应用被强杀 / Sidecar 被杀) | 三种情况统一由 Job Object 覆盖(本阶段实现,不再推到阶段6);非 Windows 平台退回 stdin EOF 路径。验收 27/31/32/33 |
| **用户改坏配置** | json5 解析(带 `lineNumber`/`columnNumber`)+ **两级校验**(致命 vs 单 Agent)+ 热重载,改回即恢复。验收 6/9 |
| 环境变量缺失(API Key)阻断验证 | 缺失只标黄提示,不阻断启动 |
| 中文 Agent 输出乱码 | **在 Sidecar 内按 Buffer 读 + 两级解码**(先 UTF-8,含 `U+FFFD` 再 `TextDecoder("gbk")`,包 try),不能用 `readline` 直接读子进程输出 |
| 打包后资源路径失效 | `resolve_resource` 映射写法 + `CARGO_MANIFEST_DIR` 开发回退 |
| **`http://` 被 `//` 注释逻辑吃掉** | 改用 `json5`,不自己写扫描器;验收 7 专门验它 |
| **刷新后界面显示"全部空闲"而进程仍在跑** | `listAgents` 回传运行时状态;`listen()` 就绪后调一次并按阶段1 规则恢复。验收 15 专门验它 |
| **刷新正好落在 `starting` 时状态恢复错** | `AgentInfo` 回传 `phase`(不是布尔 `running`),前端据此恢复成 `starting` 并保持终止禁用;`stopAgent` 对 `starting` 记录返回 `stopped:false`。验收 16 专门验它 |
| **启动失败时"没有接收者",界面永久停在"正在启动"** | 池记录被移除时必发终态通知(spawn 失败 → `agent/error`),广播不依赖发起方的 promise 还活着。验收 16 ② 专门验它 |
| **配置改回后那一项仍是灰的** | `listAgents` 与 `configReload` 也调 `ensureConfigFresh()`;前端在打开 Agent 下拉/点「重新加载配置」时重新取 roster。验收 9、10 专门验它 |
| **`jobObject` 刷新后丢失,验收前置无法确认** | 除 `sidecar-status` 事件外,把它放进 `list_agents` 返回值(`sidecar.jobObject`);`sidecar-status` 的 payload 处处带该必填字段。验收 28 专门验它 |
| **把"密钥不泄露"写成了超过实现边界的保证** | 明确写成"阶段2 的 `env-dump.js` 不输出密钥原文";Sidecar 对 Agent 输出是**原样转发、不脱敏**,阶段3 接真实 CLI 后须重新评估。验收 33 的注记 |
| **阶段1 与阶段2 的事件串扰** | 阶段1 事件改名 `diagnostic-*`;视图切换用条件渲染而非 `display:none` |
| **控制台验收不可执行** | **删掉该验收点**,不做调试端口;真正判据(输出不重复)已可机测 |
| **Sidecar 慢终止被误判为崩溃** | Rust `stop_agent` 超时(5s)必须大于 Sidecar 内部等 exit 的时长(3s) |
| **密钥顺着回传通道泄露** | 只回传注入层;密钥只回 `{ set, length }`;**`env-dump.js` 这条 `agent/output` 通道同样只打印 `已设置(共 N 位)`**(它也是回传通道,原方案漏了这点)。验收 36 |
| **esbuild 只转译不查类型,协议字段写错也能"构建成功"** | `typecheck:sidecar` 串进 `build:sidecar` 内部,任何构建路径都绕不过 |
| **两个启动入口互相打架** | 只保留底部输入框一个入口;主内容区只负责展示与终止 |
| **打包后 `config/agents.json` 落在只读安装目录,用户改不了** | 阶段6 改为复制到用户配置目录;本阶段在 README 与代码注释里写明归属(与"配置可被用户掌握"的初衷直接相关,**不要漏**) |
| **应用还没 `git init`,阶段5 的 Worktree 无从落手** | 阶段2 不受影响;在 `.gitignore` 里先写好 `sidecar/dist/`,阶段5 开工前先 `git init` |

---

## 交付后请告知用户的事

1. 创建/修改了哪些文件
2. 怎么运行:`启动开发预览.bat`,或直接 `npm run tauri dev`(sidecar 会在 `beforeDevCommand` 里自动先构建,不需要手动跑 `build:sidecar`)
3. **怎么改配置**:用记事本/VS Code 打开 `config/agents.json`,改 `model_aliases` 里的模型名,保存后**不用重启应用**,重新启动 Agent 就能看到环境变量变了。改之前可以先看文件里已有的注释,它解释了每个字段是干什么的
4. **怎么验证配置真的生效**:每次启动 Agent,主内容区会显示这次实际用的模型和注入摘要(点「详情」展开完整清单);Agent 自己的输出里也会打印同一份清单(`scripts/env-dump.js` 就是干这个的)。两处对得上,就说明配置生效了。**密钥在两处都只显示"已设置(共 N 位)",不会显示内容**——这是刻意的,不是没配上。要看密钥本身,请到系统环境变量里查
5. **Agent 启动后会一直运行**(每秒打印一行心跳),这是刻意的——用来验证终止、并发、刷新恢复。主内容区状态行右侧有「终止」
6. **只有一个启动入口**:底部输入框。选好 Agent 和模型、输入任务、按回车(或点发送)就启动。这不是聊天窗口,阶段2 一次只发一个任务
7. **如果界面底部出现一行浅色提示说"进程清理降级模式"**:这是当前系统环境不允许应用接管子进程组(常见于从某些编辑器终端里启动时)。功能不受影响,但**关闭应用后可能残留 node 进程**,需要在任务管理器里手动结束。正常从桌面/开始菜单启动不会出现这行提示
8. CC Switch 需要用户**自行启动**并配置路由;阶段2 用它只是让环境变量指向它,还没有真的发请求(那是阶段3)
9. 阶段2 **不做**的事:Hermes 调度、任务时间线、共享 Skills、Git Worktree、设置页 —— 都在后面阶段。**本阶段的界面是真实布局的第一版**,不是阶段1 那个临时诊断页了(旧页面还在,挪到了侧边栏的「诊断」里)
10. 如果 `config/agents.json` 改坏了:界面会红字提示第几行出错,改回去就好了,不用重启应用。**只有一个 Agent 写错时,只有它变灰,其它照常能用**
11. **如果配置整个写坏了(比如少了一个括号),但已有 Agent 在跑**:那个 Agent **仍然会显示在列表里,也仍然能终止它**。界面上会有一条配置错误提示,但不会因此把已经跑起来的东西锁死——你先把进程管好,再去修配置
12. **如果删掉一个正在运行的 Agent**:它会**继续留在列表里**并标一行说明("已从配置中移除,但这个实例仍在运行"),你可以照常终止它。终止之后它才会从列表消失。**不会因为你改配置文件就把正在跑的任务强行掐掉**——那太粗暴了
13. 配置里有两个 `start_delay_ms` / `stop_delay_ms`,**平时请保持 0**。它们不是给日常用的,是给"让我们能看清正在启动 / 正在终止的那一瞬间"用的(某些测试需要把过程放慢几秒)
