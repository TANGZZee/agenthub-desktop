# AgentHub Desktop 交接文档

> 最后更新：2026-02 ｜ 维护分支：`migration/hermes-desktop-base` ｜ 全部内容已推送 GitHub

本文是接手本项目所需的最小完整说明：项目现状、仓库布局、上游同步方法、
已完成与未完成的工作、常用命令和已知坑。配套详细文档见
`docs/hermes-agenthub-migration.md`（迁移架构）与 `docs/hermes-upstream-sync.md`
（同步操作细节）。

## 一、项目是什么

以 `fathah/hermes-desktop`（Hermes One，Electron + React + TS）为底座的
**AgentHub Desktop**：Hermes 是唯一主对话入口；Pi、Codex 等外部编码智能体
作为受控 Worker，由用户在 Workers 页**主动选择接入**，主进程统一准入、
启动、脱敏、取消，Hermes 只能通过 `worker.*` 工具调度，无法绕过。

本地路径：`D:\Project\Agent Hub Destop`（目录名含空格，`Destop` 是实际拼写）。

## 二、仓库与分支（GitHub：TANGZZee/agenthub-desktop）

**这个仓库不是 GitHub fork**（API 返回 `fork: false`），同步靠本地 `upstream`
远端 + merge，不需要 fork（原因与细节见 `docs/hermes-upstream-sync.md`）。

```
origin   → github.com/TANGZZee/agenthub-desktop   你的仓库，改动推这里
upstream → github.com/fathah/hermes-desktop        原项目，只拉不推
```

| 分支                            | 内容                                          | 状态                   |
| ------------------------------- | --------------------------------------------- | ---------------------- |
| `migration/hermes-desktop-base` | **主开发分支**：上游谱系 + 全部 AgentHub 工作 | 已推送，日常在这里开发 |
| `legacy/tauri-agenthub`         | 旧 Tauri 版本完整存档（含 18 个未推过的提交） | 只读存档               |
| `backup/pre-rebase`             | 历史嫁接前的孤儿历史备份                      | 只读保险               |
| `main`                          | 上游快照（落后，勿在上面开发）                | 保持不动               |

**历史谱系（重要）**：最初的迁移分支是从旧 Tauri 历史长出的孤儿提交，与上游
没有共同祖先，`git merge` 无法工作。2026-02 已用树对象嫁接重建为：

```
upstream/main (cff1f44)
  └─ e9376e69 基线（上游内容 + legacy-agenthub 参考，树与原孤儿提交逐字节相同）
      └─ d5b76a9f AgentHub 全量（目录接入、模型配置、Pi 修复）
          └─ b3b0a08f 同步工具 → 4d8f68f4 脚本修复 → 79991fb8 文档（当前 HEAD）
```

重建前后工作树零差异；两个无害差异均为 Windows 检出产物：上游 `.claude/skills/`
两个符号链接缺失、`build/linux-after-install.sh` 权限位 644。

## 三、上游同步（原项目更新怎么吸收）

上游有新提交时（当前还没有，上游 HEAD 仍是我们基线 `cff1f44`）：

```powershell
npm run sync:upstream          # 先看：新提交、涉及文件、冲突热区（不动手）
npm run sync:upstream:all      # 确认后：合并 + 自动验证
```

脚本 `scripts/hermes-upstream-sync.ps1` 的保证：拒绝脏工作树、只 merge
（从不 rebase/reset/force-push）、冲突自动 abort 保工作树可用（`-KeepConflicts`
可保留现场）、合并后自动跑类型检查 + AgentHub 测试 + ESLint、**从不自动推送**。

**冲突热区**（我们改过的上游自有文件，共 23 个，脚本会自动点名）：
`ipc/register.ts`、`preload/index.ts(.d.ts)`、`hermes.ts`、`app/start.ts`、
`Layout.tsx`、`main.css`、`desktopCommands.ts`、13 个语言包 `navigation.ts`、
`.gitignore`、`lat.md/lat.md`、`docs/hermes-agenthub-migration.md`。
解决原则：两边意图都保留——上游新行为照收，AgentHub 接线不删。
我们的 15 个自有文件（`src/main/agenthub/` 等）与上游零重叠，永不冲突。

## 四、已完成的功能

**Worker 编排（M1 闭环）**：`src/main/agenthub/` 下 admission（拒绝任意命令/
参数/环境注入）、redact（API Key/Token/私钥脱敏）、runner（受控子进程）、
orchestrator（8 个 `worker.*` 工具）、tool-server（127.0.0.1 + 随机 Token）、
catalog（用户选择持久化）、market（候选注册表）、profiles（Pi 固定参数）。

**智能体接入 = 列列表 + 用户选择**：Workers 页先列 AgentHub 已支持的 Runner
（目前仅 Pi），再列市面主流前十（Claude Code、Codex CLI、Gemini CLI、
GitHub Copilot CLI、Cursor CLI、OpenCode、Cline、Aider、Qwen Code、Goose）。
只有「本机探测到 + 有安全 Runner」才可接入；其余显示安装说明、按钮禁用并写明
原因。选择存在 `userData/agenthub-catalog.v1.json`，探测到命令**不等于**自动
接入，`worker.*` 工具改不了这份列表。

**每 Agent 独立配模型**：默认从 Hermes 模型库选（Provider/密钥/端点仍归
Hermes 管，AgentHub 不新增不改写凭据），也可选「自定义模型 ID」给单个 Agent
独立使用（由该 CLI 自己的登录解析，库外值记日志不拦截）。选择通过参数里的
`{model}` 占位符注入；未选则连同 `--model` 一起消失回到 CLI 默认；含命令行
危险字符的值直接拒绝。

**两个关键 Windows 修复**：

1. npm 装的 CLI 是 `.cmd` 批处理 shim，Node 直接 spawn 抛 `EINVAL`——
   现在解析 shim 指向的真实入口（JS 入口用 Node 跑、原生 exe 直接跑），
   任务文本不经过任何命令解释器（防注入）。
2. Pi 启动前先跑 `pi auth check` 预检，凭据未就绪立即返回中文可读错误，
   不再挂满 10 分钟超时。另：探测到但目标文件缺失的 CLI（安装损坏）拒绝接入。

## 五、未完成 / 下一步

1. **M2 多 Worker 编排**：Codex、Claude Code 的安全 Runner（只读参数复验）、
   并行步骤、依赖关系、重试、人工确认。候选已在列表里，`runner: "planned"`。
2. **Pi 真实任务**：本机 `pi.cmd` 0.85.1 已装但四个 Provider 凭据全部
   `not_ready`——需要先在终端完成 pi 登录/配 Key，之后点运行即可。
3. **Windows 进程树清理**（M4）：目前 `child.kill()` 只杀直接子进程。
4. **lat CLI 未安装**：`lat check` 靠脚本自动跳过；安装后自动纳入验证。
5. 可选：真要 GitHub 网页版 Sync fork，另建 `TANGZZee/hermes-desktop` 镜像。

## 六、常用命令

```powershell
npm run dev                        # 启动开发
npm run typecheck:node / :web      # 类型检查（npm 包装器可能误报 exit 1，看输出）
npm run sync:upstream              # 看上游更新（不动手）
npm run sync:upstream:all          # 合并 + 验证
npx vitest run tests/agenthub-orchestrator.test.ts   # AgentHub 测试（26 项）
npx eslint --max-warnings=0 src/main/agenthub src/renderer/src/screens/Workers
```

## 七、已知坑

- **npm 包装器把成功命令显示为 exit 1**：以输出里的 `NODE_EXIT`/`WEB_EXIT`/
  测试统计为准，不要只看退出码。
- **PowerShell 5.1 双坑**（脚本已规避，自己写脚本时注意）：函数里 `Write-Output`
  会被调用方捕获；`$ErrorActionPreference="Stop"` 时 git 的 stderr 会变终止错误。
- **PS1 脚本不要写非 ASCII 字符**：Windows PowerShell 5.1 按 ANSI 读无 BOM
  UTF-8，中文/破折号会乱码。
- `better-sqlite3` 的 postinstall 曾因路径含空格 + VS 工具链不全编译失败。
- 上游全量测试基线约 2178 通过 / 8 失败（Windows 适配、审批、CSS、macOS），
  不属于 AgentHub 改动。
- 本机 Claude Code 安装已损坏（`claude.exe` 被自更新重命名为 `.old.*`），
  Workers 页会正确显示「安装可能已损坏」并拒绝接入；重装后自动恢复。
- 旧 Tauri 的 Python 像素办公室（端口 19000、`third_party/Star-Office-UI`）
  尚未接入新底座。

## 八、关键文件地图

```
src/main/agenthub/        编排层全部主进程代码（admission/redact/runner/
                          orchestrator/tool-server/catalog/market/profiles）
src/shared/agenthub.ts    共享类型（Worker Profile、目录条目、工具响应）
src/renderer/src/screens/Workers/Workers.tsx   Workers 页（目录+模型+运行）
src/main/ipc/register.ts  末尾 agenthub-* IPC 通道（热区）
scripts/hermes-upstream-sync.ps1                同步脚本
docs/hermes-agenthub-migration.md               迁移架构与验收阶段
docs/hermes-upstream-sync.md                    同步操作细节
lat.md/agenthub-workers.md、agenthub-worker-tests.md   架构与测试规范
tests/agenthub-orchestrator.test.ts             26 项 AgentHub 测试
```

## 九、验证基线（交接时状态）

- Node/Web 类型检查：通过
- AgentHub + IPC + preload 测试：237 项全过
- 全量测试：2201 过 / 7 失败（全部为上游既有的审批与 macOS 用例，与本项目无关）
- ESLint（AgentHub 范围，0 警告）：通过
- Pi 预检真机验证：816ms 返回 `not_ready`（快速失败链路正常）
- `git status`：干净，HEAD = 79991fb8，已推送
