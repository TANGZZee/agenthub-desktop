```markdown
# AgentHub Desktop 设计规范

> **致 AI 编程 Agent**：  
> 本规范定义了项目的视觉与交互标准。用户要求风格为「简约大气，类似苹果」。  
> 请严格遵循本规范实现 UI。若遇到未覆盖的场景，**优先选择更简约、留白更多的方案**。  
> 所有数值为建议值，可根据实际效果微调，但风格方向不得偏离。

---

## 1. 设计原则

| 原则 | 含义 | 反例 |
|---|---|---|
| **留白优先** | 用空间分隔内容，而非线条和边框 | 密密麻麻的表格线 |
| **层级克制** | 用字重和灰度区分层级，而非多种颜色 | 五颜六色的标签 |
| **内容聚焦** | 一屏只做一件事，减少干扰元素 | 侧边栏堆满图标和文字 |
| **动效轻盈** | 动效服务于理解，不炫技 | 弹跳、旋转、闪烁 |
| **高效可达** | 键盘优先，常用操作不超过 2 步 | 深层嵌套菜单 |

---

## 2. 视觉系统

### 2.1 色彩

**主色调**：以黑白灰为主，仅用**一个强调色**。

```css
:root {
  /* 背景 */
  --bg-primary: #FFFFFF;        /* 主背景 */
  --bg-secondary: #F5F5F7;      /* 次级背景，如侧边栏 */
  --bg-tertiary: #FAFAFA;       /* 卡片内嵌区域 */

  /* 文字 */
  --text-primary: #1D1D1F;      /* 标题、正文 */
  --text-secondary: #6E6E73;    /* 辅助说明 */
  --text-tertiary: #AEAEB2;     /* 占位符、禁用 */

  /* 边框与分隔 */
  --border-light: #E5E5EA;      /* 极浅分隔线 */
  --border-medium: #D1D1D6;     /* 输入框边框 */

  /* 强调色（仅一个） */
  --accent: #0071E3;            /* 苹果蓝，用于按钮、链接、选中态 */
  --accent-hover: #0077ED;
  --accent-subtle: #E8F2FD;     /* 选中背景 */

  /* 状态色（仅用于状态指示，不用于装饰） */
  --status-success: #34C759;
  --status-warning: #FF9F0A;
  --status-error: #FF3B30;
  --status-idle: #AEAEB2;

  /* 深色模式 */
  --dark-bg-primary: #1C1C1E;
  --dark-bg-secondary: #2C2C2E;
  --dark-text-primary: #F5F5F7;
  --dark-text-secondary: #98989D;
  --dark-border: #38383A;
}
```

**用色规则**：
- 强调色只用于：主按钮、当前选中项、可点击链接、进度指示。
- 状态色只用于：Agent 状态圆点、错误提示、成功提示。
- 禁止用颜色区分任务类型（用图标或文字代替）。
- 深色模式必须支持，跟随系统设置，也可手动切换。

### 2.2 字体

```css
:root {
  --font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "PingFang SC", "Helvetica Neue", Arial, sans-serif;
  --font-mono: "SF Mono", "JetBrains Mono", "Fira Code", monospace;

  /* 字号层级 */
  --text-hero: 28px;      /* 页面主标题，字重 600 */
  --text-title: 20px;     /* 区块标题，字重 600 */
  --text-subtitle: 15px;  /* 卡片标题，字重 500 */
  --text-body: 14px;      /* 正文，字重 400 */
  --text-caption: 12px;   /* 辅助说明，字重 400 */
  --text-tiny: 11px;      /* 标签、时间戳，字重 400 */

  /* 行高 */
  --leading-tight: 1.3;
  --leading-normal: 1.5;
  --leading-relaxed: 1.7;
}
```

**字体规则**：
- 中文优先使用系统字体（PingFang SC / 微软雅黑），不加载外部字体。
- 代码和终端输出使用等宽字体。
- 标题与正文之间字重差不超过 200。
- 禁止使用斜体（除代码注释外）。

### 2.3 间距

```css
:root {
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 24px;
  --space-6: 32px;
  --space-7: 48px;
  --space-8: 64px;
}
```

**间距规则**：
- 组件内边距：`--space-3` 到 `--space-4`
- 卡片之间：`--space-4`
- 区块之间：`--space-5` 到 `--space-6`
- 页面边距：`--space-5` 到 `--space-6`
- 留白宁多勿少。

### 2.4 圆角

```css
:root {
  --radius-sm: 6px;    /* 小按钮、标签 */
  --radius-md: 10px;   /* 输入框、卡片 */
  --radius-lg: 14px;   /* 大卡片、面板 */
  --radius-xl: 20px;   /* 模态框、悬浮窗 */
  --radius-full: 9999px; /* 圆形头像、状态点 */
}
```

### 2.5 阴影

```css
:root {
  /* 极浅，用于卡片 */
  --shadow-card: 0 1px 3px rgba(0, 0, 0, 0.04),
                 0 1px 2px rgba(0, 0, 0, 0.06);

  /* 悬浮层，用于下拉菜单、模态框 */
  --shadow-float: 0 4px 16px rgba(0, 0, 0, 0.08),
                  0 2px 8px rgba(0, 0, 0, 0.04);

  /* 强调悬浮，用于弹出面板 */
  --shadow-elevated: 0 8px 32px rgba(0, 0, 0, 0.12),
                     0 2px 8px rgba(0, 0, 0, 0.06);
}
```

**阴影规则**：
- 阴影颜色用黑色低透明度，不用彩色阴影。
- 层级越高，阴影越大，但始终克制。
- 深色模式下阴影几乎不可见，改用边框区分层级。

### 2.6 动效

```css
:root {
  --duration-fast: 120ms;    /* 按钮反馈、状态切换 */
  --duration-normal: 200ms;  /* 面板展开、页面切换 */
  --duration-slow: 320ms;    /* 模态框出现、大幅布局变化 */
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-in-out: cubic-bezier(0.65, 0, 0.35, 1);
}
```

**动效规则**：
- 只做透明度和位移变化，不做缩放弹跳。
- 进入动画用 `--ease-out`，退出用 `--ease-in-out`。
- 尊重系统「减少动态效果」设置（`prefers-reduced-motion`）。
- 流式输出文字不做逐字动画，直接追加。

---

## 3. 布局系统

### 3.1 整体框架

```
┌─────────────────────────────────────────────────────┐
│  ┌──────────┐  ┌──────────────────────────────┐    │
│  │          │  │  顶部栏（当前任务标题 + 操作） │    │
│  │  侧边栏   │  ├──────────────────────────────┤    │
│  │          │  │                              │    │
│  │  · 任务   │  │                              │    │
│  │  · Agent │  │      主内容区                 │    │
│  │  · 设置   │  │      （任务时间线 / 设置）     │    │
│  │          │  │                              │    │
│  │          │  │                              │    │
│  │          │  ├──────────────────────────────┤    │
│  │          │  │  底部输入框（向 Hermes 提问）  │    │
│  └──────────┘  └──────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

**尺寸**：
- 侧边栏宽度：`220px`，可折叠为 `56px`（仅图标）。
- 顶部栏高度：`52px`。
- 底部输入框区域：自适应高度，最小 `64px`，最大 `200px`。
- 主内容区最大宽度：`860px`，居中，两侧留白。

### 3.2 侧边栏

- 背景：`--bg-secondary`。
- 无边框，与主内容区用背景色差区分。
- 顶部：应用名 + 折叠按钮。
- 导航项：图标 + 文字，高度 `36px`，圆角 `--radius-md`。
- 选中态：背景 `--accent-subtle`，文字 `--accent`，字重 500。
- 悬浮态：背景 `rgba(0,0,0,0.04)`。
- 底部：Agent 状态指示器（小圆点 + 名称）。

### 3.3 顶部栏

- 背景：`--bg-primary`，底部 `1px solid --border-light`。
- 左侧：当前任务标题（`--text-subtitle`）。
- 右侧：操作按钮（刷新、导出、设置），图标按钮 `28x28`，圆角 `--radius-sm`。
- 无多余装饰。

### 3.4 主内容区

- 背景：`--bg-primary`。
- 内容居中，最大宽度 `860px`。
- 上下留白 `--space-6`。

### 3.5 底部输入框

- 固定在底部，背景 `--bg-primary`，顶部 `1px solid --border-light`。
- 输入框：圆角 `--radius-lg`，背景 `--bg-tertiary`，无边框，聚焦时加 `1px solid --accent`。
- 左侧：附件按钮。右侧：发送按钮（圆形，强调色）。
- 支持 `Shift + Enter` 换行，`Enter` 发送。
- 输入框上方显示当前选中的 Agent（默认 Hermes）。

---

## 4. 核心组件设计

### 4.1 任务时间线

这是整个应用的核心视图。每个任务是一个卡片，按时间顺序排列。

**卡片结构**：

```
┌─────────────────────────────────────────────┐
│  ● 任务标题                        [状态标签] │
│  Pi · fast · 12s                            │
│  ┌─────────────────────────────────────────┐ │
│  │ 流式输出内容...                          │ │
│  └─────────────────────────────────────────┘ │
│  [展开详情]  [查看 Diff]                     │
└─────────────────────────────────────────────┘
```

**样式**：
- 卡片：背景 `--bg-primary`，圆角 `--radius-lg`，阴影 `--shadow-card`，内边距 `--space-4`。
- 状态圆点：`8px`，颜色对应状态（空闲灰、进行中蓝、成功绿、出错红）。
- 进行中时，圆点做缓慢呼吸动画（透明度 0.5 ↔ 1，周期 1.5s）。
- Agent 名称和模型：`--text-caption`，`--text-secondary`，用 `·` 分隔。
- 流式输出区域：背景 `--bg-tertiary`，圆角 `--radius-md`，等宽字体，最大高度 `200px`，超出滚动。
- 操作按钮：文字按钮，`--text-caption`，`--accent` 色，无背景。

**依赖关系可视化**：
- 如果任务 B 依赖任务 A，在 B 的卡片顶部显示一条细线连接到 A 的卡片底部。
- 用 `--border-light` 色，不抢眼。
- 或者更简单：在 B 的卡片上显示「等待 task-1 完成」的浅色标签。

### 4.2 Hermes 调度计划卡片

当 Hermes 输出调度计划时，先显示一个「计划卡片」，再展开各子任务。

```
┌─────────────────────────────────────────────┐
│  🧭 Hermes 调度计划                          │
│  将任务拆解为 3 个子任务，预计 2 分钟          │
│  ┌─────────────────────────────────────────┐ │
│  │ 1. Pi (fast) — 统计数据                  │ │
│  │ 2. Claude (powerful) — 重构模块          │ │
│  │ 3. Codex (balanced) — 生成测试           │ │
│  └─────────────────────────────────────────┘ │
│  [确认执行]  [修改计划]                       │
└─────────────────────────────────────────────┘
```

**样式**：
- 背景 `--accent-subtle`，圆角 `--radius-lg`，无阴影。
- 标题字重 600，`--text-subtitle`。
- 子任务列表：每行一个，`--text-body`，左侧序号用 `--text-tertiary`。
- 按钮：主按钮「确认执行」用强调色填充，次按钮「修改计划」用文字按钮。

### 4.3 Agent 状态指示器

在侧边栏底部，显示所有已注册 Agent 的状态。

```
┌──────────────────┐
│  ● Hermes   空闲  │
│  ● Pi       工作中│
│  ● Claude   空闲  │
│  ● Codex    出错  │
└──────────────────┘
```

- 每行：状态圆点 + 名称 + 状态文字。
- 状态圆点 `6px`，颜色对应状态。
- 工作中时，名称后显示当前模型，`--text-tiny`，`--text-tertiary`。
- 出错时，整行可点击，弹出错误详情。

### 4.4 设置界面

- 分组展示：模型配置、Agent 管理、Skills、Worktree、外观。
- 每组之间用 `--space-6` 分隔，不加分隔线。
- 表单控件：标签在左，控件在右，标签宽度 `120px`。
- 输入框：背景 `--bg-tertiary`，无边框，聚焦时加 `1px solid --accent`。
- 下拉选择：同输入框样式，右侧箭头图标。
- 保存按钮：固定在底部右侧，强调色填充，圆角 `--radius-md`。

### 4.5 空状态

- 无任务时：主内容区居中显示一个极简图标 + 一句话引导。
- 图标：线性风格，`48x48`，`--text-tertiary`。
- 文字：`--text-body`，`--text-secondary`。
- 不显示任何插画或装饰。

### 4.6 加载状态

- 不显示旋转加载圈。
- 用骨架屏（Skeleton）：浅灰色块，圆角 `--radius-md`，做缓慢的透明度脉动。
- 流式输出时，光标用 `2px` 宽竖线，闪烁周期 `1s`。

---

## 5. 交互设计

### 5.1 键盘优先

| 快捷键           | 功能                  |
| ---------------- | --------------------- |
| `Cmd/Ctrl + K`   | 打开命令面板          |
| `Cmd/Ctrl + N`   | 新建任务              |
| `Cmd/Ctrl + ,`   | 打开设置              |
| `Cmd/Ctrl + B`   | 折叠/展开侧边栏       |
| `Cmd/Ctrl + 1~4` | 切换到对应 Agent 视图 |
| `Esc`            | 关闭当前面板/取消操作 |
| `Enter`          | 发送输入              |
| `Shift + Enter`  | 输入换行              |

### 5.2 命令面板

- `Cmd/Ctrl + K` 唤出，居中悬浮。
- 背景模糊，圆角 `--radius-xl`，阴影 `--shadow-elevated`。
- 输入框 + 结果列表，支持模糊搜索。
- 结果项：图标 + 文字 + 快捷键提示。
- 上下键选择，`Enter` 确认，`Esc` 关闭。

### 5.3 反馈规则

- 按钮点击：`120ms` 内背景色加深 5%。
- 操作成功：不弹 toast，在操作位置显示一个短暂的绿色勾（`1.5s` 后消失）。
- 操作失败：在操作位置显示红色文字提示，不弹窗。
- 危险操作：弹出确认框，确认按钮用红色，取消按钮用灰色。

### 5.4 流式输出

- 文字直接追加，不做逐字动画。
- 自动滚动到底部，用户手动上滚后暂停自动滚动，显示「回到最新」按钮。
- 输出中的代码块：背景 `--bg-tertiary`，圆角 `--radius-sm`，等宽字体。

### 5.5 拖拽

- 任务卡片可拖拽调整顺序（如果有意义）。
- 拖拽时卡片轻微放大（`1.02`），阴影加深。
- 放置位置用一条 `2px` 强调色线指示。

---

## 6. 深色模式

- 跟随系统，也可在设置中手动切换（浅色/深色/跟随系统）。
- 深色模式下：
  - 背景用 `--dark-bg-primary` 和 `--dark-bg-secondary`。
  - 文字用 `--dark-text-primary` 和 `--dark-text-secondary`。
  - 边框用 `--dark-border`。
  - 阴影几乎不可见，改用 `1px` 边框区分层级。
  - 强调色不变，但饱和度可略微降低。
- 切换时用 `200ms` 过渡，只过渡背景色和文字色。

---

## 7. 响应式

- 最小窗口尺寸：`900 x 600`。
- 窗口宽度 `< 1100px` 时，侧边栏自动折叠为图标模式。
- 窗口宽度 `< 800px` 时，主内容区边距减小到 `--space-4`。
- 不做移动端适配。

---

## 8. 图标

- 使用 **Lucide** 图标库（线性风格，统一 `1.5px` 描边）。
- 图标尺寸：`16px`（内联）、`20px`（按钮）、`24px`（导航）。
- 图标颜色跟随文字颜色。
- 禁止使用彩色图标或填充风格图标。

---

## 9. 参考与禁止

**参考**：
- macOS 系统设置
- Things 3
- Linear
- Arc 浏览器
- Raycast

**禁止**：
- 渐变色背景
- 多种强调色混用
- 大面积彩色区块
- 弹跳、旋转、闪烁动画
- 拟物化设计
- 过度使用阴影
- 密集的表格线
- 五颜六色的标签
- 插画和卡通元素

---

## 10. 给 AI 编程 Agent 的 UI 实现指令

1. **先实现布局框架**：侧边栏 + 顶部栏 + 主内容区 + 底部输入框。用 `--bg-secondary` 和 `--bg-primary` 区分区域，不加边框。
2. **再实现任务时间线**：卡片用 `--shadow-card`，圆角 `--radius-lg`，内边距 `--space-4`。
3. **然后实现 Hermes 调度卡片**：用 `--accent-subtle` 背景，突出但不刺眼。
4. **最后实现设置界面和命令面板**。
5. **每个组件完成后，截图给用户看**，确认风格方向正确再继续。
6. **如果不确定某个设计决策，选择留白更多、颜色更少的方案。**
7. **所有颜色、间距、圆角、阴影必须使用本规范定义的 CSS 变量**，不要硬编码。
8. **深色模式必须实现**，用 `prefers-color-scheme` 媒体查询 + 手动切换。
9. **动效必须尊重 `prefers-reduced-motion`**。
10. **禁止使用任何 UI 组件库的默认样式**（如 Ant Design、Element Plus），必须基于 shadcn/ui 自定义，或手写组件。

---

> **本设计规范与项目任务书配合使用。UI 实现优先级：布局 > 时间线 > 调度卡片 > 设置 > 命令面板。**

