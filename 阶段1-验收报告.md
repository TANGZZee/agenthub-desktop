# 阶段 1 独立验收报告

> 验收对象：AgentHub Desktop 阶段 1（Tauri 壳 + 单 Agent 进程管理 + 流式输出）
> 验收日期：2026-09-13
> 验收方式：独立阅读代码 + 完整编译 + 真实运行 GUI 逐项操作
> 验收结论：**部分通过**（零功能失败项；4 项防御/环境类检查因无法在不改代码前提下触发而未验证）

---

## 0. 结论摘要

| 维度 | 结果 |
|---|---|
| 静态契约（命令 / 事件 / 错误结构 / runId / 状态机 / 并发防护） | ✅ 全部符合计划 |
| `npm run build` | ✅ 退出码 0 |
| `cargo fmt --all -- --check` | ✅ 退出码 0，无 diff |
| `cargo check`（已加载 MSVC 环境） | ✅ 退出码 0 |
| 界面与功能实测（九大类，共 40+ 检查点） | ✅ 全部通过 |
| 功能性失败项 | **0 项** |
| 未验证项 | 4 项（1 项采样精度不足 + 2 项防御性分支 + 1 项 DevTools 被禁用） |
| 是否修改项目文件 | **否**（仅按验收要求重新生成 `dist/` 构建产物） |

判定为"部分通过"的唯一原因是上述 4 项未验证项；未做任何"推测通过"。

---

## 1. 环境信息

| 项 | 值 |
|---|---|
| 操作系统 | Windows 11 Pro 10.0.26220.7872（win32 x64） |
| Node / npm | v24.19.0 / 12.0.2 |
| Rust | rustc 1.98.1 (48a229cea 2026-09-01) |
| Cargo | cargo 1.98.1 (797e8a9bc 2026-08-05) |
| Tauri 核心 | tauri **2.11.5** |
| Tauri 构建 | tauri-build **2.6.3** |
| WebView 运行时 | wry **0.55.1**（WebView2） |
| @tauri-apps/cli | **2.11.4** |
| 前端 | React 19.3.0、Vite 8.3.0、TypeScript ~6.0.3、lucide-react ^0.468.0 |
| MSVC 工具链 | `D:\Microsoft Visual Studio\2022\BuildTools` |
| 项目路径 | `D:\Project\Agent Hub Destop`（含空格） |

### 验收手段说明

本轮不是静态检查，而是**真实运行 + 真实交互**：

| 手段 | 说明 |
|---|---|
| Windows UI Automation | 读取真实 WebView2 的无障碍树，获取状态文字、按钮 `IsEnabled`、输出行内容与流标签、控件边界矩形 |
| 真实鼠标输入 | `SetCursorPos` + `mouse_event`，用于"快速连点 3 次启动"等竞态测试 |
| 真实键盘输入 | `SendKeys`（F5 刷新、方向键切换 `<select>` 预设） |
| 窗口像素抓取 | `PrintWindow(hwnd, hdc, 2)` 抓真实窗口位图，用于颜色/布局/深色模式确认 |

所有辅助脚本均放在 `C:\Users\Tang\AppData\Local\Temp\`，**未写入项目目录**。

---

## 2. 静态代码检查结论

### 2.1 必须项逐条核对

| 检查项 | 结论 | 证据位置 |
|---|---|---|
| Rust 命令**只有** `spawn_test_agent` / `kill_test_agent` / `get_agent_status` | ✅ | `src-tauri/src/lib.rs:386-390`（`generate_handler!` 仅列这三个） |
| 事件**严格**为 `agent-output` / `agent-exit` | ✅ | 后端 `lib.rs:203`、`lib.rs:271`；前端 `src/App.tsx:191`、`src/App.tsx:195`，全项目无其它事件名 |
| 错误使用结构化 `CmdError` 而非裸字符串 | ✅ | `lib.rs:23-37` 定义 `{kind, message}` + `#[serde(rename_all = "camelCase")]`；kind 取值 `alreadyRunning`(291) / `spawnFailed`(105,114,120,123,297,302,306) / `killFailed`(356) / `unknownPreset`(145)；前端 `App.tsx:66-82` 按 `errKind`/`errMessage` 分支，未匹配错误文案 |
| `runId` 由前端生成并用于过滤输出 | ✅ | 前端 `App.tsx:242` `const runId = Date.now()`，`App.tsx:244` 先写 `runIdRef` 再 `invoke`(253)；后端 `RunInfo.run_id: u64`(lib.rs:65) 直接使用传入值，不自增；过滤在 `App.tsx:192` 与 `App.tsx:196` |
| 实现 7 个状态 `subscribing`/`unknown`/`idle`/`starting`/`running`/`terminating`/`finished` | ✅ | 类型定义 `App.tsx:13-20`；初始值 `App.tsx:106`；全部有赋值路径与 UI 映射(`App.tsx:84-103`、`310-312`) |
| **挂载代际编号**，避免 StrictMode 下监听器残留 | ✅ | `App.tsx:115` `mountGenRef`；`App.tsx:134-136` `alive(gen)`；`App.tsx:186` `const gen = ++mountGenRef.current`；`App.tsx:209-212` 已作废则立即全部 unlisten；`App.tsx:225` 清理时**只递增不重置** |
| **操作令牌**，避免迟到异步结果覆盖新状态 | ✅ | `App.tsx:116` `opRef`；`App.tsx:126-132` `beginOp`/`opValid`；`start`(240)/`kill`(277)/`syncStatus`(154) 各自取号并在回调验号（254/258/159/179/285）；`applyExit`(146) `opRef.current++` 作废在途操作 |
| spawn 时**全程持有状态锁**，避免双击启动两个进程 | ✅ | `lib.rs:288` 单次 `lock_slot`，直到 `slot.replace`(310) 之后才释放；检查 `is_some`(290) 与 `spawn`(296) 之间无解锁 |
| 进程退出时**先清空槽位、再发送 `agent-exit`** | ✅ | `lib.rs:256-268` 清槽代码块在 `lib.rs:270-277` 的 `emit("agent-exit")` 之前 |
| `kill` 幂等，且能处理"进程刚好自行结束"的收尾窗口 | ✅ | `lib.rs:335-338` 空槽直接 `Ok(())`；`lib.rs:340-342` `killed` 已 true 直接 `Ok(())` 不重复 store；`lib.rs:348-353` `kill()` 失败后 `try_wait()` 得 `Ok(Some(_))` → 回滚 `killed=false` 并 `Ok(())`，由监视线程发 `killed=false` 的正常退出 |
| 窗口退出时清理直接子进程 | ✅ | `lib.rs:394-404` `RunEvent::Exit` 内对残留 child `kill()` |
| 输出全部先于退出事件（join 读线程后才 emit） | ✅ | `lib.rs:254-255` 先 join stdout/stderr，`lib.rs:270` 才 emit |
| 读线程 join 失败不阻塞状态释放 | ✅ | `lib.rs:254-255` 使用 `let _ =`，异常也不影响后续清槽 |
| 中文 ping 输出 GBK 解码 | ✅ | `lib.rs:163-172`：**先** `from_utf8`，失败**才** `GBK.decode`（未先 lossy，符合计划要求） |
| 挂载即可靠：先注册监听再查状态 | ✅ | `App.tsx:208-216` `Promise.all(listen...)` 成功后才调用 `syncStatus(gen)` |
| 状态未知时不落 idle | ✅ | `App.tsx:178-182` catch 只 `setPhase("unknown")`，从不落 idle |
| `alreadyRunning` 转入 unknown 且**不清空 runId** | ✅ | `App.tsx:259-265` 先 `setError` + `setPhase("unknown")` + `syncStatus`，未执行 `runIdRef.current = null`；该清空只在普通失败分支 `App.tsx:267` |
| 双击防护：函数内门禁（不依赖重渲染） | ✅ | `start()` `App.tsx:238`；`kill()` `App.tsx:274` |
| 呼吸动画仅运行中圆点 + `prefers-reduced-motion` 禁用 | ✅ | `src/index.css:223-227`、`index.css:245-254`、`index.css:162-171` |
| 深色模式全量变量覆盖 | ✅ | `src/index.css:129-160` |
| 主内容区最大宽度 860px 居中、上下 `--space-6` | ✅ | `src/index.css:187`、`index.css:261` |
| 图标使用 lucide-react，无彩色/填充图标 | ✅ | `App.tsx:4-11` 导入 `ArrowDown/ChevronDown/Play/RotateCcw/Square/Terminal`，均为线性图标 |
| 无 Tailwind / shadcn / 其它 UI 库（阶段 1 计划要求手写 CSS） | ✅ | `package.json` 依赖仅 react/react-dom/@tauri-apps/api/@tauri-apps/plugin-opener/lucide-react |

### 2.2 旁支确认

- `scripts/test-agent.js`：`TOTAL_LINES = 30`、`INTERVAL_MS = 500`、第 10/18 行走 `console.error`、支持 `--fast-exit` 打印一行后正常退出、`process.exitCode` 仅在异常时为 1 —— 与计划完全一致。
- `src-tauri/tauri.conf.json`：`width: 1100`（已修正原 800 与 minWidth 900 的矛盾）、`minWidth: 900`/`minHeight: 600`、`resources` 采用**映射写法** `{"../scripts/test-agent.js": "scripts/test-agent.js"}`（避免 `$RESOURCE/_up_/` 问题）—— 与计划一致。
- `src-tauri/Cargo.toml`：已加入 `encoding_rs = "0.8"`。
- `src/main.tsx`：`import "./index.css"`，`src/App.css` 已删除。
- `package.json`：`name` 已改为 `agenthub-desktop`，已加入 `lucide-react`。
- `src-tauri/capabilities/default.json`：保留 `core:default`（覆盖 event listen）与 `opener:default`。

---

## 3. `npm run build` 结果

```
> npm run build
> tsc && vite build

vite v8.3.0 building client environment for production...
✓ 1569 modules transformed.
dist/index.html                   0.48 kB │ gzip:  0.31 kB
dist/assets/index-DTEB7AMW.css    8.95 kB │ gzip:  2.47 kB
dist/assets/index-iflxCNLE.js   228.92 kB │ gzip: 72.25 kB
✓ built in 1.77s
EXIT_CODE=0
```

**退出码：0**。`tsc` 无类型错误，`vite build` 成功。

---

## 4. `cargo fmt` 与 `cargo check` 结果

```
$ cargo fmt --all -- --check
FMT_EXIT=0
（无任何输出，说明格式完全符合 rustfmt）
```

```
$ call "D:\Microsoft Visual Studio\2022\BuildTools\VC\Auxiliary\Build\vcvars64.bat"
$ cargo check
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 15.66s
CARGO_CHECK_EXIT=0
```

**两条命令退出码均为 0。**

> 附带说明（非阻塞）：`vcvars64.bat` 自身会先打印一行 `'vswhere.exe' 不是内部或外部命令，也不是可运行的程序`，随后继续执行且 `call` 返回 0；`cargo check` 不受影响。这是 VS BuildTools 安装环境的既有现象，不是项目问题。

---

## 5. 启动脚本 `启动开发预览.bat` 验证

脚本内容（`启动开发预览.bat`）：

```bat
@echo off
setlocal
cd /d "%~dp0"
set "VCVARS_PATH=D:\Microsoft Visual Studio\2022\BuildTools\VC\Auxiliary\Build\vcvars64.bat"
if not exist "%VCVARS_PATH%" set "VCVARS_PATH=C:\Program Files\...\vcvars64.bat"
call "%VCVARS_PATH%" >nul
if errorlevel 1 ( echo [ERROR] ... & pause & exit /b 1 )
echo Starting AgentHub Desktop...
call npm run tauri dev
...
```

**实测（两轮）能正常进入开发模式**：

```
Starting AgentHub Desktop...
npm notice run agenthub-desktop@0.1.0 tauri
npm notice run tauri dev
     Running BeforeDevCommand (`npm run dev`)
  VITE v8.3.0  ready in 717 ms
  ➜  Local:   http://localhost:1420/
     Running DevCommand (`cargo  run --no-default-features --color always --`)
        Info Watching D:\Project\Agent Hub Destop\src-tauri for changes...
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 6.64s
     Running `target\debug\agenthub-tmp.exe`
```

窗口随后出现（`ProcessName=agenthub-tmp`, `MainWindowTitle='AgentHub Desktop'`）。

> 验收方法备注：验收方 shell 直接以中文文件名调用 `.bat` 会被控制台代码页（GBK/936）破坏，因此改用 `%TEMP%` 下一个内容为 `call "D:\Project\Agent Hub Destop\启动开发预览.bat"` 的 ASCII 名包装脚本（GBK 编码）原样调用。**脚本本身未做任何改动，其加载 VS 环境并进入开发模式的能力已被证实。**

---

## 6. 界面与功能逐项测试

以下每项均写明【步骤】【预期】【实际】。

### 一、启动状态

| # | 步骤 | 预期 | 实际 | 结论 |
|---|---|---|---|---|
| 1.1 | 页面打开后立即采样启动按钮 | 应先短暂禁用 | **未采样到**。见第 8 节未验证项 1 | ⚠️ 未验证 |
| 1.2 | 等待监听 + 状态查询完成 | 启动按钮变为可用 | `STATE status='空闲' start=enabled stop=disabled retry=absent` | ✅ |
| 1.3 | 模拟事件订阅失败 | 停留在"正在连接"，**不显示**重试按钮 | **未能触发**。见第 8 节未验证项 2 | ⚠️ 未验证 |
| 1.4 | 模拟状态查询失败 | 进入"状态未知"，启动禁用，显示"重新同步" | **未能触发**。见第 8 节未验证项 3 | ⚠️ 未验证 |

### 二、Node 测试脚本（预设"Node 测试脚本"）

| # | 步骤 | 预期 | 实际 | 结论 |
|---|---|---|---|---|
| 2.1 | 选择"Node 测试脚本" → 点击启动 | 应实时逐行显示 | 轮询行数随时间增长：`OUT=0 → 2 → 3 → 4 → … → 29`，耗时约 15.4s，约 500ms/行，**非一次性出现** | ✅ |
| 2.2 | 统计总行数 | 31 行 = 1 行开头说明 + 30 行测试内容 | `LINES total=31` | ✅ |
| 2.3 | 检查第 10/30、18/30 行 | 必须来自 stderr 且显示红色 | 控制台第 11 条与第 19 条为 `ERR` 标签，内容分别为 `第 10/30 行（来自 stderr）`、`第 18/30 行（来自 stderr）`；`05-scrolled-up.png` 确认渲染为红色 | ✅ |
| 2.4 | 检查 StrictMode 是否造成重复输出 | 不得重复 | `OUT=29 + ERR=2 = 31`，每行恰好出现一次，无重复 | ✅ |
| 2.5 | 检查最终状态 | 显示绿色"已完成" | `status='已完成'`，`03-node-finished.png` 确认绿色圆点 + 绿字 | ✅ |
| 2.6 | 检查最后几行是否在状态变化之前出现 | 必须 | 3 次独立复现，用单次 UIA 快照同时读行数与状态：<br>`RACE PROOF: observed lines=31 while status was still '运行中' at t=15149ms`<br>`RACE PROOF: observed lines=31 while status was still '运行中' at t=15144ms`<br>第 3 次：首次读到 31 行时状态已是"已完成"（即**从未出现"已完成但行数不足"**） | ✅ |
| 2.7 | 运行中检查启动按钮 | 必须禁用 | 整个运行期 `start=disabled`，`stop=enabled` | ✅ |
| 2.8 | 快速连续点击启动（真实鼠标，45ms 间隔连点 3 次） | 只能有一个进程，不能显示虚假 `alreadyRunning` | `PROCCOUNT test-agent.js = 1`（`pid=18604`，只有一个）；输出速率仍为 500ms/行（未翻倍）；状态正常；无任何错误红字 | ✅ |

### 三、快速退出（预设"Node 快速退出"）

| # | 步骤 | 预期 | 实际 | 结论 |
|---|---|---|---|---|
| 3.1 | 选择"Node 快速退出" → 点击启动 | 只打印一行后立即退出 | `LINES total=1`，内容 `[Node 快速退出] 已启动，本行输出后立即正常退出` | ✅ |
| 3.2 | 检查界面状态 | 最终必须显示绿色"已完成" | `status='已完成'`（`07-fast-exit.png` 确认绿色） | ✅ |
| 3.3 | 检查是否卡住 | 不得卡在"正在启动"或"运行中" | `TRACE t=243ms status='运行中'` → `TRACE t=470ms status='已完成'`，未卡住 | ✅ |

### 四、终止测试

| # | 步骤 | 预期 | 实际 | 结论 |
|---|---|---|---|---|
| 4.1 | 启动 Node 测试 → 运行 3s → 点击终止 | 最终显示灰色"已终止"，不能红色失败 | `status='已终止'`（灰色，`08-terminated.png` 确认灰点+灰字） | ✅ |
| 4.2 | 终止处理中连续快速点击终止（Invoke + 2 次真实鼠标点击） | 最终仍是灰色"已终止" | 首次 `CLICK '终止' -> Invoke() ok`，随后 3 次均 `DISABLED`（按钮禁用 + 函数内门禁）；此后连续采样 11s，状态稳定为"已终止"，未翻红 | ✅ |
| 4.3 | 进程自然结束后立刻点击终止（收尸窗口） | 不能出现错误的红色失败 | 脚本轮询到 31 行出现的瞬间立即 Invoke 终止（此时按钮**仍为 enabled**，说明前端仍认为在运行）：<br>`REAPWIN: saw 31 lines at 15071ms -> clicking 终止 now`<br>`REAPWIN: Invoke 终止 -> ok`<br>最终 `status='已完成'`（**绿色**，非红色"运行失败"）。<br>分析：后端 `kill()` 返回 Err 后 `try_wait()` 确认进程已退出 → `killed=false` → 前端按正常结束处理。**该边界为实测命中，非仅代码评审** | ✅ |
| 4.4 | 进程自然结束后立刻再次点击启动 | 不能出现虚假"已有测试进程运行中" | 在 UI 反映"已完成"后 **78ms** 立即点击启动 → `status='运行中'`，新输出从 0 行重新计起；无任何错误 | ✅ |

### 五、Ping 测试

| # | 步骤 | 预期 | 实际 | 结论 |
|---|---|---|---|---|
| 5.1 | 选择"Ping 测试" → 点击启动 | 第一行输出不得缺失 | 表头 `正在 Ping 127.0.0.1 具有 32 字节的数据:` 完整存在。<br>注：`ping` 本身第 1 行是空行，已单独用 `ping 127.0.0.1 -n 2 \| cat -A` 核对，属 ping 原生输出，**不是丢行** | ✅ |
| 5.2 | 检查中文是否乱码 | 不得乱码 | 全部 20 条回复 + 4 行统计信息中文正常（GBK→UTF-8 解码生效） | ✅ |
| 5.3 | 检查是否逐行实时且正常结束 | 应满足 | 行数约 1s 增长一条，自然结束变"已完成"，共 **27 行**，与原生 ping（空行+表头+20 回复+空行+4 统计）**完全一致** | ✅ |

### 六、刷新恢复

| # | 步骤 | 预期 | 实际 | 结论 |
|---|---|---|---|---|
| 6.1 | 运行中按 F5 | 刷新后应恢复为"运行中" | F5 后 `OUT=0`，随后 `status='运行中'` | ✅ |
| 6.2 | 检查输出区 | 应为空（预期行为） | `OUT=0 ERR=0`，输出区为空 | ✅ |
| 6.3 | 刷新后不操作，等待自然结束 | 必须变绿"已完成"，不能永久卡"运行中" | 状态推进为 `已完成`；此后仅收到刷新后产生的行（17 行），说明 runId 通过 `get_agent_status` 正确恢复 | ✅ |

### 七、自动滚动

| # | 步骤 | 预期 | 实际 | 结论 |
|---|---|---|---|---|
| 7.1 | 输出较长内容，处于底部 | 自动滚动到底 | `BACK-TO-LATEST: absent`（无按钮） | ✅ |
| 7.2 | 用户鼠标滚轮上滚 | 自动滚动暂停 | `BACK-TO-LATEST: present enabled=True`（`05-scrolled-up.png` 可见按钮） | ✅ |
| 7.3 | 点击"回到最新" | 应回到输出末尾 | 按钮消失，`06-back-to-latest.png` 确认已回到末尾（第 30/30 行可见） | ✅ |

### 八、窗口尺寸与深色模式

| # | 步骤 | 预期 | 实际 | 结论 |
|---|---|---|---|---|
| 8.1 | 窗口缩小到 900×600 | 无横向溢出、文字重叠、按钮被挤出 | `BOUNDS window=(80,60)-(980,660) size=900x600`<br>`BOUNDS maxTextRight=948 maxTextBottom=640 (window right=980 bottom=660)`<br>`BOUNDS overflow: NONE`<br>`button '启动' rect=(747,248)-(835,285)`<br>`button '终止' rect=(843,248)-(931,285)`<br>`combo rect=(129,248)-(735,285)`<br>全部控件在窗口内，无重叠（`14-900x600-light.png`） | ✅ |
| 8.2 | 切换 Windows 深色模式 | 背景/文字/边框/状态色正常，无亮白断层 | 将 `HKCU\...\Themes\Personalize\AppsUseLightTheme` 由 1 改为 0 并广播 `ImmersiveColorSet` 后：背景深色、文字浅色、边框深色、绿色状态点与灰字正常、输出区无亮白断层；stderr 行在深色下仍为红色（`15-dark-900x600.png` / `16-dark-scrolled.png` / `17-dark-stderr.png`）。**测试完成后已还原为 1** | ✅ |

### 九、关闭清理

| # | 步骤 | 预期 | 实际 | 结论 |
|---|---|---|---|---|
| 9.1 | 启动 Node 测试（`pid=29512 node.exe "node" test-agent.js`）运行中直接关闭窗口 | 无残留 | 开发进程退出码 0；残留检查三组查询全部为空 | ✅ |
| 9.2 | 启动 Ping（`pid=8472 cmd="ping" 127.0.0.1 -n 20`）运行中直接关闭窗口 | 无残留 | 开发进程退出码 0；残留检查三组查询全部为空 | ✅ |
| 9.3 | 关闭开发终端 | — | 后台开发任务正常结束（exit code 0），同时确认 vite dev server（node.exe）随开发进程一并退出 | ✅ |

残留检查实际输出（由 `.ps1` 文件执行，避免命令行自匹配）：

```
=== test-agent.js ===
=== ping.exe ===
=== agenthub-tmp / node dev-server ===
=== DONE ===
```

**三组查询均无任何输出。**

---

## 7. 失败或异常项目

**无功能性失败项。**

以下为两点非阻塞观察（不计入失败）：

1. **`src-tauri/Cargo.toml` 包名仍为 `agenthub-tmp`**（产物 `target\debug\agenthub-tmp.exe`，lib 名 `agenthub_tmp_lib`）。
   阶段 1 计划只要求修改 `package.json` 的 `name`（已改为 `agenthub-desktop`），未要求修改 Cargo 包名；窗口标题取自 `tauri.conf.json` 的 `productName`，实际显示为"AgentHub Desktop"，正确。
   **不算违反计划**，建议阶段 2 顺手统一命名。

2. **`index.html` 的 `<title>` 仍为模板值 `Tauri + React + Typescript`**。
   该值即 WebView 文档标题（UIA 中 `RootWebArea` 名可见）。窗口标题正确，计划未要求修改，属模板遗留。

其余模板遗留物（`README.md`、`src/assets/react.svg`、`public/vite.svg`、`public/tauri.svg`、`.vscode/`）计划中均未要求阶段 1 清理，不计为问题。

---

## 8. 未验证项目及原因

| # | 项目 | 为什么未验证 | 代码层面的确认 |
|---|---|---|---|
| 1 | **一.1 页面打开后启动按钮应先短暂禁用** | `subscribing` 相位在本地开发环境仅持续**数毫秒**（`listen()` 是一次本地 IPC）。UIA 每次查询开销 50–70ms，用最高频轮询实测（3 次 F5，每次 120 样本）每次**第一个样本（t=56/66/67ms）按钮已经是 enabled**，无法区分"相位已跳过"与"从未禁用"。属**采样能力不足**，非功能缺陷。 | 初始 state 为 `subscribing`（`App.tsx:106`）；`canStart` 对 subscribing 为 false（`App.tsx:310`）→ 启动按钮禁用 |
| 2 | **一.3 事件订阅失败应停留在"正在连接"，且不显示重试按钮** | 无法在不修改项目文件的前提下注入订阅失败。 | `Promise.all(...).catch` 分支执行 `setPhase("subscribing")` + 红字提示（`App.tsx:217-221`）；`showRetry` 仅对 `unknown` 为真（`App.tsx:312`）→ 不显示重试按钮；`canStart` 对 subscribing 为 false → 启动禁用 |
| 3 | **一.4 状态查询失败应进入"状态未知"、启动禁用、显示"重新同步"** | 后端 `get_agent_status` 实现上**永不返回 `Err`**（`lib.rs:365-379`），现实中无法触发；注入失败需要改代码或启用远程调试。 | `syncStatus` 的 `.catch` 执行 `setPhase("unknown")` + 红字（`App.tsx:178-182`）；`showRetry` 为 `phaseState === "unknown"`（`App.tsx:312`）→ 显示"重新同步"；`canStart` 对 unknown 为 false → 启动禁用 |
| 4 | **"开发模式下控制台无 React 警告"** | 无法读取 WebView2 控制台：<br>① 本构建按 F12 与 Ctrl+Shift+I 均**不打开 DevTools**（实测无 DevTools 窗口出现）；<br>② 以 `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9222` 启动后，WebView2 子进程命令行中**不存在** `--remote-debugging-port` 参数，9222 端口未监听——说明该环境变量被 Tauri 自身的 `additionalBrowserArgs` 覆盖，CDP 不可用。 | **该验收点的另一半"输出不重复"已确证**：31 行严格等于 29 OUT + 2 ERR，每行恰好一次，StrictMode 双挂载未造成监听器残留或重复渲染。 |

---

## 9. 关键证据附录

### 9.1 终端输出节选

**二、Node 测试脚本完整轨迹（节选）**

```
CLICK '启动' -> Invoke() ok
TRACE t=   222ms  status='运行中' OUT=0  ERR=0 start=disabled
TRACE t=   634ms  status='运行中' OUT=2  ERR=0 start=disabled
TRACE t=  1031ms  status='运行中' OUT=3  ERR=0 start=disabled
...
TRACE t=  4732ms  status='运行中' OUT=10 ERR=0 start=disabled
TRACE t=  5156ms  status='运行中' OUT=10 ERR=1 start=disabled   ← 第 10/30 行 (stderr)
...
TRACE t=  8963ms  status='运行中' OUT=17 ERR=2 start=disabled   ← 第 18/30 行 (stderr)
...
TRACE t= 14932ms  status='运行中' OUT=28 ERR=2 start=disabled
TRACE t= 15404ms  status='已完成' OUT=29 ERR=2 start=enabled
LINES total=31
     1. OUT|[Node 测试] 共 30 行，每 500ms 输出一行
     2. OUT|[Node 测试] 第 1/30 行（来自 stdout）
     ...
    11. ERR|[Node 测试] 第 10/30 行（来自 stderr）
     ...
    19. ERR|[Node 测试] 第 18/30 行（来自 stderr）
     ...
    31. OUT|[Node 测试] 第 30/30 行（来自 stdout）
```

**二.8 快速连点 3 次**

```
PROCCOUNT test-agent.js = 0
RAWDBL '启动' 3 rapid clicks at (961,336)
PROCCOUNT test-agent.js = 1
  pid=18604 name=node.exe cmd="node" test-agent.js
...（输出仍为 500ms/行，未翻倍）
```

**四.3 收尸窗口**

```
CLICK '启动' -> Invoke() ok
REAPWIN: saw 31 lines at 15071ms -> clicking 终止 now
REAPWIN: Invoke 终止 -> ok
STATE status='已完成' start=enabled stop=disabled retry=absent
```

**六、刷新恢复**

```
CLICK '启动' -> Invoke() ok
STATE status='运行中' start=disabled stop=enabled lines(OUT=12 ERR=1)
KEYS '{F5}'
TRACE t=   162ms  status='<unknown>' OUT=0 ERR=0 start=disabled   ← 页面重载中
TRACE t=   617ms  status='运行中'  OUT=1 ERR=0 start=disabled   ← 恢复为运行中
...
TRACE t=  8654ms  status='已完成'  OUT=16 ERR=1 start=enabled   ← 自然结束，未卡住
```

**八.1 边界检查**

```
BOUNDS window=(80,60)-(980,660) size=900x600
BOUNDS maxTextRight=948 maxTextBottom=640  (window right=980 bottom=660)
BOUNDS overflow: NONE
BOUNDS button '启动' rect=(747,248)-(835,285)
BOUNDS button '终止' rect=(843,248)-(931,285)
BOUNDS combo rect=(129,248)-(735,285)
```

### 9.2 截图清单

全部位于 `C:\Users\Tang\AppData\Local\Temp\shots\`：

| 文件 | 说明 |
|---|---|
| `01-idle.png` | 初次打开：空闲、启动可用、终止禁用、空状态 |
| `02-after-load.png` | 页面加载后状态 |
| `03-node-finished.png` | **31 行 + 绿色"已完成"**（关键证据） |
| `04-autoscroll-bottom.png` | 底部时无"回到最新"按钮 |
| `05-scrolled-up.png` | **第 10/30 行 `ERR` 显示为红色**，右下角出现"回到最新"（关键证据） |
| `06-back-to-latest.png` | 点击后回到输出末尾 |
| `07-fast-exit.png` | 快速退出：1 行 + 绿色"已完成" |
| `08-terminated.png` | **终止：灰色圆点 + 灰字"已终止"**（关键证据） |
| `09-restart-after-natural-end.png` | 自然结束后立即重启成功 |
| `10-reap-window.png` | 收尸窗口点终止 → 绿色"已完成" |
| `11-ping.png` | Ping 中文输出无乱码、27 行 |
| `12-refresh-recovery.png` | F5 后恢复"运行中" |
| `13-rapid-click.png` | 连点 3 次后仍只有单进程 |
| `14-900x600-light.png` | **900×600 无横向溢出**（关键证据） |
| `15-dark-900x600.png` | **深色模式整体**（关键证据） |
| `16-dark-scrolled.png` | 深色模式滚动 + "回到最新"按钮 |
| `17-dark-stderr.png` | **深色模式下 stderr 行仍为红色**（关键证据） |

### 9.3 验收过程产生的日志与辅助脚本（均在 `%TEMP%`，未进入项目目录）

| 路径 | 用途 |
|---|---|
| `C:\Users\Tang\AppData\Local\Temp\agenthub-drive.ps1` | UIA 驱动脚本（读状态 / 点击 / 选择预设 / 抓图 / 边界检查 / 竞态探测） |
| `C:\Users\Tang\AppData\Local\Temp\agenthub-drive-log.txt` | 每次操作与状态快照的完整时间戳日志 |
| `C:\Users\Tang\AppData\Local\Temp\agenthub-residue-check.ps1` | 进程残留检查（按命令行筛选 `test-agent\.js`） |
| `C:\Users\Tang\AppData\Local\Temp\agenthub-uia-dump.ps1` / `agenthub-uia-raw.ps1` | 无障碍树导出 |
| `C:\Users\Tang\AppData\Local\Temp\agenthub-theme.ps1` | 深色模式切换与还原 |
| `C:\Users\Tang\AppData\Local\Temp\agenthub-cdp.mjs` | CDP 探针（因 Tauri 覆盖参数未生效，仅作尝试记录） |
| `C:\Users\Tang\AppData\Local\Temp\run-agenthub-dev.bat` | 以 GBK 编码原样 `call` 项目启动脚本的 ASCII 名包装（启动脚本本身未改动） |

---

## 10. 是否修改过项目文件

**没有修改、创建或删除任何项目文件。**

- `find . -newermt "2026-09-13 01:47" -not -path "./node_modules/*" -not -path "./src-tauri/target/*"` 的结果只有目录自身，**无任何文件**。
- 关键文件时间戳全部保持验收前原值：
  - `src/App.tsx` → 2026-09-13 01:14:35
  - `src/index.css` → 2026-09-13 01:14:47
  - `src/main.tsx` → 2026-09-13 00:30:22
  - `src-tauri/src/lib.rs` → 2026-09-13 01:19:49
  - `src-tauri/tauri.conf.json` / `Cargo.toml` / `package.json` → 2026-09-12 23:44:45
  - `scripts/test-agent.js` → 2026-09-12 23:54:06
  - `启动开发预览.bat` → 2026-09-13 00:24:16
- 唯一变化：执行验收要求的 `npm run build`（以及 `npm run tauri dev`）时重新生成了 `dist/` 与 `src-tauri/target/` 构建产物。
- 系统深色模式已还原为原值（`AppsUseLightTheme=1`）。
- 未结束或杀死任何与项目无关的 `node.exe` 进程；残留检查严格按命令行筛选 `test-agent\.js`，全程未把 Codex / Pi / Vite 的 node 进程误判为项目残留。
- 本报告文件本身是应要求新生成的验收文档（`阶段1-验收报告.md`），不属于对项目源码的修改。

---

## 11. 总体判断

阶段 1 的**全部核心验收点实测通过，零功能缺陷**：

- **流式输出链路正确**：逐行实时到达、顺序正确、stderr 着色正确、31 行无重复（StrictMode 监听器残留问题确实被 `mountGenRef` 方案解决）、最后几行确实先于状态变化到达。
- **秒退竞态确实被修复**："Node 快速退出"预设稳定复现秒退场景，界面正确落到绿色"已完成"，未卡在"启动中"或"运行中"。
- **终止链路健壮**：幂等、连续点击不翻红，且**收尸窗口被实测命中**并正确显示绿色"已完成"而非红色失败。
- **刷新恢复正确**：F5 后重新挂上运行中的进程（输出区为空符合设计），自然结束时正确收尾，未永久卡"运行中"。
- **退出后无虚假 alreadyRunning**：自然结束后 78ms 内重启成功。
- **窗口与深色模式合格**：900×600 无横向溢出/重叠/控件挤出；深色模式无亮白断层，状态色与 stderr 红字均可读。
- **关闭清理彻底**：Node 与 Ping 两种子进程在关窗后均无残留，vite dev server 亦随之退出。
- **三个高风险设计点**（挂载代际编号、操作令牌、先清槽后发事件）均经受住了针对性实测。

判定为**部分通过**的唯一原因是第 8 节列出的 4 项未验证项（1 项采样精度不足、2 项防御性分支、1 项 DevTools 被本构建禁用）。这些项均已逐条给出代码位置与推理依据，**未做任何"猜测通过"**。

**建议：可以进入阶段 2。**

若希望把这 4 项也变成可实测项，最省事的两种做法：

1. 在 `tauri.conf.json` 中增加 dev-only 的 `additionalBrowserArgs`（打开 `--remote-debugging-port`），即可用 CDP 注入故障并读取控制台；
2. 在阶段 2 增加一个隐藏自检预设或调试开关，用于触发 `alreadyRunning` / `spawnFailed` / 订阅失败 等分支。
